\vspace{-4mm}
\section{Introduction}
\label{sec:intro}
% \captionsetup[table]{aboveskip=2pt, belowskip=2pt} % 标题与表格上/下方距离
\begin{figure}[t]
    \centering
    \includegraphics[width=0.98\linewidth]{graphs/baslineworkflow.pdf}
    \vspace{-5pt}
% \vspace{-5mm}
    \caption{Representative workflow of four LLM reasoning paradigms.}
    \label{fig:intro_demo}
% \vspace{-1em}
    \vspace{-5pt}
    
\end{figure}
    % \vspace{-5pt}

Large Language Models (LLMs) have demonstrated strong performance across a wide range of tasks by scaling to billions of parameters and pre-training on massive and diverse text corpora~\cite{brown2020language}. 
However, due to the prohibitive cost of retraining, these models are inherently static.
% —once trained, their knowledge quickly becomes outdated. 
This limitation results in factual gaps, temporal inconsistencies, and reasoning hallucinations when LLMs are required to process time-sensitive or evolving information~\cite{petroni2020kilt, talmor2018commonsenseqa,khot2022decomposed}.

Retrieval-Augmented Generation (RAG) has emerged as an effective paradigm to mitigate these limitations by allowing LLMs to access external information sources during inference~\cite{ huang2025survey, huang2025embedding,zheng2025lifting}. 
Typical RAG systems embed both questions and documents into a shared vector space and retrieve semantically similar passages~\cite{tog2.0ma2024think, tan2025hydra}. 
Although effective in many cases, such retrieval often prioritizes semantic similarity while neglecting structural and temporal dependencies across entities and events \cite{jia2024faithful}. 
For instance, two temporally distinct statements such as “Barack Obama is the President of the United States (2009–2017)” and “Joe Biden is the President of the United States (2021–)” may appear semantically related, yet they cannot simultaneously hold true. 
Plain-text retrieval often fails to distinguish between such temporally exclusive facts, leading to incorrect or contradictory reasoning chains.

Temporal reasoning further amplifies these challenges. 
Real-world entities and relationships evolve continuously, i.e., people change roles, organizations merge, and events unfold in sequence over time \cite{liang2023learn, ARI}. 
Temporal-aware questions therefore often involve implicit time constraints, multiple dependent conditions, and mixed temporal granularities such as “Who chaired the committee before the 2010 reform?” or “Which country hosted the first summit after 2015?”. 
These questions require reasoning that integrates both temporal and semantic alignment, which remains difficult for text-based RAG methods that lack structured temporal awareness.

To address this issue, integrating Temporal Knowledge Graphs (TKGs) with LLM reasoning provides a promising direction \cite{jia2024faithful,T4, ARI, khot2022decomposed, qianyihu-etal-2025-time}. 
TKGs encode factual knowledge as quadruples (subject, relation, object, timestamp), offering explicit temporal grounding and relational structure. 
By leveraging TKGs, models can reason over evolving entities while maintaining factual and temporal consistency. 
Temporal Knowledge Graph Question Answering (TKGQA) serves as a representative evaluation for this task, requiring systems to answer natural language questions by retrieving temporally relevant facts from TKGs.

\myparagraph{Challenges in existing methods}
Most existing TKG-based reasoning frameworks follow a ``plan-retrieve-answer'' pipeline. 
In this paradigm, LLMs decompose complex temporal questions into a series of sub-tasks, retrieve related facts from a TKG, and generate an answer based on the retrieved context. 
While this improves interpretability and modularity, several challenges face. 

% \myparagraphunderline{Challenge 1: Temporal faithfulness in multi-hop chains} 
% Exsting method still privilege semantic proximity over temporal validity. 
% Starting from one-hop neighbors and scoring by textual similarity often yields paths that look topical but are chronologically wrong. 

% \myparagraphunderline{Challenge 1: Temporal coherence in multi-hop and multi-entity reasoning}  
% Many temporal QA systems optimize for semantic proximity rather than temporal correctness, often retrieving paths that appear relevant but violate time constraints.  
% For instance, answering “Who chaired Company X before the 2018 divestiture?” may return (Person A, Chair@2019) because of lexical overlap, ignoring the before relation.  
% Similarly, when a question involves multiple entities—e.g., “Which coach led Team A when Player B transferred from Club C?”—existing methods explore each entity separately and later merge partial evidence, producing candidates that never share a consistent timeline.  
% Effective temporal reasoning must therefore enforce monotonic timestamps across hops, synchronize all entities on a unified temporal axis, and apply operator-aware filtering early to prevent over-generation and timeline drift.
% \myparagraphunderline{Challenge 1: Temporal faithfulness in multi-hop reasoning}  
% Most temporal QA pipelines textasize semantic similarity rather than chronological correctness.  
% Expanding from one-hop neighbors and ranking by embedding proximity often yields paths that appear relevant but violate time constraints.  
% For example, answering “Who chaired Company X before the 2018 divestiture?” may return (Person A, Chair@2019) because of lexical overlap, despite breaking the before relation.  
% In multi-hop queries like “Who did X hire after Y resigned during Q3 2017?”, locally valid hops can combine into a globally inconsistent chain unless timestamps remain monotonic.  
% % As temporal KGs grow denser, naive expansion causes combinatorial explosion, making early operator-aware filtering and adaptive depth control essential.

% \myparagraphunderline{Challenge 2: Multi-entity temporal synchronization}  
% When questions mention multiple entities, existing methods explore them independently and merge evidence later, often producing candidates that never co-occur on a single, time-consistent path.  
% For example, in “Which coach led Team A when Player B transferred from Club C?”, independent exploration can yield facts for each entity but misalign their temporal windows.  
% % A robust TKGQA system must co-constrain all topic entities along one reasoning path, align their temporal bounds (e.g., day, month, year), and preserve consistent event order to avoid timeline drift.

% \myparagraphunderline{Challenge 3: Operator diversity and adaptive retrieval}  
% Temporal questions involve diverse operators, each requiring a distinct retrieval policy.  
% Single-round question rewriting converts implicit constraints to explicit timestamps, but struggles with combined constraints.
% Fixed templates or one-size-fits-all retrievers cannot adapt to these variations.
% Misidentifying operators or using static retrieval parameters leads to under- or over-coverage.  
% % Effective reasoning therefore requires adaptive toolkit selection and parameter tuning conditioned on the detected operator.

% \myparagraphunderline{Challenge 4: Lack of reasoning experience management}  
% Most pipelines remain memoryless, discarding experience from prior runs.  
% they inadequate for diverse practical tasks and require substantial efforts in time and human to acquire highquality examples,
% fail to provide necessary guidance for ongoing temporal reasoning processes and are susceptible to incorporating extraneous noise
% Without a persistent memory, models relearn similar reasoning repeatedly, wasting tokens and causing unstable results.  
% Recurring sub-questions across temporal types (e.g., first and during) cannot benefit from prior success unless experience is stored and retrieved under operator constraints.  
% A memory mechanism that reuses matching experience and learns from new cases is crucial for efficiency and stability.


% \myparagraphunderline{Challenge 1: Temporal faithfulness and multi-entity synchronization}
% \noindent\underline{Challenge 1: Temporal faithfulness and multi-entity synchroniza-} \underline{tion.}
% Most temporal QA systems optimize for semantic similarity rather than temporal correctness.  
% Expanding from one-hop neighbors and ranking by embedding proximity often yields paths that appear relevant but violate chronological constraints.  
% For example, answering “Who chaired Company X before the 2018 divestiture?” may retrieve (Person A, Chair, 2019) due to lexical overlap, despite contradicting the before relation.  
% The problem worsens in multi-hop or multi-entity settings, such as “Which coach led Team A when Player B transferred from Club C?”, where independent exploration for each entity generates partially valid facts that never align on a consistent timeline.  
% Ensuring temporal faithfulness thus requires early operator-aware filtering, synchronized reasoning across entities, and monotonic timestamp constraints throughout the reasoning chain.


\myparagraphunderline{Challenge 1: Temporal faithfulness in multi-hop reasoning}  
Most temporal QA pipelines\cite{jia2024faithful,T4, ARI}, as shown in Figure \ref{fig:intro_demo}(a), prioritize semantic similarity over chronological accuracy.  
Expanding from one-hop neighbors and ranking by semantics often retrieves paths that appear contextually relevant but violate time constraints.  
For example, answering “Which country did X last visit before 2020?” may simply return (X, visit, Y, 2019) because of lexical overlap, despite contradicting the before-last relation.  
In multi-hop queries such as “Who did X hire after Y resigned during Q3 2017?”, locally valid hops can combine into a globally inconsistent reasoning chain.
% unless monotonic timestamp ordering is strictly enforced.  
% Ensuring temporal faithfulness therefore requires operator-aware filtering and adaptive depth control during path exploration.

\myparagraphunderline{Challenge 2: Multi-entity temporal synchronization}  
When a question contains multiple entities, most systems \cite{T4,  tog1.0sun2023think,plan-on-graph} explore each entity independently and attempt to merge partial evidence later.  
This disjoint process often produces candidates that never align within a single, time-consistent reasoning path.  
For instance, as shown in Figure \ref{fig:intro_demo}(b), independent exploration can yield valid facts for each entity, but fails to synchronize their temporal windows.  
% A robust TKGQA system must co-constrain all topic entities on the same path, align temporal bounds (e.g., day, month, year), and maintain consistent event order to prevent timeline drift and cross-entity inconsistency.

\myparagraphunderline{Challenge 3: Operator diversity and adaptive retrieval}  
Temporal questions encompass diverse operators, each requiring a distinct reasoning policy.  
Previous methods \cite{saxena2021question,T4} focus on 
single-round rewriting and can expose explicit timestamps but struggle with combined constraints, as shown in Figure \ref{fig:intro_demo}(c).  
% A static template and one-size-fits-all retriever 
It prevents the model from adapting to such heterogeneity, resulting in either under-coverage (missing valid evidence) or over-retrieval (noisy results).  
% Effective temporal reasoning thus demands adaptive toolkit selection and parameter tuning tailored to each operator.

\myparagraphunderline{Challenge 4: Lack of reasoning experience management}  
Most existing pipelines \cite{qianyihu-etal-2025-time, T4, tog2.0ma2024think, pogtan2025paths} remain memoryless, discarding successful reasoning traces after each run.  
They often rely on manually crafted or static exemplars, which are costly to construct and insufficiently generalizable across diverse temporal question types, leading models repeatedly re-solve similar sub-questions from scratch, failing to transfer prior knowledge across operators or tasks.  
% This leads to redundant exploration, excessive token usage, and unstable reasoning outcomes over time.  
% A structured and retrievable experience memory is therefore essential to accumulate, reuse, and refine successful reasoning behaviors for continual improvement.

% This leads to redundant exploration, inefficient token usage, and unstable reasoning outcomes.  Models repeatedly solve similar sub-questions from scratch and fail to transfer knowledge across temporal types.  
% A reusable and searchable experience memory is essential for improving efficiency, stability, and continual adaptation.



% In the following sections, we further analyze these challenges and introduce \mot — a Memory-Augmented Temporal Knowledge Graph framework designed to enhance LLM reasoning. 
% \mot performs dynamic temporal decomposition, experience-guided retrieval, and memory-based refinement, enabling LLMs to reason faithfully, efficiently, and consistently over time-evolving knowledge.
% Large Language Models (LLMs) have achieved remarkable performance by scaling to billions of parameters and pre‑training on vast and diverse corpora
% \cite{brown2020language,chowdhery2023palm}.
% However, the prohibitive expense of full-model training for LLMs makes continual retraining infeasible, causing static parametric knowledge to quickly become obsolete and resulting in factual gaps and hallucinations \cite{besta2024graphGoT,touvron2023llama}.
% This issue is alleviated by retrieval-augmented generation (RAG), which fetches external evidence at inference time.
% \cite{gao2023retrieval}.

% Large Language Models (LLMs) have achieved remarkable progress across diverse tasks by scaling to billions of parameters and pre-training on vast and heterogeneous corpora~\cite{brown2020language,chowdhery2023palm}. 
% However, their static parametric nature poses an inherent limitation: once trained, the embedded knowledge quickly becomes outdated, while full retraining or continual fine-tuning is computationally prohibitive~\cite{besta2024graphGoT,touvron2023llama}. 
% As a result, LLMs frequently exhibit \text{factual decay}, \text{knowledge gaps}, and \text{hallucinated reasoning}, especially when faced with dynamic or temporally evolving information.

% Retrieval-Augmented Generation (RAG) has emerged as an effective paradigm to address these issues by equipping LLMs with external evidence access during inference~\cite{gao2023retrieval}. 
% In typical RAG pipelines, both questions and documents are embedded into a shared dense vector space, enabling similarity-based retrieval of relevant passages~\cite{baek2023knowledge,jiang2023structgpt,huang2024embedding,huang2024foundation}. 
% While efficient for textual matching, this paradigm struggles with questions requiring multi-step logical or temporal reasoning, where answers depend not merely on semantic proximity but also on the correct temporal and relational ordering of events. 
% For example, plain-text retrieval may conflate different mentions of the same entity (\textit{e.g.}, ``U.S. President'' vs.~``Barack Obama’’) or treat temporally distinct facts (\textit{e.g.}, ``CEO in 2010’’ vs.~``CEO in 2018’’) as equivalent due to surface-level similarity. 
% Consequently, the retrieved context can be semantically plausible yet temporally inconsistent, leading to incorrect or contradictory reasoning chains.

% Temporal reasoning further amplifies these challenges.  
% Real-world entities and relations evolve over time—people change positions, organizations merge, and events unfold in sequence.  
% Temporal-aware questions thus often involve \text{implicit constraints} (“before the merger”), \text{multi-hop dependencies} (“who succeeded the person who signed the agreement”), and \text{multi-granular timestamps} (“in Q2 2017”, “during 2008–2010”).  
% Such queries require both fine-grained temporal grounding and structured reasoning over evolving factual evidence—capabilities that text-based RAG pipelines inherently lack.

% To address these shortcomings, integrating \textbf{Temporal Knowledge Graphs (TKGs)} into LLM reasoning provides a promising solution.  
% TKGs store factual triples annotated with timestamps, enabling explicit modeling of temporal dependencies and evolution patterns.  
% Leveraging TKGs allows LLMs to perform temporally faithful reasoning grounded in verifiable event sequences rather than static text correlations.  
% \textbf{Temporal Knowledge Graph Question Answering (TKGQA)} is therefore an ideal evaluation setting: it challenges systems to answer natural-language questions by retrieving temporally relevant facts and reasoning across time-aware relationships.


% Many RAG systems rely on vector retrieval over text, embedding question and documents into a dense space and selecting semantically similar passages \cite{baek2023knowledge,jiang2023structgpt,huang2024embedding,huang2024foundation}. 
% While effective for measuring text similarity, such approaches struggle with complex reasoning that requires integrating heterogeneous clues across multiple text corpous and efficiently processing and
% understanding tasks that involve temporal information \cite{tog2.0ma2024think}. 
% For example, in the plain text RAG, it may ocurs multiple entity refer the same real meaing  of an entity and multiple text chunck refer to the same entity but because of the sematic matching making them appered differentlt. This issue is extending to the termporal-aware question, majority method focus on the sematic information overlook the complex temporal operators inside.
% However, In the real world, entities and relationships evolve dynamically.
% complex queries in tempporal-aware quries is challenging, including implicit temporal constraints,multi-constraint combinations, multi-hop reasoning, and multi-granular time. Such queries are highly relevant to real-world applications.


% To address these challenges, incorporating external knowledge sources like Temoporal Knowledge Graphs (TKGs) is a promising solution to enhance LLM tempooral-aware reasoning. TKGs offer abundant temroiral factual knowledge in a structured format, serving as a reliable source to improve LLM time-aware capabilities.  
% Temporal Knowledge Graph Question Answering (TKGQA) serves as an approach for evaluating the integration of TKGs with LLMs,
% which requires machines to answer natural language questions by retrieving relevant facts from KGs.

% \myparagraph{Limitations of existing methods}
% Current approaches typically follow a simple \texttt{plan-retrieval-answering} routine. In this approach, LLMs are prompted to decompose complex reasoning tasks into a series of sub-tasks, forming a \texttt{plan}. Simultaneously, external TKGs are embedded to retrieve the temporal facts to answer each step of the plan. 
% Despite their success, they still face limitations as follows.


% such as handling multi-hop reasoning problems, addressing questions with multiple topic entities, and effectively utilizing the structural information of graphs.






% Datasets have catalyzed the field. 
% \textbf{CronQuestions} introduced a large benchmark built directly over a temporal KG with questions stratified by structural complexity (e.g., simple time lookup, time joins, and ordering)~\cite{TemporalKGQA_CRON}. 
% More recently, \textbf{MusTQ} textasizes multi-step temporal reasoning rather than isolated fact lookup, revealing that many systems still underperform on compositional operators even when entity linking is correct.
% Outside curated KGs, timeline-centric generators such as \textbf{TimelineKGQA} systematically probe LLMs on timeline construction, time-aware comparison, and multi-entity synchronization, underscoring the need for reasoning that respects both semantics and chronology.  
% Meanwhile, the community continues to refine the problem landscape and taxonomies of temporal questions and methods

% In parallel, KG-enhanced LLM frameworks have shown that explicit graph structure can mitigate hallucination and improve \text{traceability} of answers. 
% For example, Think-on-Graph (ToG) treats the LLM as a planner that explores and prunes KG paths via a beam-search loop; this yields stronger multi-hop reasoning while exposing the evidence chain~\cite{sun2023tog}.  
% However, such pipelines are largely \text{static}: the exploration policy and pruning heuristics are fixed per run, and successful runs are not distilled into a reusable memory that can accelerate or improve future reasoning. 
% Very recent variants (e.g., ToG-3.0) introduce more adaptive retrieval orchestration, reinforcing the trend toward dynamic, controller-style KG reasoning.  

% \myparagraph{Why temporal QA remains hard.}
% Temporal questions require aligning \text{what} happened with \text{when} it happened. 
% Concretely, a faithful chain must (i) satisfy the temporal operator (e.g., \text{``X before Y''} demands strict ordering), (ii) remain \text{monotone in time} along the reasoning path (no backward hops that violate chronology), and (iii) synchronize multiple entities across hops (e.g., two clubs and three transfer events on a timeline). 
% Moreover, the best strategy varies by question: one item may need an \text{interval containment} operator over a career span; another may require \text{first/last} selection among repeated events; a third may combine \text{range filtering} with \text{event ordering}. 
% These diverse patterns demand a system that can \text{choose} the right temporal operator, \text{retrieve} aligned evidence, and \text{explain} the reasoning as a time-consistent path.

% \myparagraph{Limitations of current approaches.}
% Despite progress, we identify three persistent limitations in existing KG- or RAG-enhanced LLM pipelines for temporal QA:

% \myparagraphunderline{Challenge 1: Temporal faithfulness in multi-hop chains} 
% Many pipelines still privilege \text{semantic proximity} over \text{temporal validity}. 
% Starting from one-hop neighbors and scoring by textual similarity often yields paths that look topical but are chronologically wrong. 
% For example, answering \texttt{“Who chaired Company X \textbf{before} the 2018 divestiture?”} by expanding from \texttt{(Company X, event, divestiture@2018)} may surface \texttt{(Person A, role, Chair@2019)} because it shares entities/keywords, yet it violates the \textsc{before} constraint. 
% In multi-hop settings (e.g., \texttt{X hired Y \textbf{after} Z resigned \textbf{during} Q3 2017}), local hops that are individually plausible can compose into a globally inconsistent chain unless the path enforces \text{monotone, non-decreasing timestamps} at every step. 
% As KGs grow denser, naive breadth-first expansion explodes combinatorially, making early, operator-aware \text{temporal filtering} and depth control mandatory to avoid over-generation and missed gold paths.

% \myparagraphunderline{Challenge 2: Multi-entity temporal synchronization} 
% When a question mentions multiple entities, most systems explore them \text{independently} and later try to stitch partial evidence. 
% This decoupling invites spurious candidates that never co-occur on a single, time-consistent chain. 
% Consider \texttt{“Which coach led Team A \textbf{when} Player B transferred from Club C?”}. 
% Exploring around \texttt{Team A} and \texttt{Player B} in isolation can return valid facts for each, yet fail to synchronize the \text{same season window} or \text{match day granularity}, producing answers that are individually correct but jointly incoherent. 
% Robust TKGQA must \text{co-constrain} all topic entities on the same path, enforce shared temporal bounds (\text{day/month/year}), and preserve order across hops to prevent entity drift and timeline mismatch.

% \myparagraphunderline{Challenge 3: Operator diversity and adaptive retrieval} 
% Temporal questions span heterogeneous operators—\textsc{before/after}, \textsc{during}, \textsc{between}, \textsc{first/last/n-th}, duration comparison, timeline construction—each implying a \text{different} retrieval and pruning policy. 
% Fixed “plan–retrieve” templates struggle when the true operator demands a specialized strategy: 
% \textsc{first} requires global ordering over repeated events; \textsc{between} calls for tight interval filters; \textsc{during} needs containment checks at the right granularity (e.g., \texttt{during Q2 2020}). 
% Mis-typing the operator (or using a one-size-fits-all retriever) leads to either under-coverage (gold path excluded) or over-inclusion (noisy candidates that swamp the judge). 
% A practical system must \text{adaptively select} operator-specific toolkits, parameters, and depth/width budgets conditioned on the question signal.

% \myparagraphunderline{Challenge 4: No reusable experience, unstable decisions} 
% Most pipelines discard \text{how} a question was solved—useful decomposition patterns, seeds that worked, toolkits that succeeded for a given operator, and which paths passed sufficiency. 
% Without a persistent, searchable memory (with embeddings for the \text{original question} and \text{indicator}, type labels, and hit/time statistics), the system repeatedly explores unpromising regions, wastes LLM tokens, and exhibits run-to-run variability. 
% Crucially, sub-questions that recur across different temporal types (e.g., \textsc{first} and \textsc{during} both invoking the same seed/operator motif) cannot benefit from prior success unless experience is stored \text{and} retrieved under operator constraints. 
% Closing this loop—\text{reuse when it matches, learn when it does not}—is essential for both efficiency and stability.
% \myparagraphunderline{(L1) Insufficient temporal sensitivity and path faithfulness.}
% Many systems score or rank evidence by semantic similarity alone, with temporal checks relegated to a late or weak filter. 
% This often yields paths that look topical but violate chronological constraints, or that interleave events at incompatible granularities (day vs.\ month vs.\ year). 
% Surveys and benchmarks repeatedly report such mismatches between semantic plausibility and temporal validity in TKGQA

% \myparagraphunderline{(L2) Fixed pipelines with limited adaptivity.}
% Plan–retrieve–answer routines typically apply a \text{single} retriever and a \text{single} pruning recipe across all questions. 
% When retrieval under-covers the correct time-aligned path, these systems either restart from scratch or escalate depth uniformly, increasing cost without informed guidance. 
% Even when beam search is used (e.g., ToG), the controller rarely conditions on \text{past successful trajectories}, leading to repeated exploration of unpromising regions.  

% \myparagraphunderline{(L3) Absence of long-term experience and structured memory}
% Most pipelines discard evidence about \text{which} decomposition templates, seeds, or toolkits were effective for a given \text{temporal type} and \text{indicator}. 
% Without a memory that stores embeddings for questions and indicators (plus sufficiency outcomes and timestamps), systems cannot prioritize strategies that previously worked for the same operator class (e.g., \textsc{first}/\textsc{last}) or similar temporal graphs. 
% As a result, they pay a recurrent cost in exploration and suffer from unstable decisions across sessions~\



\myparagraph{Contribution} 
In this paper, we introduce \textbf{\mot}, a \text{Memory-Augmented Temporal Knowledge Graph} framework designed to enhance LLM temporal reasoning, as shown in Figure \ref{fig:intro_demo}(d).  
Unlike existing methods that rely on static retrieval or pre-defined templates, \mot integrates structured temporal grounding, hierarchical reasoning, dynamic toolkit invocation, and continual memory updating into a unified framework.  
It enables LLMs to reason faithfully over time-aware facts, adapt retrieval strategies to temporal operators, and progressively improve performance through experience reuse.  
% An overview of the \mot framework is shown in Figure~\ref{fig:overall}, consisting of four coordinated layers: temporal grounding, hierarchical reasoning, memory experience management, and hybrid retrieval.  

% \myparagraphunderlinenew{To address the multi-hop temporal reasoning problem}  
% \mot introduces an operator-aware hierarchical reasoning mechanism that decomposes complex temporal questions into structured, hierarchical sub-questions.  
% Each node captures a distinct temporal relation and indicator, allowing reasoning to proceed stepwise along a multi-hop temporally monotonic paths.  
% % Each node explicitly encodes a temporal operator and constraint, enabling stepwise reasoning along a time-consistent chain.  
% % Guided by temporal indicators and depth prediction, \mot retrieves multi-hop reasoning paths that preserve monotonic timestamps across hops.  
% Unlike previous models that expand from local neighborhoods based purely on semantic similarity, \mot enforces time-consistent multi-hop reasoning through explicit temporal indicators and depth prediction.  
% % This design ensures that every reasoning path preserves chronological order and leads toward globally coherent temporal conclusions.


\myparagraphunderlinenew{To address multi-hop temporal reasoning problem}  
\mot introduces a hierarchical reasoning framework that decomposes complex temporal questions under a unified global plan. 
All sub-indicators are evolved from the root indicator of the main question, ensuring that each branch inherits consistent temporal constraints and preserves monotonic timestamp progression.  
Guided by this global supervision, \mot performs controlled branch expansion, retrieving multi-hop reasoning paths remaining faithful to both semantic relevance and chronological order.  

\myparagraphunderlinenew{To handle multi-entity temporal synchronization}  
\mot ensures that the final reasoning path jointly incorporates all topic entities under a unified temporal framework and synchronized timeline.  
Instead of exploring entities independently, the system retrieves co-constrained evidence paths that preserve shared temporal bounds and consistent granularity, ensuring global coherence and containing the factual basis for the correct answer.
% This unified supervision prevents timeline drift and entity desynchronization, enabling coherent evidence construction where all entities contribute to a single, temporally faithful reasoning chain.

\myparagraphunderlinenew{To adapt to diverse temporal operators}  
\mot introduces a library of temporal reasoning toolkits that support heterogeneous operators.
% , including before/after, during, between, first/last/n-th, and duration comparison.  
Instead of fixed templates, \mot adaptively selects the most suitable toolkit through experience-guided prompts.
% conditioned on the question’s operator type. 
This adaptive retrieval policy ensures that each temporal operator triggers an appropriate reasoning strategy, thereby improving precision and reducing over-retrieval noise.

\myparagraphunderlinenew{To manage and reuse reasoning experience}  
\mot maintains a continuously evolving experience memory that records successful records.  
Each experience entry is stored alongside embeddings of both the question and its temporal indicator, enabling efficient similarity-based retrieval under operator and type constraints.  
During inference, \mot retrieves relevant exemplars to guide new reasoning, while after execution, verified trajectories are written back. 
% This accumulates problem-solving knowledge over time, reducing redundant exploration and stabilizing LLM decision-making.
% , and improving efficiency.
% In summary, \mot offers four key advantages:
The advantage of \mot can be abbreviated as follows:
\begin{itemize}[leftmargin=*]

\item \textbf{Memory-augmented temporal reasoning.} 
% Integrating dynamic memory retrieval and update into temporal question decomposition, enabling long-term reuse of reasoning trajectories.
MemoTime introduces a unified framework that integrates dynamic memory retrieval and update into temporal-aware question decomposition,  
allowing the model to recall, reuse, and refine reasoning trajectories for long-term temporal understanding.
\item \textbf{Hierarchical and interpretable control.}  
A hierarchical controller Tree of Time executes, verifies, and refines sub-questions, ensuring faithful and interpretable temporal reasoning.

\item \textbf{Hybrid retrieval and pruning.}  
An operator-aware retrieval layer combines symbolic graph expansion with embedding search, applying temporal-first pruning and semantic re-ranking for 
both chronological validity and precise evidence grounding.
% precise evidence grounding.
\item \textbf{Self-evolving experience memory.}  
Verified reasoning responses are continuously organized in an adaptive experience pool, forming a closed feedback loop for continual improvement.

\item\textbf{Efficiency and adaptability}:  
a) \mot is a plug-and-play framework that can be seamlessly applied to various LLMs and TKGs.
% \underline{Resource‑savvy}: subgraph detection and beam‑controlled search halve token usage and cut retrieval latency;
b) \mot is auto-refresh. New information is incorporated instantly via TKG retrieval instead of costly LLM fine-tuning.
c) \mot achieves state-of-the-art results on all the tested datasets, surpasses the strong baseline by up to 24.0\%, and enables smaller models (e.g., Qwen3-4B) to achieve reasoning performance comparable to GPT-4-Turbo.
\end{itemize}
% \begin{itemize}[leftmargin=*]

% \item \textbf{Memory-augmented temporal reasoning.}  
% MemoTime introduces a unified framework that integrates dynamic memory retrieval and update into temporal-aware question decomposition,  
% allowing the model to recall, reuse, and refine reasoning trajectories for long-term temporal understanding.

% \item \textbf{Hierarchical and interpretable reasoning control.}  
% A hierarchical controller recursively executes, verifies, and refines sub-questions with operator-specific toolkits,  
% ensuring temporal faithfulness, semantic coherence, and interpretable reasoning chains across multi-hop temporal queries.

% \item \textbf{Hybrid temporal retrieval and pruning.}  
% An operatorized retrieval layer combines symbolic graph exploration with embedding-based retrieval.  
% A temporal-first pruning and semantic–temporal re-ranking mechanism ensures both chronological validity and high-quality evidence grounding.

% \item \textbf{Self-evolving experience memory for continual improvement.}  
% Verified reasoning traces, toolkit selections, and sub-question embeddings are organized into an adaptive experience pool,  
% forming a closed feedback loop that enhances retrieval precision, reasoning stability, and efficiency across sessions.

% \end{itemize}

% \begin{itemize}[leftmargin=*]

% \item \textbf{Memory-augmented temporal reasoning.}  
% MemoTime introduces a unified framework that integrates dynamic memory retrieval and update into temporal-aware question decomposition,  
% enabling the system to accumulate, recall, and reuse reasoning trajectories for long-term improvement.

% \item \textbf{Hierarchical and interpretable reasoning.}  
% A hierarchical controller recursively executes, verifies, and refines sub-questions with operator-specific toolkits,  
% maintaining semantic fidelity, temporal monotonicity, and interpretability across reasoning.

% \item \textbf{Hybrid temporal retrieval and pruning.}  
% An operatorized retrieval layer combines symbolic graph exploration with embedding-based retrieval.  
% A temporal-first pruning and semantic–temporal re-ranking strategy ensures chronologically valid, high-quality evidence for downstream reasoning.

% \item \textbf{Adaptive and self-evolving experience memory.}  
% Verified reasoning traces, toolkit configurations, and sub-question embeddings are continuously organized into a structured experience pool,  
% supporting fast retrieval, re-weighted adaptation, and stable decision-making across reasoning cycles.

% \item \textbf{Continual self-improvement and efficiency.}  
% By coupling reasoning, retrieval, and memory updates in a closed feedback loop, MemoTime continually refines its reasoning patterns,  
% achieving stable performance, reduced token usage, and enhanced generalization across temporal question types.

% \end{itemize}

% \begin{itemize}[leftmargin=*]
% \item \textbf{Memory-augmented temporal reasoning.}  
%   We introduce a unified framework that integrates dynamic memory retrieval and update with temporal-aware question decomposition, enabling long-term learning and reuse of reasoning trajectories.
  
%   \item \textbf{Hierarchical reasoning with adaptive control.}  
%   We design a hierarchical controller that recursively executes, verifies, and refines sub-questions, dynamically selecting appropriate toolkits and ensuring temporal monotonicity during reasoning.

%   \item \textbf{Hybrid temporal retrieval and pruning.}  
%   We develop an operatorized retrieval layer combining symbolic graph expansion and dense embedding retrieval.  
%   A temporal-first pruning strategy guarantees chronological validity, while semantic–temporal re-ranking and LLM-aware selection ensure high-fidelity evidence.

%   \item \textbf{Self-evolving experience memory.}  
%   Verified reasoning traces, toolkit choices, and sub-question embeddings are recorded and continuously reorganized in an adaptive experience pool, forming a structured long-term memory that enhances efficiency and accuracy across sessions.
%     \item \textbf{Faithful temporal reasoning:} Ensures chronological consistency in multi-hop reasoning by enforcing monotonic timestamps and operator-aware path validation.
%     \item \textbf{Entity-coherent evidence construction:} Aligns multiple entities under shared temporal bounds to maintain global coherence in reasoning chains.
%     \item \textbf{Adaptive and efficient retrieval:} Dynamically selects temporal toolkits and retrieval strategies tailored to each question’s operator and constraint type.
%     \item \textbf{Continual self-improvement:} Integrates reasoning, retrieval, and memory updates into a closed feedback loop that refines performance across reasoning cycles.
% \end{itemize}
% Together, these contributions position \mot as a unified, interpretable, and continually improving framework for enhancing LLM temporal reasoning over structured knowledge graphs.




% \noindent
% \textbf{Contribution.}
% In this paper,
% we introduce \textbf{\mot}, a memory-augmented framework that enhances LLM reasoning over Temporal Knowledge Graphs.
% \mot unifies four cooperative layers:
% (i) \text{Temporal Grounding} builds a $D_{\max}$-bounded temporal subgraph around topic entities and classifies the temporal operator;  
% (ii) \text{Hierarchical Reasoning} constructs and executes a hierarchical question tree, using structured indicators to route decisions;  
% (iii) \text{Temporal Facts Retrieval and Pruning} executes experience-informed toolkits, expands time-monotone paths, and performs temporal-first pruning followed by semantic–temporal re-ranking and LLM judgment;  
% (iv) \text{Experience Memory} records embeddings and traces for question types, indicators, seed/toolkit choices, sufficiency outcomes, and retrieval stats, and then \text{retrieves} them by operator-aware similarity for future runs.
% Our contributions are summarized as follows:
% \begin{itemize}[leftmargin=*]

% \item \textbf{Memory-augmented temporal reasoning.}  
% MemoTime introduces a unified framework that integrates dynamic memory retrieval and update into temporal-aware question decomposition,  
% allowing the model to recall, reuse, and refine reasoning trajectories for long-term temporal understanding.

% \item \textbf{Hierarchical and interpretable reasoning control.}  
% A hierarchical controller recursively executes, verifies, and refines sub-questions with operator-specific toolkits,  
% ensuring temporal faithfulness, semantic coherence, and interpretable reasoning chains across multi-hop temporal queries.

% \item \textbf{Hybrid temporal retrieval and pruning.}  
% An operatorized retrieval layer combines symbolic graph exploration with embedding-based retrieval.  
% A temporal-first pruning and semantic–temporal re-ranking mechanism ensures both chronological validity and high-quality evidence grounding.

% \item \textbf{Self-evolving experience memory for continual improvement.}  
% Verified reasoning traces, toolkit selections, and sub-question embeddings are organized into an adaptive experience pool,  
% forming a closed feedback loop that enhances retrieval precision, reasoning stability, and efficiency across sessions.

% \end{itemize}
% % \noindent


