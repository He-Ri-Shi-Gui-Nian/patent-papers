% \section{Background}

% \zqm{can we simply change the title to Evaluation Of Graph RAG since it is the only subtitle, or do not use subtitles at all?}

\section{Current Evaluation Method for GraphRAG}

GraphRAG evaluation consists of two main components, i.e.,  \textit{question generation} and \textit{answer evaluation}~\cite{yu2024evaluation}. 
Specifically, question generation produces a set of user queries to test the GraphRAG methods, while answer evaluation asses the quality of the answers generated by the GraphRAG methods~\cite{salemi2024evaluating}.

For question generation, MGRAG~\cite{edge2024local} and LightRAG~\cite{guo2024lightrag} employ a summary-based method with three steps. 
The first step extracts the beginnings and ends of the articles in the dataset. 
For an article, the beginning part usually outlines the topic and provides background information, while the ending part typically contains the conclusion or summary.
In the second step, the extracted segments are concatenated to form a summary of each article and a dataset description is obtained by stacking the summaries of all articles.
The third step feeds the dataset description to an LLM for question generation, using prompts like `\textit{Given the following description of a dataset, please generate questions for potential user $\cdots$}'. 


%In this framework, prior studies and evaluation efforts have predominantly adopted a rather straightforward and direct approach to compare the quality of two answers~\cite{wang2024evaluating,lyu2024crud,salemi2024evaluating,chen2024benchmarking,han2024rag,hu2024rag}. 
%This approach typically involves presenting the LLM with two answers to the same question, placed in a fixed position, and asking the model to determine which answer is superior based on predefined aspects (i.e.,\textit{Comprehensiveness}, \textit{Diversity}, \textit{Empowerment}, and \textit{Directness}).


Answer evaluation is usually conducted by using an LLM as automated judge~\cite{chang2024survey,bohnet2022attributed,kocmi2023large,lin2023llm,chiang2023can}, and two answers for the same question are compared each time~\cite{wang2024evaluating,lyu2024crud,salemi2024evaluating,chen2024benchmarking,han2024rag,hu2024rag}. In particular, the GraphRAG methods are first instructed to generate answers for a set of questions. Then, the answers produced by two methods for the same questions are paired for comparison. The LLM  is tasked with selecting a superior answer from the pair in some predefined aspects (e.g., \textit{Comprehensiveness}, \textit{Diversity}, \textit{Empowerment}, and \textit{Directness}), and each aspect comes with specific criteria as text instructions. For each aspect, the LLM selects a winner and provides an explanation justifying its decision. Subsequently, the LLM integrates the evaluations across all the aspects to determine an overall winner, e.g., using a prompt like `\textit{select an overall winner based on aspects including$\cdots$}'. For two GraphRAG methods, win rates are computed as the percentages of questions they win in the direct comparison. We note that existing researches place the answers of two GraphRAG methods in a fixed position (e.g., always `AB') in the evaluation prompt.

Some researches~\cite{fastgraphrag2024,jin2024graph} adopt datasets with predefined question-answer pairs. In these cases, answer quality can be assessed using  the similarity between the generated answers and the ground truth answers. 
However, this approach is susceptible to training data contamination~\cite{zhou2024trustworthiness}, wherein the questions and answers from public datasets may have been included in the LLM's training data, and thus it is difficult to discern whether the generated answers are  attributed to the GraphRAG method or the LLM.
Human experts can provide highly reliable evaluations for answer quality~\cite{tseng2024expert} but are very expensive to employ. As such, an automated evaluation framework is favorable due to its good generality and low cost. 

%However, this process is resource-intensive and challenging to scale due to its reliance on domain expertise and the effort required to develop comprehensive answers, thereby limiting its feasibility in certain scenarios. 

% \subsection{LLM as a judge}

% Evaluation has been an important challenge in the field of Artificial Intelligence (AI) and Natural Language Processing (NLP). Traditional approaches, whether based on matching (e.g., BLEU, ROUGE) or embedding (e.g., BERTScore, BARTScore) techniques, often have difficulty in capturing complex and nuanced attributes, such as the usefulness, harmlessness, or logical consistency of the generated content. However, with the rapid development of Large Language Models (LLMs), researchers have proposed the “LLM-as-a-Judge” paradigm, where LLMs are utilized to perform scoring, ranking, or selection in various tasks and applications. The core of this paradigm is to address the limitations of traditional approaches by providing more flexible and nuanced evaluation strategies through the powerful language understanding and generation capabilities of LLMs.

% Zhou et al. ~\cite{zhou2024llm} carry out an in-depth assessment to determine if LLMs can act as dependable tools for automated paper reviews. Wang et al.~\cite{wang2024automated} developed a specialized article scoring and feedback system designed for various genres, including essays, narratives, and question-answering articles. Zheng et al. ~\cite{zheng2023judging}explore using strong LLMs as judges to evaluate large language model (LLM) based chat assistant models. Online AI Feedback (OAIF)~\cite{guo2024direct}, utilizes an LLM as an annotator to choose the preferred response in the training process, thereby providing online feedback and directing language model alignment. Li and Qiu ~\cite{li2023mot}propose a framework called MoT, which enables the LLM to self-improve through Memory-of-Thought without the need for annotated datasets or parameter updates. 

% Despite the current research on LLMs as judges, there is still a lack of an evaluation system that can fairly and equitably measure the capabilities of Graph RAGs. This requires both the dynamic formulation of questions tailored to Graph RAGs and a fair assessment of the generated answers.