\section{Experiments}

In this part, we adopt our evaluation framework to examine 3 representative GraphRAG methods and show that it leads to conclusions that are radically different from those reported previously.

\subsection{Experiment Settings}
\stitle{GraphRAG methods.} We evaluate 3 representative GraphRAG methods, i.e., MGRAG~\cite{edge2024local}, LightRAG~\cite{guo2024lightrag}, and FGRAG~\cite{fastgraphrag2024}, which are popular recently. We include NaiveRAG as a baseline, which directly retrieves text chunks according to embedding similarity w.r.t. the question and does not utilize knowledge graph. Details of the GraphRAG methods are provided in Appendix \ref{app:graph rags}. We use their publicly accessible codebases to conduct answer generation.

% While our study follows a complete text-to-graph pipeline, some approaches like Graph CoT do not. This design difference makes it hard to integrate these methods into our testing framework, which relies on a full pipeline for performance evaluation. Thus, despite their potential strengths, these methods were not included in our evaluation due to framework incompatibility.
% \zqm{With such a strong tone or with a more humble one?}

\stitle{Datasets.}
We use the Ultra Domain Benchmark (UDB) datasets ~\cite{qian2024memorag} as the knowledge base. In particular, UDB is a diverse and representative collection compiled from 428 university textbooks and spanning 18 subjects. Each sub-corpus contains between 600,000 and 5 million tokens. We use the following 3 sub-corpus.

\squishlist
    \item {\textit{Mix:}}
    A compact and diverse sub-corpus of UDB. It is the smallest among our three datasets with 5.74 MB.
    \item {\textit{Agriculture:}} It focuses on agricultural contents and covers topics like beekeeping and urban farming. Its size is 70 MB.
    % Agriculture dataset focuses on textual content in the field of agriculture, and its data sources include several agriculture-related university textbooks. These textbooks cover various aspects of agricultural production, such as beekeeping, small farm operations, grassland productivity, and innovative practices in urban agriculture. Among the three datasets, this dataset ranks second in terms of size, with a total capacity of 70 MB.

    \item {\textit{Music:}}
    It encompasses a variety of subjects related to music (such as musical styles, biographies, etc). It is the largest among the three datasets, totaling 143 MB.
\squishend 

 These datasets are also used in the experiments of LightRAG~\cite{guo2024lightrag}. Additionally, we utilize the \textit{GPT-4o-mini} model as the LLM to generate answers as well as serve as the evaluation judge. We generate 50 distinct questions for each of the 3 levels, i.e., node, edge, and subgraph, using our graph-text-grounded question generation method. As such, the total number of questions is 150. 
 

\begin{figure}[t!]  % 使用 figure 环境，可以添加标题和编号
    \centering  % 图片居中
        \includegraphics[width=0.8\columnwidth]{pictures/sanity-check.pdf}  % 插入图片，设置宽度为文本宽度的 50%
    \vspace{-0.8em}
    \caption{Sanity check. We compare the answers generated by the same GraphRAG method (i.e., LightRAG). The current evaluation (left plot) leads to wrong conclusion, i.e., the method outperforms itself, while our evaluation (right plot) correctly determines that the method matches itself.}  % 添加图片标题
    \label{fig:sanity-check}  % 添加标签，用于引用
\end{figure}

% 插入图片
\begin{figure*}[t!]  % 使用 figure 环境，可以添加标题和编号
    \centering  % 图片居中
    \includegraphics[width=0.95\textwidth]{pictures/overall.pdf}  % 插入图片，设置宽度为文本宽度的 50%
    \vspace{-0.8em}
    \caption{One-to-one comparison between the 3 GraphRAG methods and NaiveRAG, each row corresponds to a dataset.}  % 添加图片标题
    \label{fig:overall}  % 添加标签，用于引用
\end{figure*}

\stitle{Sanity check.} Before commencing the experiments, we check whether our evaluation framework removes all the biases. In Figure~\ref{fig:sanity-check}, we create two instances of LightRAG (denoted as $A$ and $B$) and compare them as different GraphRAG methods. Intuitively, a reasonable evaluation should declare that the two match each other in answer quality as they are essentially the same GraphRAG method. However, the current evaluation shows that $A$ has a win rate of 90\%, while $B$ has a win rate of only 10\%, translating into an advantage of 80\% and suggesting significant performance gains. This is because the answers of $A$ are placed in the front (i.e., position bias) and only a single trial is conducted (i.e., trail bias). In contrast, our evaluation correctly determines that the two match each other. In Appendix \ref{app:case study}, we provide a case study to show how our evaluation framework assesses answer quality.



\subsection{Results and Analysis}

\stitle{Overall performance.}
We perform a one-by-one comparison for the 3 GraphRAG methods and NaiveRAG and report the results in Figure~\ref{fig:overall}. We can make the following observations.

First, the win rates are generally smaller than reported by previous work, and the tie rates are notable, making the performance gaps between the methods small. For instance, LightRAG~\cite{guo2024lightrag} reports that it achieves win rates of 66.70\% (vs. NaiveRAG) and 56.38\% (vs. MGRAG) on the Agriculture dataset, while in Figure~\ref{fig:overall}, the win rates of LightRAG become 39.06\% (vs. NaiveRAG) and 32.33\% (vs. MGRAG) on the same dataset. Moreover, the tie rates are over 20\% in most of the cases. These results are because our evaluation framework resolves the biases and allows ties. Moreover, our evaluation also changes the relative performance of the methods. For example, LightRAG is reported to outperform NaiveRAG~\cite{guo2024lightrag} but our results show that NaiveRAG performs better than LightRAG.   


Second, FGRAG performs the best among the methods, followed by MGRAG, then NaiveRAG, and finally LightRAG. This is because FGRAG assigns more concise and informative contexts to the entities and relations in the knowledge graphs and adopts the PageRank scores to rank the retrieved entities and relations. Microsoft also shows that FGRAG can accurately retrieve information relevant to the question~\cite{fastgraphrag2024}. The results suggest that a high-quality knowledge graph and proper re-ranking for the retrieved contents are beneficial for performance.

Third, the performance advantages of the GraphRAG methods over NaiveRAG generally improve with dataset size. This is because GraphRAG methods use knowledge graphs for context retrieval while NaiveRAG directly retrieves text chunks. The knowledge graphs help to accurately retrieve related information, and such ability becomes more important for large datasets. 

% %\textit{LightRAG's performance improves with larger datasets when compared with NaiveRAG}, aligning with GraphRAG's design.
% LightRAG is designed to build a graph-based knowledge base, while NaiveRAG focuses on retrieving text directly. With larger datasets (i.e., Agriculture and Music), NaiveRAG's effectiveness may decrease without adjusting the text length, whereas LightRAG should improve due to its graph extraction method. However, LightRAG's performance on larger datasets does not significantly exceed NaiveRAG's, possibly because it retrieves excessive, irrelevant information, diluting the impact of valid answers.

% Upon theoretical examination, it is anticipated that LightRAG constructs a graph-based representation of the knowledge base, whereas NaiveRAG focuses on direct text retrieval. Consequently, as the dataset expands, the efficacy of NaiveRAG is expected to diminish in the absence of adjustments to the total length of the retrieved text. Conversely, LightRAG, which extracts information directly from the graph structure, should theoretically benefit from larger datasets, enhancing its capacity to facilitate question-answering.
% However, the observed performance of LightRAG on larger datasets, as demonstrated in the presented dataset, shows improvement yet fails to markedly surpass that of NaiveRAG. This discrepancy may be attributed to the information retrieved by LightRAG being overly verbose, encompassing substantial amounts of data that are not pertinent to the query at hand. Even when the retrieved information contains valid answers, the utility of such data is often obscured by the overwhelming presence of extraneous details, thereby diminishing the overall effectiveness of the relevant information.

% 插入图片

\begin{figure}[t!]  % 使用 figure 环境，可以添加标题和编号
    \centering  % 图片居中
        \includegraphics[width=0.7\columnwidth]{pictures/mix-agriculture-music-average-heatmap.pdf}  % 插入图片，设置宽度为文本宽度的 50%
    \caption{Relative win rates (defined as row minus column) between the 3 GraphRAG methods and NaiveRAG. A positive value means that the Row method outperforms the Column method. The results are averaged over 3 the datasets.}  % 添加图片标题
    \label{fig:heatmap}  % 添加标签，用于引用
\end{figure}

\begin{figure*}[t!]  % 使用 figure 环境，可以添加标题和编号
    \centering  % 图片居中
    \includegraphics[width=0.9\textwidth]{pictures/music-detail.pdf}  % 插入图片，设置宽度为文本宽度的 50%
    \vspace{-1.2em}
    \caption{Detailed win rate comparison for the 4 aspects of answer quality, the dataset is Music.}  % 添加图片标题
    \label{fig:detail}  % 添加标签，用于引用
\end{figure*}

\stitle{Relative win rate.}
To better visualize the performance differences between the methods, we introduce \textit{relative win rate}, which is defined as follows:
\begin{equation*}
\text{Relative Win Rate} = \frac{A_{\text{win}} - B_{\text{win}}}{A_{\text{win}} + B_{\text{win}} + \text{Tie}},
\end{equation*}
where $A_{\text{win}}$ is the number of questions method $A$ wins, and similarly for $B_{\text{win}}$ and $\text{Tie}$.
Intuitively, the relative win rate measures the advantage of one method (i.e., $A$) over another (i.e., $B$). 

Figure~\ref{fig:heatmap} plots the relative win rate between the methods. We observe again that the performance gaps between the methods are moderate. Specifically, with the exception of FGRAG and MGRAG, which improve LightRAG by 21\% and 11\%, respectively, the relative win rates between the other methods are all below 8\%. We provide the relative win rate for each individual dataset in Appendix \ref{app:heatmaps}, and the observations are similar. 



%Consequently, we contend that, despite the burgeoning interest in GraphRAG research, the advancements achieved by the proposed models are not as substantial as often portrayed in the literature. 

%It is noteworthy that FGRAG consistently outperforms all other GraphRAG models in pairwise comparisons, albeit the margin of improvement is not particularly pronounced. 











\stitle{Detailed performance.}
In Figure \ref{fig:detail}, we look into how the methods perform in the detailed aspects of answer quality (i.e., comprehensiveness, relevance, empowerment, and directness). We only report the results on the Music dataset due to the page limit, and the results on the other datasets can be found in Appendix \ref{app:detailed}. We make the following observations.

First, the tie rates are much higher than the overall answer quality results in Figure~\ref{fig:overall}. In particular, the tie rates are above 50\% for many cases and seldom below 25\%. This suggests that the answers generated by different methods are likely to have similar quality in individual evaluation aspects, and as a consequence, even if our evaluation decides that a method wins another for a question, the actual quality gap between their answers can be small.

Second, the performance of the methods for the detailed aspects reflects their designs. For instance, when comparing FGRAG and MGRAG, FGRAG excels in comprehensiveness, while MGRAG leads in directness. This is because FGRAG retrieves broader knowledge, which enhances comprehensiveness, while MGRAG directly leverages the relevant summaries for more targeted answers. Similar observations can also be made for LightRAG and NaiveRAG.

% In addition to the experiment about detailed aspects, we further extended our investigation to evaluate the efficacy of different GraphRAG methods across diverse question types, the results of which are presented in Appendix \ref{app:questiontype}. Our experimental results demonstrate that MGRAG achieves superior performance at the node level, which can be attributed to its capability of generating highly specific and targeted reports for individual nodes. Also, the overall performance of FGRAG follows ranking: subgraph level > edge level > node level. This observation aligns with the design rationale of Graph RAG, which is optimized to address complex questions.

In Appendix \ref{app:questiontype}, we also explore how the methods perform for different question types, i.e., node, edge, and subgraph. The results show that MGRAG excels for node questions as it generates highly high-quality community summaries around nodes, while FGRAG performs the best for subgraph questions due to its comprehensive retrieval and re-ranking. 



% \textit{FGRAG does have some advantages.}Our analysis also indicates that FGRAG outperforms both LightRAG and NaiveRAG across all evaluated aspects, despite tie cases. 

% Our analysis reveals that when comparing FGRAG against LightRAG and NaiveRAG, despite a considerable number of ties, FGRAG demonstrates an advantage across all four evaluated aspects. This finding supports the conclusion that FGRAG generally outperforms the other two models. In the comparison between FGRAG and MGRAG, we find that FGRAG is superior in comprehensiveness while MGRAG is superior in directness. The differential performance can be elucidated by the distinct retrieval strategies employed by FGRAG and MGRAG. FGRAG gathers data on entities and relationships connected to the query, which, while not always directly applicable, contributes to a more comprehensive response by incorporating a wider knowledge base. Conversely, MGRAG obtains a summary of community-related content, concentrating on information that is immediately pertinent to the question, thus yielding more direct answers.  The same phenomenon is also observed for LightRAG and NaiveRAG.



\renewcommand{\arraystretch}{1}
\begin{table}
  \caption{Average token consumption and query time }
  \vspace{-0.5em}
  \label{tab: tokentime}
  \begin{tabular}{ccc}
    \toprule
      Method name & Token consumption & Query time (s)\\
    \midrule
    MGRAG & 326,200 & 25.84\\
    LightRAG & 19,650 & 18.27 \\
    NaiveRAG & 4,213 & 11.55 \\
    \textbf{FGRAG} & \textbf{3,310} & \textbf{8.77}\\
    \bottomrule
  \end{tabular}
\end{table}

\stitle{Execution costs.} We also test the methods for their average number of consumed tokens and query time for answering a question, and Table~\ref{tab: tokentime} reports the results. The results are averaged over 100 questions based on the Mix dataset, and the query time is the duration from question submission to answer generation, which includes API request time. The results show that MGRAG consumes the most tokens and the longest time. This is because it retrieves and sorts the community summaries, which require repeated LLM calls. FGRAG is the most efficient, with minimal token consumption and the shortest query time. This benefits from its concise knowledge format. LightRAG ranks second, while NaiveRAG performs nearly as well as FGRAG.