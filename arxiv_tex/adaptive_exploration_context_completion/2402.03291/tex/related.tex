\section{Related Work}
\label{sec:related}
%\system bridges three areas of related work: data science workflows, authoring tools in interactive programming environments, and graph visualization and exploration. 

% \subsection{Study of Data Science Workflows}
% \label{sec:data_science_limitation}
% Recent studies interviewing data scientists find that interactive visualization interfaces are
% most useful if their outputs can be captured via the programming environment~\cite{futzing2019moseying, chattopadhyay2020s, wongsuphasawat2019goals}. In particular, data scientists often switch between multiple tools, including programming environments (\eg MATLAB, Jupyter, or PyCharm), graphical interfaces (\eg Tableau or PowerBI), and data editors (\eg Excel or Google Sheets)~\cite{rahman2020leam,wongsuphasawat2019goals}.
% Such back and forth switching results in a discontinuous workflow,
% often forcing analysts to execute repetitive \emph{glue} tasks manually
% to bridge the gap between two systems during each switch~\cite{chattopadhyay2020s}. 
% This overhead often discourages data scientists from using analysis 
% tools and restricts them to working
% only in code~\cite{wongsuphasawat2019goals}. 

% Recent work emphasizes
% on employing visualizations as a medium for capturing 
% and exporting
% ``actions and transformations interactively
% performed in the component'',
% thereby eliminating the glue tasks between subsequent steps~\cite{batch2017interactive}.
% Alspaugh et al.~\cite{futzing2019moseying} advocated for systems
% that offer the best of both worlds --- the expressivity of coding
% platforms and the ease of use of visual analysis tools. 
% Rahman et al.~\cite{rahman2022ie} investigated data scientists' iterative workflows, 
% \eg information extraction over unstructured text, 
% in an industry setting and called for systems to capture interaction provenance 
% as a first-class feature of any analytic process to ensure reusability and reproducibility. 
% Evaluation of version control systems 
% for computational notebook~\cite{Kery2018InteractionsFU, kery2019towards36} also 
% highlighted the importance of 
% persisting interaction history. Zhang et al.~\cite{zhang2020data} identified lack of provenance as a contributing factor in obfuscation and loss of knowledge when data science teams share data.

% While investigating the limitations of existing workflows of data scientists, Rahman et al.~\cite{rahman2022ie} also identified other aspects such as on-demand customizations of codes via interactions.
% While the idea is somewhat similar to how widgets in computational notebooks parameterize code to impact a single variable, data scientists asked for even more advanced customizations. For example, tuning the \emph{search} algorithm 
% within a search bar to accommodate arbitrary search objectives of a user, such as semantic search (\eg finding a word and its synonyms in documents) and text-specific regular expression-based search (\eg finding a sequence of parts-of-speech tags). Teddy~\cite{teddy2020chi} also reported similar customization requests from data scientists for ease of exploration of text reviews.  

% While developing \system, we aim to incorporate the takeaways from these studies within our design goals besides observations from additional contextual inquiry studies on graph exploration workflows.

% \subsection{Authoring Tools within Interactive Programming Environments}
% \label{sec:related_ipe}
% % \danc{This subsection talks more about tools supporting data science workflows in interactive programming environments rather than the env themselves. The second paragraph focuses on tools not specifically designed for notebooks. The title can be a bit misleading. something like "existing interactive data exploration and analysis"?}
% Data practitioners often use interactive programming environments
% which enable both interactive exploration of data and iteration on their intended tasks~\cite{kery2018story,rahman2022ie}. Computational notebooks such as Jupyter~\cite{kluyver2016jupyter} and Observable~\cite{observable} are examples
% of such environments commonly used by practitioners. Charting libraries such as Altair~\cite{vanderplas2018altair} and Plotly~\cite{inc2015collaborative} also enable interactive visualization. However, the onus is on the user to select suitable visualizations. 
% Moreover, these visualizations lack customization of the interaction outcomes and do not capture interaction history. 
% B2~\cite{wu2020b2} is a dataframe wrapper with chart recommendation capabilities. In addition, B2 captures provenance by transforming UI interactions into dataframe operations and appending those operations in a notebook cell. The intermediate states can be reproduced by uncommenting and executing a python statement which is a cumbersome experience. However, the provenance lacks a tight coupling with the dataframe, as removing the notebook cell erases the interaction history. Therefore, additional version control mechanisms are required to achieve true persistence~\cite{brachmann2020your}. Moreover, B2 is not interaction aware as it does not characterize the visual interaction that transforms the dataframe.
% \system, on the other hand, extends widgets in computational notebooks to enable persistent and interaction-aware
% provenance and allow for interactive exploration of the interaction history. While B2 is limited to tabular dataframes, the current implementation in the \system framework is for exploring knowledge graphs. However, \system may support other data domains as users can plug in any functions to overwrite the underlying data operation corresponding to an interaction.
% \emph{mage}~\cite{kery2020mage} is another tool similar to B2 that translates interactions on interactive components within widgets to code, \eg dataframe operations, and exhibit similar limitations. Other interactive programming environments are not limited to the notebook paradigm. For example, GUESS~\cite{adar2006guess} and Leam~\cite{rahman2020leam, griggs2021towards} operate on graph and text data, respectively, where interactions with visualizations are captured in a Python-based environment while textual commands enable manipulation of the visual output. Therefore, these tools also exhibit similar limitations as B2 for provenance management. Besides, these are bespoke tools requiring users to learn a new platform. 

% Frameworks such as Panel~\cite{panel},
% Plotly Dash~\cite{plotlydash}, and Symphony~\cite{bauerle2022symphony} 
% use independent components to create visualizations that can be used in both Jupyter notebooks and standalone
% websites. \system also uses similar React-based independent components that can support a wider range of visualization libraries. However, Panel visualizations lack the notion of state and do not capture interaction provenance, which \system supports. Plotly Dash does not easily extend to custom visualizations such as node-link diagrams (required for graph exploration) and coordinated views. While Symphony components enable interactive interfaces for machine learning tasks involving various data types, it is \emph{closed-source}. Moreover, Symphony does not support on-demand customization of interactions on the components and does not track interaction history, both of which are supported as built-in features by \system widgets. Streamlit~\cite{streamlit} is another platform for generating interactive web dashboards using Python script. However, the platform prioritizes web applications rather than exploratory data science and does not focus on objectives such as provenance. Moreover, there is a significant learning curve associated with learning a new platform.

% \subsection{Graph Visualization and Exploration}
% \label{sec:related_graph}
A graph or a network is an abstract data type consisting of a finite set of nodes (entities) and edges (relations)~\cite{liu2018graph}. Depending on the properties of the nodes and edges, graphs can be of various types: homogeneous or heterogeneous and directed or undirected~\cite{liu2018graph, 2019_eurovis_mvnv}. Furthermore, graphs can be static or dynamic. In this paper, we focus on static \emph{Knowledge Graphs} (KGs) --- a directed heterogeneous graph with edges encoding facts among nodes (\eg Person \emph{lives\_in} City)~\cite{kg2019def}. Visualizing KGs requires displaying both graph topology (\ie structure) and attributes associated with the nodes and edges.
We refer the readers to a survey of heterogeneous graph visualization for further details~\cite{2019_eurovis_mvnv}. Besides the node and edge properties, any graph visualization system must consider other aspects such as scale, sparsity, and visualization goal. In \system, we made such rendering decisions by referring to the usage guidelines communicated through the decades-long research conducted by the data visualization and HCI communities on defining graph layouts, encoding, and underlying data operations~\cite{liu2018graph, 2019_eurovis_mvnv, pienta2015scalable}. 
Instead of generating visualizations from scratch, \system leverages existing graph visualization libraries. 
These libraries provide built-in features such as
choice of multiple layout algorithms, scaling to larger datasets, customization and extensibility for add-ons, compatibility with different setups (\eg browsers, touch screens), rendering capabilities in various formats (\eg WebGL, SVG, and CSS-based), and integration with existing graph databases such as Neo4j~\cite{miller2013graph}. We utilize an existing embeddable graph visualization library called Cytoscape.js~\cite{franz2016cytoscape} as \system embeds visualizations in Jupyter Notebook cells. Other popular libraries include
D3.js~\cite{bostock2011d3}, G6~\cite{g6}, Neovis.js~\cite{neovis},  Sigma.js~\cite{sigmajs}, and Vivagraph.js~\cite{vivagraphjs}, among others.

The primary goal of \system is to support various exploration tasks within knowledge graphs as users work on projects related to KG construction and serving. 
To this end, we refer to existing graph task taxonomies to define various interactions representing a user's exploration goal~\cite{pretorius2014tasks, kerracher2015task, ahn2013task, lee2006task}. 
Various bespoke graph exploration tools are available in industry~\cite{graphileon, graphistry, graphpolaris, graphxr, hume, linkurious, neobloom, neodash, yworks}. 
These tools enable graph exploration via direction manipulation or graph querying. These tools do not support blending codes with visualizations, forcing users to use different tools for their analysis workflow.
Graphistry~\cite{graphistry}, igraph~\cite{igraph}, and NetworkX~\cite{networkx} offer python packages and libraries for network analysis in computational notebooks such as Jupyter Notebook. Graphistry additionally supports GPU-accelerated rendering of large graph visualizations. However, the generated visualizations are static objects displayed in notebook cells lacking interactivity and are unsuitable for interactive graph exploration. 
There are academic prototypes for general purpose and targeted graph exploration as outlined in various surveys~\cite{pienta2015scalable,liu2018graph}. 
While these are also bespoke tools lacking integration with interactive programming environments, they inform different layouts for visualizing graphs~\cite{henry2007nodetrix, shneiderman2006network}, interactions~\cite{pienta2017facets, pienta2017vigor, chau2011apolo}, and underlying data operations~\cite{shen2006visual}. 
\system, on the other hand, is embedded within the existing data science ecosystem, \ie in computational notebooks. 
%In doing so, \system must address additional challenges that these prior systems do not handle --- supporting the highly iterative workflows while capturing the state of exploration, the provenance of interactions on visualizations, and on-demand customization of interactions.




