\documentclass{article}

% if you need to pass options to natbib, use, e.g.:
%     \PassOptionsToPackage{numbers, compress}{natbib}
% before loading neurips_2024_calm


% ready for submission
\usepackage[final]{neurips_2024_calm}


% to compile a preprint version, e.g., for submission to arXiv, add add the
% [preprint] option:
%     \usepackage[preprint]{neurips_2024_calm}


% to compile a camera-ready version, add the [final] option, e.g.:
%     \usepackage[final]{neurips_2024_calm}


% to avoid loading the natbib package, add option nonatbib:
%    \usepackage[nonatbib]{neurips_2024_calm}


\usepackage[utf8]{inputenc} % allow utf-8 input
\usepackage[T1]{fontenc}    % use 8-bit T1 fonts
\usepackage{hyperref}       % hyperlinks
\usepackage{url}            % simple URL typesetting
\usepackage{booktabs}       % professional-quality tables
\usepackage{amsfonts}       % blackboard math symbols
\usepackage{nicefrac}       % compact symbols for 1/2, etc.
\usepackage{microtype}      % microtypography
\usepackage{xcolor}         % colors
\usepackage{subfig}
\usepackage{caption}
\usepackage{multirow}
\usepackage{graphicx}
\usepackage{amsmath} 
\usepackage{tikz} % For drawing circles

\newcommand*\circled[1]{\tikz[baseline=(char.base)]{
    \node[shape=circle,draw,inner sep=0pt, minimum size=9pt] (char) {#1};}}

\title{Causal Reasoning in Large Language Models: \\
A Knowledge Graph Approach}


% The \author macro works with any number of authors. There are two commands
% used to separate the names and addresses of multiple authors: \And and \AND.
%
% Using \And between authors leaves it to LaTeX to determine where to break the
% lines. Using \AND forces a line break at that point. So, if LaTeX puts 3 of 4
% authors names on the first line, and the last on the second line, try using
% \AND instead of \And before the third author name.


\author{%
  Yejin Kim \\
  GraphLab \\
  George Washington University\\
  Washington, DC, USA \\
  \texttt{yejinjenny@gwu.edu} \\
  \And
  Eojin Kang \\
  AI Convergence \\
  Hankuk University of Foreign Studies \\
  Seoul, Korea \\
  \texttt{eojinkang@hufs.ac.kr} \\
  \And
  Juae Kim$^{*}$ \\
  AI Convergence \\
  Hankuk University of Foreign Studies \\
  Seoul, Korea \\
  \texttt{juaekim@hufs.ac.kr} \\
  \And
  H. Howie Huang\thanks{These authors are corresponding authors.} \\
  GraphLab \\
  George Washington University \\
  Washington, DC, USA \\
  \texttt{howie@gwu.edu} \\
}

% \author{Yejin Kim$^{1}$, Eojin Kang$^{2}$, Juae Kim$^{2}$\thanks{These authors are corresponding authors.}, H. Howie Huang$^{1*}$ \\
% $^{1}$ GraphLab, George Washington University, Washington, DC, USA\\
% $^{2}$ AI Convergence, Hankuk University of Foreign Studies, Seoul, Korea\\
% \newline
% \texttt{\{yejinjenny, howie\}@gwu.edu}\\
% \texttt{\{eojinkang, juaekim\}@hufs.ac.kr}
% }

\begin{document}


\maketitle


\begin{abstract}
Large language models (LLMs) typically improve performance by either retrieving semantically similar information, or enhancing reasoning abilities through structured prompts like chain-of-thought. While both strategies are considered crucial, it remains unclear which has a greater impact on model performance or whether a combination of both is necessary. This paper answers this question by proposing a knowledge graph (KG)-based random-walk reasoning approach that leverages causal relationships. We conduct experiments on the commonsense question answering task that is based on a KG. The KG inherently provides both relevant information, such as related entity keywords, and a reasoning structure through the connections between nodes. Experimental results show that the proposed KG-based random-walk reasoning method improves the reasoning ability and performance of LLMs. Interestingly, incorporating three seemingly irrelevant sentences into the query using KG-based random-walk reasoning enhances LLM performance, contrary to conventional wisdom. These findings suggest that integrating causal structures into prompts can significantly improve reasoning capabilities, providing new insights into the role of causality in optimizing LLM performance.


% This paper studies the significance of relevant information [WHAT IS THE DEFINITION] as a result information retrieval, exemplified by Retrieval-Augmented Generation (RAG). We introduce a random-walk reasoning method with knowledge graph (KG) to extract the reasoning information.
% We aim to experimentally find more effective methods of providing information for prompts to large language models in commonsense question answering (QA).
% Our experiments demonstrate that proposed random-walk reasoning  improves overall performance over RAG.
% Furthermore, our findings challenge the traditional belief that only relevant information is crucial for enhancing QA performance, revealing that ``seemly irrelevant`` information could still help boost commonsense QA performance.
% Notably, incorporating three less relevant information sentences extracted through KG-based random-walk reasoning has led to significant improvements over the baseline.

%%% ***** Past version needs to be modified ***** %%%%
% Information retrieval has received considerable attention in recent years, as relevant knowledge can be used to assist AI responses. 
% This paper studies the significance of relevant information [WHAT IS THE DEFINITION] as a result information retrieval, exemplified by Retrieval-Augmented Generation (RAG). We introduce a random-walk reasoning method with knowledge graph (KG) to extract the reasoning information.
% We aim to experimentally find more effective methods of providing information for prompts to large language models in commonsense question answering (QA).
% Our experiments demonstrate that proposed random-walk reasoning  improves overall performance over RAG.
% Furthermore, our findings challenge the traditional belief that only relevant information is crucial for enhancing QA performance, revealing that ``seemly irrelevant`` information could still help boost commonsense QA performance.
% Notably, incorporating three less relevant information sentences extracted through KG-based random-walk reasoning has led to significant improvements over the baseline.

% 참고용: Recent advancements at the intersection of causality and large language models (LLMs) underscore the potential to leverage causal reasoning within these models. This paper explores how integrating causal reasoning in LLMs can enhance their ability to solve commonsense question answering (QA) tasks, complementing and boosting the effectiveness of traditional RAG methods.This paper explores the significance of additional information provided by information retrieval, exemplified by Retrieval-Augmented Generation (RAG), and introduces a random-walk reasoning method with knowledge graph (KG) to extract the reasoning information. We an approach that integrates causality-inspired reasoning through graph random-walking with knowledge graphs (KGs) to enhance QA.  our findings challenge the traditional belief that only relevant information is crucial for enhancing QA performance, revealing that even irrelevant information can boost commonsense QA performance.

\end{abstract}

\section{Introduction}
%%% 0921

Large language models (LLMs) have demonstrated significant advancements in natural language processing tasks through two primary approaches: providing auxiliary information via retrieval and enhancing reasoning abilities within prompts. Retrieval-augmented generation (RAG) \citep{lewis2020retrieval} is designed to provide information relevant to the given context or query. This relevant information is identified using embedding similarity searches and then integrated into the LLM’s prompt, thereby enhancing the accuracy, relevance recency of the generated response. Recent studies have shown that RAG can significantly boost performance in tasks such as summarization and question answering (QA) \citep{rh:conf/aclnut/GuYW19, rh_DBLP:conf/emnlp/0001PCKW21, rh_DBLP:conf/acl/Komeili0W22}. However, this approach primarily focuses on retrieving directly related information, raising the question of whether relying solely on such relevant information is sufficient for achieving optimal LLM performance.

Another approach to improving the quality of LLM responses is to enhance their reasoning abilities \citep{raesoning_jain2023mechanistically, raesoning_DBLP:conf/acl/SuzgunSSGTCCLCZ23, DBLP:conf/nips/Wei0SBIXCLZ22, DBLP:conf/nips/KojimaGRMI22}. Reasoning capabilities enable LLMs to go beyond surface-level information and make logical connections between concepts, thereby enhancing their ability to handle more complex queries \citep{raesoning_jain2023mechanistically, raesoning_DBLP:conf/acl/SuzgunSSGTCCLCZ23, DBLP:conf/nips/Wei0SBIXCLZ22, DBLP:conf/nips/KojimaGRMI22}. Structured reasoning techniques, such as knowledge graph (KG)-based methods, offer an effective alternative by allowing LLMs to utilize structured knowledge and causal relationships for deeper reasoning \citep{ling2023knowledge, yao2023beyond, DBLP:series/tanlp/SpeerH13}. This capability makes KGs a promising resource for comprehending concepts, applying logical reasoning, and refining or validating the model’s understanding using existing knowledge \citep{wang2023knowledge, yao2023beyond}.


\begin{figure*}
\centering 
\subfloat[KG example of QA]{\includegraphics[width=.43\linewidth] % A KG example of COMMONSENSE QA
{figures/model1.png}}\hspace{5pt}
\subfloat[KG-Based Reasoning]{\includegraphics[width=.55\linewidth]{figures/exp_overview.png}}
\caption{(a) Illustration of a KG structure and an example of CommonsenseQA \citep{talmor2018commonsenseqa}. At the bottom, the question and its concept are represented as $q$ and $qc$ respectively, while the answer is denoted as $a$. (b) Performance comparison on commonsense QA. The dotted line (baseline) represents the performance when no additional context is provided through the prompt. The three bars represent the number of sentences provided as context in the prompt.} 
\label{fig:enter-label}
\end{figure*}



In this paper, we aim to investigate the relative contributions of semantic information retrieval and causal reasoning to the LLMs. To  this end, we propose a KG-based random-walk reasoning approach that navigates paths of interconnected nodes and edges to uncover causal relationships and extract contextual information, thereby enhancing the reasoning capabilities of LLMs. We use the CommonsenseQA \citep{talmor2018commonsenseqa} dataset, constructed from the KG, ConceptNet \citep{speer2017conceptnet}, as our evaluation benchmark. Figure \ref{fig:enter-label}-(a) shows a KG and an example of CommonsenseQA. This dataset is suitable for our study as it requires both relevant information retrieval and complex reasoning to solve problems based on the structured relationships within the KG. We systematically assess the impact of providing semantically relevant information and causal reasoning by conducting experiments with various settings that control the presence and type of contextual information provided to the LLM.

Specifically, we evaluate the effect of incorporating causal relationships extracted through random-walk reasoning within the ConceptNet graph, measuring how these elements contribute to LLM performance in answering commonsense questions. To comprehensively analyze the contributions of information retrieval and reasoning, we designed the following experimental settings:
\begin{enumerate}
    \item \textbf{Relevant Information Only}: In this setting, the context provided to the LLM consists of information with high embedding similarity to the question, inspired by RAG, without any additional reasoning process.

    \item \textbf{Graph Inference Only}: The context provided offers reasoning ability through a continuous process of graph inference, but the information is entirely unrelated to the question.

    \item \textbf{Keyword Relevant Information + Graph Inference (KGI)}: The information means sentences related to the keywords of the question, also providing reasoning ability through the proposed approach. However, there is no guarantee that the information in the graph is directly related to the content of the question. In other words, reasoning and information are provided together, but it is possible to include irrelevant information to the question.

    \item \textbf{Query Relevant Information + Graph Inference (QGI)}: This setting explores how providing both relevant information related to the question and causal relationship through the KG-based random-walk reasoning can enhance the LLMs.
\end{enumerate}


Figure \ref{fig:enter-label}-(b) provides a summary of the experiments. Compared to the baseline, where no additional context is given, the prompt involving either relevant information or causal reasoning improves performance. Notably, in the "Graph Inference Only" setting, when causal relationships were conveyed through the reasoning of two or three sentences, performance improved even though the content was entirely unrelated to the question. This result indicates that our experimental settings successfully convey reasoning capabilities through causal structures. Furthermore, this finding suggests that, contrary to conventional wisdom, it may be more advantageous to equip LLMs with reasoning abilities, grounded in causality, rather than merely providing semantically related information. A comparison between the "Graph Inference Only" and "KGI" settings shows that information extracted through the KG-based random-walk reasoning method yields better performance than the embedding similarity search-based method in the "Graph Inference Only" setting. This demonstrates that the proposed method, which leverages causal relationships through KG, is more effective for commonsense QA. Lastly, the highest improvement is observed in the "QGI" setting, which combines both relevant information and causal reasoning, aligning with our expectations.

Our contributions are summarized as follows:
\begin{itemize}
    \item We demonstrate the contributions of relevant information and reasoning abilities through experimental comparisons.
    
    \item We propose a novel KG-based random-walk reasoning method to utilize causal relationship.
    
    \item We show that providing reasoning capabilities grounded in causal relationships can lead to performance improvements, even when using seemingly unrelated information.
\end{itemize}



%Information retrieval, which continues to receive attention, is fundamentally aimed at pinpointing and securing data pertinent to a user query, thereby enhancing the performance of AI. Recently, Retrieval-Augmented Generation \cite{lewis2020retrieval} (RAG) has drawn considerable attention from NLP researchers. A RAG includes an embedding-based retrieval mechanism called Dense Passage Retrieval \cite{dpr:conf/emnlp/KarpukhinOMLWEC20} (DPR) and a reader that finds answers based on context. To efficiently use the resource-intensive Large Language Models (LLMs), such as GPT-3 \cite{gpt3:conf/nips/BrownMRSKDNSSAA20}, PaLM \cite{palm_DBLP:journals/jmlr/ChowdheryNDBMRBCSGSSTMRBTSPRDHPBAI23}, and LLama \cite{touvron2023llama}, which rely on their vast parametric memory, RAG can also extract relevant knowledge from an external knowledge base. This approach not only mitigates hallucinations but also improves performance in tasks like summarization and Question Answering (QA) \cite{rh:conf/aclnut/GuYW19, rh_DBLP:conf/emnlp/0001PCKW21, rh_DBLP:conf/acl/Komeili0W22}. 

%In recent years, information retrieval technologies exemplified by Retrieval-Augmented Generation \citep{lewis2020retrieval} (RAG) have consistently drawn attention. The main idea of RAG involves providing the most related, accurate, and up-to-date information to the query to Large Language Models (LLMs), such as GPT-3 \citep{gpt3:conf/nips/BrownMRSKDNSSAA20}, PaLM \citep{palm_DBLP:journals/jmlr/ChowdheryNDBMRBCSGSSTMRBTSPRDHPBAI23}, and LLama \citep{touvron2023llama}. It leverages embedding similarity searches to retrieve relevant information, which is then used as context to generate text by LLMs, thereby enhancing the relevance and accuracy of the output. In previous studies, RAG have improved response accuracy in tasks like summarization and Question Answering (QA) \citep{rh:conf/aclnut/GuYW19, rh_DBLP:conf/emnlp/0001PCKW21, rh_DBLP:conf/acl/Komeili0W22}. 


%Another approach to obtain the higher response accuracy of LLMs is to enhance their reasoning ability \citep{raesoning_jain2023mechanistically, raesoning_DBLP:conf/acl/SuzgunSSGTCCLCZ23, DBLP:conf/nips/Wei0SBIXCLZ22, DBLP:conf/nips/KojimaGRMI22}. Knowledge Graph (KG) is one of the representative strategies used to improve reasoning ability \citep{ling2023knowledge, yao2023beyond, DBLP:series/tanlp/SpeerH13}. The utilization of KG for inference is a research area in the spotlight in various domains including intelligent QA systems. A KG organizes information in a graph structure, with nodes interconnected by edges, which have associated attributes. By leveraging the information gathered from the connections of nodes and edges within a KG, it becomes feasible to enhance reasoning abilities. This includes the capacity to comprehend concepts, apply logical reasoning, and refine or validate the architecture using existing knowledge \citep{wang2023knowledge, yao2023beyond}. 



% \begin{figure}
%     \centering
%     \includegraphics[width=0.75\textwidth]{figures/exp_overview(2).png}
%     \caption{Comparison of the impact of relative information and reasoning ability. The dotted line (baseline) represents the performance when no additional context is provided through the prompt.}
%     \label{fig:enter-label}
% \end{figure}



%In this paper, we explore the relative contributions of relative information and reasoning ability through experimental comparison. To assess the explicit contributions, we adopted commonsense QA as target task and CommonsenseQA \citep{talmor2018commonsenseqa} as a target data which is a benchmark dataset constructed from the ConceptNet knowledge graph \citep{DBLP:series/tanlp/SpeerH13}. This is because Commonsense QA was constructed to evaluate the ability to solve problems based on the ConceptNet graph, allowing us to provide both information and reasoning capabilities through the graph. In this paper, we assess the impact of relative information and reasoning ability on QA task by conducting experiments that control and provide the causal relationship between the relative information and the graph connections derived from a random-walk reasoning approach contained in the ConceptNet graph.


%To examine the explicit contributions, we designed the following experimental settings:
%\begin{enumerate}
%    \item \textbf{Only Information}: In this setting, the context provided to the LLM consists of information that has embeddings similar to the question inspired by RAG. However, no reasoning process is provided between these pieces of information.

%    \item \textbf{Only Reasoning}: The context provided offers reasoning ability through a continuous process of graph inference, but the information is entirely unrelated to the question.

%    \item \textbf{Information + Reasoning (KBI; Keyword Based Information)}: The information is related to the keywords (question concept) of the question while also providing reasoning ability through the random-walk approach. However, there is no guarantee that the information in the graph is directly related to the content of the question. In other words, reasoning and information are provided together, but it is possible to include irrelevant information to the question.

%    \item \textbf{Information + Reasoning (QBI; Question Based Information)}: This setting explores how much providing both relative information directly related to the question and reasoning ability can enhance the LLM's response capability. The random-walk approach is also applied for this setting.
    
%\end{enumerate}

%Figure \ref{fig:enter-label} (b) provides a summary of the experiments conducted in this paper. Three bars represent the number of sentences provided as context in the prompt. Compared to the baseline, where no additional information is provided, the additional context involving relative information or reasoning improves performance. Interestingly, in the "Only Reasoning" setting, when reasoning ability was provided through two and three sentences, there was an improvement in performance despite the content being entirely unrelated to the question. This suggests that our experimental settings effectively convey reasoning ability. Furthermore, this result suggests that, contrary to conventional wisdom, it may be more beneficial to equip LLMs with reasoning abilities rather than merely providing related information. A comparison between the "Only Information" and "KBI" settings indicates that information extracted through the KG-based random-walk reasoning method yields better performance than the embedding similarity search-based method representing "Only Information," suggesting the proposed method is more advantageous for commonsense QA. Lastly, the highest improvement is observed in QBI, which combines information and reasoning ability, aligning with our expectations. Our contributions are summarized in three points.


%\begin{itemize}
%\item We show the contributions of relative information and reasoning ability with experimental comparison.
%\item We propose a KG-based random-walk reasoning method to solve the commonsense QA problem. 
%\item We present that providing reasoning capabilities through our method can lead to improvements, even when using seemingly unrelated information.

    %\item We show which method, RAG or graph-based, is more effective for commonsense QA when we assume that the same information candidates are provided.
     %\item We present how prompts affect the commonsense QA system problems without relevant information.
%\end{itemize}


% \section{Background}
% \textbf{Commonsense QA} is a well-known fact that relevant information and reasoning capabilities are crucial for solving commonsense QA problems \cite{DBLP:conf/ecai/KrauseS23, DBLP:conf/ijcai/BaoLZLYLZ23, DBLP:conf/nips/Wei0SBIXCLZ22}. The most generally used commonsense QA dataset is COMMONSENSE QA \cite{talmor2018commonsenseqa} which includes multiple-choice questions with five options. This data is designed to assess the commonsense knowledge and reasoning abilities of AI models with steps as follows. Initially, sub-graphs are extracted from \textbf{ConceptNet} \cite{DBLP:series/tanlp/SpeerH13} with entities called concepts. Then, crowd workers make and verify generated questions and answers. In this study, we have chosen the COMMONSENSE QA dataset as it is a suitable choice for investigating the impact of relevant information and reasoning ability, given that the dataset explicitly contains information based on ConceptNet.
% --------End background--------

% Starting with question class/category/tags to find the most similar node then use the detailed question to find the most similar verbalized triple from the node (in or outbound). Not to miss information as we start with a similar node with the detailed question. Roughly pick the node and top-down process to funnel to answers.
\section{Method}
Our main goal is to investigate KG-based reasoning for the commonsense QA task without further training on LLMs. We first briefly introduce problem formulation. Subsequently, we examine the disparities in prompting procedures between the conventional retrieve-based method and our KG-based reasoning approach.
% retrieval로 중요 정보를 뽑는 것
% Our main goal is to explore KG-based reasoning for the commonsense QA task without further training on the LLMs. We first briefly introduce problem formulation and then discuss the difference of prompting to generative AI between the conventional and proposed methods. 

% A detailed process of each method can be divided into two components: 1) retrieval-based prompting and 2) graph-based reasoning prompting.
%Our main contribution is to explore a graph-based reasoning process in a commonsense QA task. 

% \begin{figure*}
% \centering %41:59
% \subfloat[KG example of COMMONSENSE QA]{\includegraphics[width=.34\linewidth] % A KG example of COMMONSENSE QA
% {figures/model1.png}}\hspace{5pt}
% \subfloat[KG-Based Reasoning]{\includegraphics[width=.64\linewidth]{figures/model2.png}}
% \caption{Overview of the Proposed Method. (a) Illustration of a KG structure and an example of QA. At the bottom, the question and its concept are represented as $q$ and $qc$ respectively, while the answer is denoted as $a$. (b) Detailed process of prompting through the KG-based reasoning.} 
% \label{fig:model}
% \end{figure*}


\subsection{Problem Formulation} \label{3_1} 
 In a multiple-choice format such as Figure \ref{fig:enter-label}-(a), the goal is to predict an answer $a \in Aq$ given a pair of a question and a question concept $(q, qc)$, where $q \in Q$. Any keyword capable of categorizing a question could potentially serve as $qc$. In our research, we use the term question concept, $qc$, to encompass all such keywords. The set of choices denoted as $Aq$, varies with each question, and both questions and answers are represented as variable-length text sequences. KG denoted as $G=(V,E)$, is configured as a heterogeneous graph in general. Within this graph, $V$ is the set of entity nodes, and $E \subseteq V \times R \times V$ denotes the set of edges connecting nodes in $V$, where $R$ constitutes a set of relation types. The node $v_{qc} \in V$ denotes the node in the KG that is most semantically similar to $qc$ which means that $v_{qc}$ captures the essence of $qc$ within the graph structure. Using $v_{qc}$ as a starting point, we can explore $n$-hop neighbors which can gather additional information related to the question, $q$. This process is based on the flow of the graph and allows for effective graph reasoning; it adheres to the directionality of edges within the graph structure. Our graph reasoning process aligns with the inherent structure and semantics of the KG.
  
%  For instance, by utilizing $v_{alcohol}$ as an anchor node, we can identify inbound edges leading to the anchor node or outbound edges originating from the anchor node as illustrated in Figure \ref{fig:model} (a).
 
 
%  This process of gathering information based on the flow of the graph allows us to conduct effective graph reasoning. Notably, our graph reasoning approach adheres to the directionality of edges within the graph structure. This means that we respect the flow of information encoded in the graph, ensuring that our reasoning process aligns with the inherent structure and semantics of the knowledge graph.

% This exploration is not limited to direct connections but extends to nodes reachable within a certain number of hops.
 
 % We define $G_{qc_{h}}$ and $G_{qc_{t}}$ as sub-graphs of $G$ that are extracted based on the question concept, $qc$. The node $v_{qc} \in V$ represents the most semantically similar node to $qc$. Using $v_{qc}$ as an anchor node, both $G_{qc_{h}}$ and $G_{qc_{t}}$ are constructed. Specifically, $G_{qc_{h}}$ is formed by including all outbound edges of all edge types from the node $v_{qc}$, while $G_{qc_{t}}$ is induced by incorporating all inbound edges of the various edge types associated with $v_{qc}$.
% We define $G_{qc_{h}}$ and $G_{qc_{t}}$ are sub-graphs of $G$ which extracted based on $qc$. $e_{qc}$ is the most similar node with $qc$ semantically. Based on $e_{qc}$, both $G_{qc_{h}}$ and $G_{qc_{t}}$ are generated. $G_{qc_{h}}$ is induced on the outbound edges of all the edge types of the given node, $e_{qc}$ and $G_{qc_{h}}$ is induced on the inbound edges of all the edge types.
%$G_{qc_{h}}$ is a sub-graph of $G$ extracted based on matched head node with $qc$, $G_{qc_{t}}$ is from matched tail node.

\begin{figure}
    \centering
    \includegraphics[width=1.0\textwidth]{figures/model2.png}
    \caption{Detailed process of prompting through the KG-based reasoning.}
    \label{fig:model}
\end{figure}


\subsection{Retrieval-Based Prompting}  \label{3_2}
A conventional retrieval-based prompting such as RAG follows:
{\small
\begin{equation}\label{eq1}
\prod_{i}^{N}\sum_{d \in top\text{-}k(p(\cdot|x))}P_\eta(d|x)P_\theta(y_i|x,d,y_{1:i-1})
\end{equation}
}
where $x$ denotes an input query sequence used to retrieve text documents $d$, incorporating them as additional context during the generation of the target sequence $y$. In this context, $x$ is a question, $q$ while $y$ corresponds to an answer denoted by $a$ in our commonsense QA task. The retrieval process $P_\eta(d|x) \propto exp(Enc(d)^T, Enc(x))$, where $Enc$ functions as an encoder representing both documents and queries. Specifically, $Enc(d)$ stands for the embedding of a document, while $Enc(x)$ signifies a query embedding generated by the encoder. Determining the top-$k$ documents, denoted as $top\text{-}k(p(\cdot|x))$, where the list of $k$ documents $d$ with the highest prior probability $P_\eta(d|x)$ calculated by a similarity function such as cosine similarity. A generator $P_\theta(y_i|x, d, y_{1:i-1})$ generates the current token by considering the context of the preceding $i-1$ tokens $y_{1:i-1}$, the initial input $x$, and a retrieved document $d$.

\subsection{KG-Based Random-Walk Reasoning}
Given a KG with a pair of question, $q$ and question concept, $qc$ as shown in Figure \ref{fig:enter-label}-(a), we seek the node most closely associated with the question concept. Following this initial step of narrowing down the information based on the question concept, we then leverage graph reasoning to extract additional information necessary for formulating a comprehensive answer to the question.

In Figure \ref{fig:model} Step 1, we search for the most similar node aligned with the question concept ($qc$), the term "\textit{liquor}". Employing a pre-trained text encoder denoted as $E$, we encode both the anchor text and the entirety of text associated with nodes within the KG. This process yields embeddings for both the anchor text and all node texts. Subsequently, we calculate the cosine similarity between the embedding of the anchor text and the embeddings of all node texts, establishing similarity scores for each pair.
%%% IN PROGRESS
In this case, $v_{qc}$ is the node \circled{3} in Figure \ref{fig:enter-label}-(a), identified as \textit{alcohol}. Subsequently, we either traverse one hop outbound from the node \textit{alcohol} or find a node that is one hop inbound to \textit{alcohol}. In our case, there are eight one-hop nodes connected to \textit{alcohol}, numbered \circled{1}, \circled{2}, \circled{4}, \circled{5}, \circled{6}, \circled{7}, \circled{9} and \circled{\footnotesize{10}}. Among these, nodes \circled{2} and \circled{9} are inbound, while the others are outbound neighbors. In Figure \ref{fig:model} Step 2, we verbalize triples—structured statements consisting of a subject, predicate, and object—based on their direction relative to node \circled{3}. For example, in the triple \textit{“alcohol, causes, sleep”} node \circled{3}, \textit{alcohol} is the subject, the relationship, \textit{causes} is the predicate, and node \circled{6}, \textit{sleep} is the object. All connections from or to node \circled{3} are similarly transformed into triples, where the relationships between nodes are clearly expressed in natural language. Using the same encoder $E$, we encode both the verbalized triples and the question $q$. We then identify the verbalized triple whose representation is most similar to that of $q$. Through Step 2, we obtain a single verbalized triple structured as a sentence. As the next step, rather than using a similarity function to select the next object node from the current object node \circled{6} (\textit{sleep}), we randomly select $n$. This approach is intended to provide reasoning context following the presentation of the most relevant information just once. We combine these sentences into a question, $q$, and prompt the model to generate an answer based on equation \ref{eq1}.

% Through Step 2, we obtain one verbalized triple formed as a sentence. Instead of using the similarity function to select the next object node from the current object node \circled{6}, \textit{sleep}, we randomly select $n$. The purpose is to give the context of reasoning only once after we give the most similar information. we generate the sub-graph, $G_{qc_h}$ ($G_{castle_{h}}$) centered around the \textit{castle}, encompassing the first, fourth, fifth, ninth, and tenth nodes. Simultaneously, another sub-graph, $G_{qc_t}$ ($G_{castle_{t}}$) based on the \textit{castle} is extracted, including the second, sixth, and seventh nodes. Then, we aggregate all triples, each consisting of a head, relation, and tail, from each sub-graph. For instance, in the first triple of Step 1, shown in Figure \ref{fig:model}, the node \textit{castle} functions as the head, the relationship is expressed by \textit{location of}, and the tail node is represented by \textit{europe}. Denoting a triple as \textit{(head, relation, tail)}, we represent this instance as \textit{(castle, location of, europe)}. In Step 2, we verbalize these triples. The process employed for each triple set $G_{qc_{h}}$ and $G_{qc_{t}}$ are identical, differing only in the direction of edges. Specifically, the triple set of $G_{qc_{h}}$ comprises outbound edges, while $G_{qc_{t}}$ involves inbound edges. For simplicity, we focus on elucidating the $G_{qc_{h}}$ process. Utilizing an encoder, we encode both a question, $q$, and the verbalized triples, subsequently seeking the most similar triple. For example, we may obtain \textit{(castle, location of, europe)} as the converted sentence, \textit{"castle is a typical location for europe, or castle is the inherent location of europe."} is the most similar to $q$. As Step 3, from the retrieved triple, we trace the tail node, $europe$ of the triple, and generate $G_{europe_{h}}$. In Step 4, we verbalize the triples that we generated in Step 3. Lastly, we randomly select $n$ from the set of triples from $G_{europe_{h}}$. Repeating Steps 3 and 4 enables the generation of additional relayed sentences as desired, given the condition that the edge exists. To conclude, a single iteration of Steps 1 through 4 produces a total of two verbalized triples, one from each of Step 2 and Step 4. We combine these sentences into a question, $q$, and prompt the model to generate an answer based on equation \ref{eq1}.

% 1. **Generating Sub-Graphs**:
%    - Given a Knowledge Graph (KG), we generate sub-graphs \( G_{qc_{h}} \) or \( G_{qc_{t}} \) based on a question concept \( qc \).
%    - Let \( G_{qc_{h}} \) represent the sub-graph centered around the head of the question concept, and \( G_{qc_{t}} \) represent the sub-graph centered around the tail of the question concept.
%    - This can be represented as:
%      \[ G_{qc_{h}} = \{v_i, r_i, v_j \} \quad \text{where} \quad v_i \in V_{qc_{h}}, r_i \in R_{qc_{h}}, v_j \in V_{qc_{h}} \]
%      \[ G_{qc_{t}} = \{v_i, r_i, v_j \} \quad \text{where} \quad v_i \in V_{qc_{t}}, r_i \in R_{qc_{t}}, v_j \in V_{qc_{t}} \]

% 2. **Triple Extraction**:
%    - Extract triples (head, relation, tail) from each sub-graph.
%    - For instance, in Step 1, \( (castle, location\ of, europe) \) is a triple extracted from \( G_{qc_{h}} \).
   
% 3. **Verbalization**:
%    - Verbalize the triples extracted from each sub-graph.
%    - For example, \( (castle, location\ of, europe) \) may be verbalized as \textit{"castle is a typical location for europe, or castle is the inherent location of europe."}.
   
% 4. **Encoder Similarity**:
%    - Utilize an encoder to encode both the question \( q \) and the verbalized triples.
%    - Find the most similar triple to the question from the encoded triples.
   
% 5. **Generating Sub-Graph for Tail Node**:
%    - Trace the tail node of the retrieved triple and generate a sub-graph \( G_{europe_{h}} \).
   
% 6. **Verbalization of Generated Triples**:
%    - Verbalize the triples generated in the previous step.
   
% 7. **Iteration**:
%    - Repeat Steps 5 and 6 to generate additional relayed sentences as desired, given the condition that the edge exists.

% 8. **Combining Sentences**:
%    - A single iteration of Steps 1 through 4 produces a total of two verbalized triples.
%    - Combine these sentences into a question \( q \) and prompt the model to generate an answer.

% Equation \ref{eq1} is referred to for generating the answer based on the process described. The equation itself is not provided, but it's implied that it's a part of the system for generating answers based on the extracted information and the question.

%We follow the retrieval process $P_\eta(d|x)$ outlined in Section \ref{3_2}, wherein a question $q$ is encoded as an input query, $x$, and verbalized triples are encoded as pertinent text documents, $d$.
\section{Experiments}

\subsection{Experimental Setup}
We validate the efficiency of our proposed method in zero-shot setting on Llama 2-Chat \citep{touvron2023llama}, without further training or tuning on the model, to focus on the effect of the KG-based reasoning process. Thus, we assess results across diverse prompt settings, considering different approaches to retrieving and reasoning information.
\paragraph{\textbf{Dataset and Encoder}} We use the CommonsenseQA dataset \citep{talmor2018commonsenseqa} for evaluation emphasizing the need for diverse commonsense knowledge to choose the correct answers. We employed 1,221 data from the validation dataset due to the unavailability of publicly disclosed answers.
To ensure a fair and comprehensive comparison, we choose to employ the ConceptNet KG as the search and reasoning source for both RAG and our proposed method in this experiment. For retrieval purposes, we verbalize the triplets in sentence structures, resulting in 3,423,004 sentences using descriptions of ConceptNet\footnote{\url{https://github.com/commonsense/conceptnet5/wiki/Relations}}. Additionally, we explore a significant web dataset, using Wikipedia as the search source for the "Relevant Information Only" experiment in this study. The Wikipedia embeddings index provided by txtai\footnote{\url{https://huggingface.co/NeuML/txtai-wikipedia\#wikipedia-txtai-embeddings-index}} is employed. The \textit{e5-base} model \citep{wang2022text} is utilized to represent all text documents such as Wikipedia and verbalized ConceptNet triples, as well as questions and question concepts.
%In this section, we validate the efficiency of our proposed method. It is important to note that our focus is on the effect of the KG-based reasoning process without further training or tuning on the LLMs. Thus, we assess results across diverse prompt settings, considering different approaches to retrieving and reasoning information.
%\paragraph{\textbf{Dataset}} We use the COMMONSENSE QA 1.0 version \cite{talmor2018commonsenseqa} dataset, which covers multiple-choice QA with a five-choice format, emphasizing the need for diverse commonsense knowledge to choose the correct answers. %The test dataset comprises 1.2K questions, each with one correct answer and four distractors. %add about conceptnet, rag, prompting, verbalize template
%This dataset, built by aggregating triplets from ConceptNet, features a single source concept (question concept) and corresponding target concepts (answer concept) with the same semantic relation. Although the complete dataset consists of 12,247 questions, our experiment uniquely focused on a zero-shot setting and utilized no training data. Moreover, due to the unavailability of publicly disclosed answers for the evaluation dataset, we employed 1,221 questions from the validation dataset.

%We structured our multiple-choice prompts by combining options with their respective answers. For instance, we used a format like "A. motion, B. stretch, C. exercise, D. movement, E. muscles." While the Llama 2 generally adheres to the expected output format, such as" Answer: C. exercise," in some cases, it deviates from this norm. Establishing clear boundaries for a correct answer is necessary for precise model evaluation. We identified correct answers, including single-letter options and the answer itself.

%To ensure a fair and comprehensive comparison, we choose to employ the ConceptNet KG as the search and reasoning source for both RAG and our proposed method in our experiment. For retrieval purposes, we express the triplets in sentence structures, resulting in 3,423,004 sentences. Additionally, we explore a significant web dataset, using Wikipedia as the search source for the RAG experiment in this study. The Wikipedia embeddings index provided by txtai\footnote{\url{https://huggingface.co/NeuML/txtai-wikipedia\#wikipedia-txtai-embeddings-index}} is employed. The source documents, comprising Wikipedia and verbalized ConceptNet triples \footnote{\url{https://github.com/commonsense/conceptnet5/wiki/Relations}}, as well as the queries, whether questions or question concepts are all represented by the \textit{e5-base} model\cite{wang2022text}.

%\paragraph{\textbf{Experiment Settings on RAG Experiments}}
%To retrieve text documents, we devised three types of input queries: 1) question, 2) question + question concept, and 3) question concept. The second type, question + question concept, is designed to focus more intensively on the source concept of the triplet. In contrast, the third type, the question concept, only considers the source concept. The search on transformed sentences from ConceptNet operates at the sentence level, contrasting with the document-level operation of the Wikipedia search. Similar to the approach with the Wikipedia dataset, we employed the \textit{e5-base} model, ensuring methodological consistency.

\paragraph{\textbf{Evaluation Metrics}} %accuracy, output format 
We evaluate performance using accuracy as the criterion. In experiments where the output format is incorrect in the prompt, multiple scenarios arise.
%, usually involving variations such as replacing periods with commas.
Consequently, all experiments are conducted without an explicit output format. Regardless, when analyzing the results, if the correct answer is "B. exercise," valid responses may take the form of "B", "B.", "B,", "exercise" or "X. exercise". Incorrect responses can include options like "A. exercise", where the alphabet is incorrect but the answer is accurate, "B. exercise, C. muscle", when multiple selections are made, or instances where irrelevant statements are presented.
%like "Is exercise good for health?" 

\subsection{Results}
Table \ref{table:main} presents the result of RAG and KG-based random-walk reasoning methods. Our baseline is set as a plain question without additional information. For RAG, we retrieve the top-$k$, where $k = \{1, 2, 3\}$ sentences from verbalized triples within the ConceptNet KG. In the proposed method, we extract sequential triples connecting to an anchor node determined by its similarity score with the question concept. In contrast, "Relevant Information Only" extracts triples exclusively based on text similarity scores with the combined question concept and the question itself. The findings underscore the effectiveness of KG-based reasoning, leveraging information from connected nodes, particularly in scenarios where the number of information sentences remains constant. In our random-walk approach, we prioritized nodes that were physically close to the starting point. For instance, in the "KGI" case when $k$ = 3 (Table \ref{table:main}), nodes 1 through 5 were selected based on their distance of 1 from the start. This criterion was consistently applied across all experiments. Upon careful performance analysis, it becomes apparent that pertinent information situated at the outset or conclusion of the anchor node proves more advantageous than information located in the middle (Graph, $k$ = 2 in Table \ref{table:main}). Our best performance is attained by incorporating comprehensive context, achieved by combining top-1 information of RAG with data derived from KG-based random-walk reasoning.

\input{tables/main_results}
\input{tables/bottom_results}
% \input{tables/main_bottom_results}


In Table \ref{table:bottom}, we investigate the setting in which the given information is less relevant to the question and its concept. It is crucial observation that even when opting for a less related anchor node and executing a random-walk to obtain $k$ sentences, there is an observed enhancement in performance (Graph, $k$ = 2 and 3 in Table \ref{table:bottom}). This suggests that reasoning abilities, such as connection of node relationships, contribute to problem-solving. Conversely, inputting less relevant information without a coherent flow or reasoning in "Irrelevant Information Only" proves to be ineffective in performance. %resolving the problem.

We explore diverse prompt configurations and retrieval source datasets, detailed in Table \ref{table:ablation}, to assess the impact of incorporating extra information into the question. Our findings reveal that using a substantial web dataset, Wikipedia, as a source dataset does not enhance task performance. It is noteworthy that both Relevant Information and Graph scenarios experience decreased performance when incorporating more than three information sentences. The order of prompting is crucial, showing superior performance when retrieved documents precede the question rather than following it. Additionally, the direction of reasoning in the graph is essential, as evidenced by reduced performance when ignoring edge direction.

\input{tables/ablation_results}
 
% Our baseline is prompting with a plain question without additional documents. With RAG, we retrieve top-$N$, where $N = \{1, 2, 3\}$ sentences from verbalized triples of ConceptNet KG. In Graph, we extract sequential triples from an anchor node retrieved by similarity score with question concept while RAG extracts triples only using text similarity score with combined question concept with question. The result shows that graph-based reasoning using information from connected nodes boosts the performance in the condition that the number of information sentences is the same. Considering the performance, we analyze that if the start or end of relevant information is about the anchor node, it is helpful rather than the information being in the middle. Our best score is using comprehensive context which is combined RAG top-1 information with graph-based reasoning information.
% N 을 효율적으로 다이나믹하게 선택할수 있는방법



\section{Conclusion}
Our experiments were designed to investigate the impact of information retrieval exemplified by Relevant Information, and the reasoning process represented by KG-based random-walk, on improving commonsense QA. Consequently, delivering outcomes inferred through the proposed method generally led to better results than supplying relevant information in Relevant Information. Additionally, the experimental results matched our expectation that performance would be most improved when utilizing both information extracted by embedding matching and graph reasoning. However, we also obtained unexpected results where performance improved when providing irrelevant information through graph reasoning. We analyze that these results indicate providing a reasoning process can enhance the performance of commonsense QA.

The main limitation of this paper is that it is restricted to the commonsense QA task. However, since this task requires both reasoning ability and information with specific information, our experiments have empirically proven which method is more effective. This suggests that focusing on enhancing reasoning capabilities could be beneficial for improving commonsense QA performance in the future.

\bibliographystyle{ACM-Reference-Format}
\bibliography{neurips_2024_calm}

% \fi
\end{document}
\endinput
