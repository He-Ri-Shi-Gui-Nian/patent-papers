\begin{figure*}[!htp]
    \centering
    \includegraphics[width=0.95\textwidth]{img/model_structure.png}
    \caption{\textsc{Temp}'s Architecture. The left top  part is the dependency graph for query ``\emph{List the presidents of Asian countries that have held the Summer Olympics}", \emph{rel\_1}, \emph{rel\_2}, and \emph{rel\_3} represent the relations \emph{Hold}, \emph{Locate}, and \emph{President of}, respectively.} %\todo{We should keep the notation consistent: the orange hasType links should be from entities to their types (as in Fig 1), rather than the other way around. Also, where are the types of relations here?} \textcolor{blue}{\textbf{To Jeff(sorry nb doesn't work here): I have changed arrow direction for types to entities and relationships in Fig 1, because it should be the type supervise the entity. Types of relations are in the lower left corner (Step 1 part).}}}
    \label{fig_3}
\end{figure*}
\vic{
%We introduce our model \textsc{Temp}, which exploits type information readily available in KGs to answer queries.
\vicf{Our model} \textsc{Temp} is composed of  two sub-models:
%
\textit{Type-aware Entity Representations (TER)}, which uses  type information of an entity to enrich its vector representation, and \textit{Type-aware Relation Representations (TRR)}, which %uses entity type information to construct a \textit{type graph} to 
further integrates entity representations, relation types, and relation representations %by means of a bidirectional attention mechanism 
to strengthen the entity and relation vector representations simultaneously. 
%
Interestingly, as we only leverage type information to perform an in-depth characterization of entities and relations \emph{without modifying the training target} of existing QE-based models, \textsc{Temp} can be easily embedded into them in a plug-and-play fashion.} %Fig.~\ref{fig_3} shows  \textsc{Temp}'s architecture.} %\todo{Should we explain our claimed feature of plug-and-play somewhere in the technical section? Or how do people add our TEMP model to their QE solution?}%\nb{To Jeff: In the previous sentence of your sentence, we mentioned that \emph{as we only leverage type information to perform an in-depth characterization of entities and relations without modifying the training target of existing QE-based models}, If anyone want to use the TEMP module, they only need to replace the corresponding entities and relationships with the TEMP module. They don't need too many operations, only need to limit the vector space according to the corresponding QE method (Because different models may have different constraints on the space). Add some other description?}
%%One is to use the type information surrounding the entity to reinforce the entity representation, called TER (Type-aware Entity Representations), while the other is to use entity type information to build a \textit{type graph}, and further match entities, relations types, and relation information by bidirectional attention mechanism to strengthen the entity and relation representations simultaneously, called TRR (Type-aware Relation Representations). The overall architecture is shown in Figure \ref{fig_3}. 
%As we only leverage type information to perform in-depth characterization of entities and relations, without modifying the training target of original QE-based models, TEMP can be easily embedded into them in a plug-and-play fashion.
\subsection{TER: Type-aware Entity Representations}\label{sec:TER}
\vic{ The main intuition behind TER is that the types of an entity provide 
%for a given triple $(h,r,t)$ in a KG, the  type information of $h$ and $t$ can provide 
valuable information about what it represents in the KG. %corresponding entities.
For instance, if an entity contains types such as \textit{sports/multi\_event\_tournament, time/event, olympics/olympic\_games}, it is  plausible to infer that the corresponding entity represents %maybe 
\textit{Olympics}. To capture this intuition, we design an iterative  multi-highway layer~\cite{highway:2015} %\todo{Why do we use an iterative  multi-highway layer? Are there any alternatives?} \nb{To Jeff: The purpose of using iterative is to enrich the representation of entities by types, and the results of the previous round can be used as input for the next round. For other alternatives, we have elaborated in Appendix A Design Alternatives.} 
to aggregate the type information in entity type assertions % of  an entity 
to get a more accurate and comprehensive representation of it\footnote{See appendix for other aggregation alternatives.}. Let  $\mathcal{H}_{s}^{i}\in\mathbb{R}^{d\times n}$ denote the hidden state of type information of an entity %$h$ or $t$ 
in iteration $i \geq 1$, where \emph{d} and \emph{n} respectively represent the vector size and the number of types of an entity. The highway-based type fusion representation of a given entity can be calculated as follows:}

%their corresponding entities. For example, if an entity contains types such as ``sports, multi\_event\_tournament", ``time, event", ``olympics, olympic\_games", it's reasonable to guess that the corresponding entity is related to %maybe 
%`Olympics". We design an iterative  multi-highway layer to aggregate the type information around the entity, to form its more accurate and comprehensive representation ~\cite{highway:2015}. In particular, let us denote $\mathcal{H}_{s}^{i}\in\mathbb{R}^{d\times n}$ as the hidden state of type information around entity $e_{h}$ or $e_{t}$ in iteration \textit{i}. \emph{d} and \emph{n} represent the vector size and the number of types of specify entity, respectively. The highway-based type fusion representation of a given entity can be calculated as follows:
\vspace{-0.2cm}
\begin{equation}
g = \sigma(W_{i}\mathcal{H}_{s}^{i} + b_{i})
\label{highway_agg_1}
\end{equation}
\vspace{-0.3cm}
\begin{equation}
\mathcal{H}_{s}^{i+1} = g\ast(W_{i}'\mathcal{H}_{s}^{i} + b_{i}') + (1-g)\ast\mathcal{H}_{s}^{i}
\label{highway_agg_2}
\end{equation}
\vspace{-0.2cm}
\begin{equation}
\widetilde{\mathcal{H}_{s}^{K}} = W\mathcal{H}_{s}^{K} + b
\label{highway_agg_3}
\end{equation}
%\par Where 
\zhiwei{ $\mathcal{H}_{s}^{1}$} is the initial feature of types of  an entity, $\sigma$ is an element-wise sigmoid function, \{$W_{i}, W_{i}'$\}$\in\mathbb{R}^{d\times d}$, \{$b_{i}, b_{i}'$\}$\in\mathbb{R}^{d\times 1}$ are  learnable matrices, and \emph{g}$\in\mathbb{R}^{d\times n}$ is the reset gate. After iterating \emph{K} times \zhiwei{(we set \emph{K}=2)}, the final message $\mathcal{H}_{s}^{K}\in\mathbb{R}^{d\times n}$ (undergoing a linear operation to obtain $\widetilde{\mathcal{H}_{s}^{K}}\in\mathbb{R}^{d\times 1}$) is taken as the representation for the  types of a given entity. %\nb{Zhiwei: Is this sentence missing some elements? The subject of is seems to be incoherent with the previous sentence.}}
%\par Where $\mathcal{H}_{s}^{0}$ is initial feature of types around the entity, $\sigma$ is an element-wise sigmoid function, \{$W_{i}, W_{i}'$\}$\in\mathbb{R}^{d\times d}$, \{$b_{i}, b_{i}'$\}$\in\mathbb{R}^{d\times 1}$ are  learnable matrices. \emph{g}$\in\mathbb{R}^{d\times n}$ is the rese gate. After repeating \emph{K} times, the final message $\mathcal{H}_{s}^{K}\in\mathbb{R}^{d\times n}$, undergoing linear operation to obtain $\widetilde{\mathcal{H}_{s}^{K}}\in\mathbb{R}^{d\times 1}$, is taken as the representation for the neighbor types of the entity.
%
 \vic{We further concatenate the initial entity and its type aggregation representation to get an enhanced entity representation.}
 %\vspace{-0.1cm}
\begin{equation} 
\mathcal{H}^{e} = W'[\widetilde{\mathcal{H}_{s}^{K}}, \mathcal{H}_{s}^{0}] + b',
\label{resnet_entity}
\end{equation}   
 \vic{where [$\cdot$] is the concatenation function, $W^{'}$$\in\mathbb{R}^{d\times d}$ and $b^{'}$$\in\mathbb{R}^{d\times 1}$ are the parameters to learn. $\mathcal{H}^{e}\in\mathbb{R}^{d\times 1}$ is the final representation of the entity. It is important to note that for inductive reasoning, we will not concatenate with the initial entity information $\mathcal{H}_{s}^{0}$ as the entities seen during training are not presented in the test phase. The process is shown in top center of Figure~\ref{fig_3}.}
\vspace{-0.2cm}


% \par We further concatenate the initial entity and its type aggregation representation to obtain an enhanced entity representation. It is worth noting that for the inductive ability test, we will not concatenate with the initial entity information.
% \begin{equation}
% \mathcal{H}^{e} = W'[\widetilde{\mathcal{H}_{s}^{K}}, \mathcal{H}_{e}] + b'
% \label{resnet_entity}
% \end{equation}
% \par Where [$\cdot$] is the concatenation function, $\mathcal{H}_{e}\in\mathbb{R}^{d\times 1}$ is initial representation of entity, $W^{'}$$\in\mathbb{R}^{d\times d}$ and $b^{'}$$\in\mathbb{R}^{d\times 1}$ are the parameters to learn. $\widetilde{\mathcal{H}_{s}^{K}}\in\mathbb{R}^{d\times 1}$ is the final representation of the entity. The process is shown in top left of Figure 3.


\subsection{TRR: Type-aware Relation Representations}\label{sec:TRR}
%\textcolor{red}{Paragraph under revision:} Performing TER on entities is useful for \emph{NV-QS}, such as 1p, 2i, 3i, in Figure 2. However, for \textit{long-chain} \emph{EV-QS} containing variable nodes, such as 2p, 3p, pi, it is not enough to only perform TER on the anchor and target nodes. 
\vic{Performing TER on entities is useful for queries without existentially quantified variables. However, for queries with \emph{chained}  existential variables (chain of variable nodes in the DG) it is not enough to only perform TER on the anchor entity \zhiwei{or} target variable. Intuitively, the problem is that in the long-chain reasoning process, the  correlation between the anchor entity  and target variable may not be strong enough after several relation projections. Besides, continuous relational projections may cause exponential growth in the search space, further increasing the complexity of \zhiwei{the model}.}
%The reason lies in that in the entire long-chain reasoning process, the intuitive correlation between the anchor and target nodes may not be strong enough after several relation projections. Besides, continuous relational projections may cause exponential growth in the search space, further increasing the complexity of our model.
\par %Actually, in addition to the type information of the entity, 

\vic{We start by observing that the types of a relation  are correlated with its representation in the KG. For example, assuming a relation $r$ has types  \textit{government/ political\_appointer} and \textit{organization/role}, then we can plausibly infer that the relation  $r$ represents  \textit{President of}.
%We observe type of a relation $r_{h\rightarrow t}$ is also highly correlated with relation over KGs. For example, assume $r_{h\rightarrow t}$ is surrounded by the types such as ``\emph{government, political\_appointer}", ``\emph{organization, role}", then we can infer the relation  $r_{h\rightarrow t}$ is probably related to relation \textit{President of}. 
In long-chain queries, type-enhancement on relations can help to reduce the answer entity space and cascading errors caused by multiple projections. However,  in most existing KGs,  relations lack  specific type annotations (such as domain and range). We address this problem %\nb{Zhiwei: What does this problem mean? Because in my opinion, our Step1~3 are not just to solve the problem that there is no type of relationship in KG, but from the semantic analysis of this sentence, this problem should only mean what Step 1 did.} 
by building, based on the original KG, a novel %type-aware knowledge graph, 
\emph{type graph} with types as nodes and relations as edges (see bottom left of Fig.~\ref{fig_3}). %, i.e.\ we  relate types  in the same way the entities they belonged to  are related.
%Nevertheless, in the existing KGs, the relations lack the specific type annotations. We use three steps to address this problem. In step 1, we propose to construct a new type-aware knowledge graph named \emph{type graph}, with types as nodes and relations as edges, i.e., to connect the type through relations, based on the original KGs.
In a subsequent step, we aggregate the type information on the type graph to obtain the type embedding corresponding to a specific relation. Finally, we integrate the entity representation, the aggregated type information of a  relation and its %\nb{Zhiwei: Do we need to be more explicit about its? Although I know what its means.} 
representation by a bidirectional attention mechanism, so that the intermediate variable nodes can perceive the message of anchor or target nodes and of the relations in the chain of reasoning (see right of Fig.~\ref{fig_3}). This will help to avoid the weakening of the connection between anchor and target entity caused by long-chain reasoning.}
%In step 3, we match the entity, aggregation type content of specific relation and relation message by a bidirectional attention mechanism, so that the intermediate variable nodes can perceive the message of anchor or target nodes and the relations in the chain of reasoning, to avoid the weakening of the relationship between anchor and target entity, caused by long-chain reasoning.
\subsubsection{Step 1: Type Graph Construction}
%The types of head $e_{h}$ and tail $e_{t}$ in a particular triple ($e_{h}$,$r_{h\rightarrow t}$,$e_{t}$) typically contain some %implicit 
%evidence that can be used to deduce the relations. %Hence, we construct a type graph based on the original KGs. 
\vic{We formally define a \emph{type graph $\mathcal G_\mathsf{tp}$}. }%, with entity type information as nodes and relations as edges.} %Different relations can be related to each other through type knowledge in the \emph{type graph}. 
 Let  $\mathcal G = (\mathcal{E,R,C,T})$ be a KG. For a  relation $r\in \mathcal{R}$, 
 $\mathcal T_r\subseteq \mathcal T$  denotes the set of  relation assertions in which $r$ occurs. For a  relation assertion $\mathsf t\in  \bigcup_{r \in \mathcal R}\mathcal T_r$, $\mathsf{hd}(\mathsf t)$ and $\mathsf{tl}( \mathsf t)$   respectively denote 
the head  and tail entities of $\mathsf t$, and $\mathsf{tp}_\mathsf t (\mathsf{hd}(\mathsf t)) = \{c \mid (\mathsf{hd}(\mathsf t), type, c) \in \mathcal T\}$ denotes the set of types of  the head   of $\mathsf t$; $\mathsf{tp}_{\mathsf t}(\mathsf{tl}( \mathsf t))$ is defined analogously. 
%For a given relation assertion $\mathsf t \in \mathcal T$,  let  $\mathsf{tp}_\mathsf t (h)$ and $\mathsf{tp}_{\mathsf t}(t)$ denote the set of types of  entities $h$ and $t$ in $\mathsf t$. 
Since  $r$ may occur in multiple relation assertions, we will compute the type information of $r$ by taking the intersection of the types of the head and tail entities of relation assertions in which  $r$ occurs. %More precisely, for a  relation $r\in \mathcal{R}$, %we use
%the set of $\mathcal T_r\subseteq \mathcal T$    relation assertions in which $r$ occurs, and  
For $r \in \mathcal R$, %for a  relation assertion $\mathsf t\in  \mathcal T_r$, $\mathsf{hd}(\mathsf t)$ and $\mathsf{tl}( \mathsf t)$   respectively denote 
%the head  and tail entities of $\mathsf t$. %and   to denote the set of  tail entities  in $\mathcal T_r$. 
we define
\begin{equation*}
    \mathsf{tp}_{r}^{\mathsf{hd}}(\mathcal G) = \bigcap_{ \mathsf t \in \mathcal T_r} \mathsf{tp}_\mathsf t(\mathsf{hd}(\mathsf t)), \qquad \mathsf{tp}_{r}^{\mathsf{tl}}(\mathcal G) = \bigcap_{\mathsf t \in \mathcal T_r} \mathsf{tp}_\mathsf t(\mathsf{tl (t)}).\vspace{-0.1cm}
     \end{equation*}
% \begin{equation}
% \mathsf{tp}_{r}(h) = \mathsf{tp}_{r}(h_{1}) \cap \mathsf{tp}_{r}(h_{2}) \ \cap  \ldots \cap \ \mathsf{tp}_{r}(h_{n})
% \end{equation}
% \vspace{-0.2cm}
% \begin{equation}
% \mathsf{tp}_{r}(t) = \mathsf{tp}_{r}(t_{1}) \cap \mathsf{tp}_{r}(t_{2}) \cap  \ldots \cap \mathsf{tp}_{r}(t_{n}),
% \end{equation}
%where \emph{n} denotes the number of triples of a specified relation $r$ in the KGs, and ($e_{h_{i}}$,$r$,$e_{t_{i}}$) represents the \emph{i-th} triple containing the relation \emph{r}. 
\vic{%$\mathsf{tp}^{\mathsf{hd}}_{r}(K)$ and $\mathsf{tp}_{r}^\mathsf{tl}(K)$ are respectively the  intersection of the types of the head and tail entities of all triples connected by $r$. 
In addition, we define $\mathcal G_\mathsf{tp}=(V, E, T)$ by setting 
%
$V = \bigcup_{r \in \mathcal R} \mathsf{tp}^{\mathsf{hd}}_{r}(\mathcal G) \cup \mathsf{tp}^{\mathsf{tl}}_{r}(\mathcal G)$ %$\nb{Zhiwei: Why we use K, should not be $\mathcal G $? Besides, $\mathsf{tp}_{r}^{\mathsf{hd}}(\mathcal G)$ and $\mathsf{tp}_{r}^{\mathsf{tl}}(\mathcal G)$ have no description?}, 
\zhiwei{,} $E=\mathcal R$, and $(v,r,v') \in T$ if there exists $\mathsf t \in \mathcal T_r$ such that $v = \mathsf{tp}_\mathsf t(\mathsf{hd(t)})$ and $v' = \mathsf{tp}_\mathsf t(\mathsf{tl(t)})$.}  %Figure \ref{fig_3} illustrates a type graph.}\nb{V: @Zhiwei: The actual type graph  was not defined. I am planning to define it, but I am not entirely sure what the nodes are. I dont think are all types as we are taking the intersection.} %after the Step 1, the type nodes corresponding to the triples containing the same relation will be linked together through the corresponding relation.
%As shown in Figure \ref{fig_3}, after the Step 1, the type nodes corresponding to the triples containing the same relation will be linked together through the corresponding relation.
\subsubsection{Step 2: Relation Type Aggregation}
\vic{For a given  relation $r \in E$, %in the \emph{type graph}, 
we define the types associated with a relation as $\mathsf{tp}_r(\mathcal G_\mathsf{tp})$ =  $\mathsf{tp}^{\mathsf{hd}}_{r}(\mathcal G) \cup \mathsf{tp}^{\mathsf{tl}}_{r}(\mathcal G)$. We fix an arbitrary linear order on the elements of $\mathsf{tp}_r(\mathcal G_\mathsf{tp})$, %\nb{Zhiwei: I don't quite understand what this sentence means.}, 
and denote by $\mathsf{tp}^i_r(\mathcal G_\mathsf{tp})$ the $i$-th type, for all $1 \leq i \leq |\mathsf{tp}_r(\mathcal G_\mathsf{tp})|$.  Note that not all types in $\mathsf{tp}_r(\mathcal G_\mathsf{tp})$ are relevant for answering a given query. For example, assume that  the relation \emph{has\_part} contains the types \{\emph{vehicle}, \emph{animal}, \emph{universe}\}. For the query \emph{``What organs are parts of a cat?"}, we should give type \emph{animal} more attention, but for the query \emph{``What components are parts of a car?"} we should concentrate on the type \emph{vehicle}.}
%we take an intersection\nb{V: @Zhiwei, are you taking the intersection or the union. I do not understand why would you be only interested in the types that are common for head and tail (that is what you obtained with intersection). For the moment I am also using intersection but something seems strange} of $\mathcal{T}_{r}(e_{h})$ and $\mathcal{T}_{r}(e_{t})$ to get $\mathcal{T}_{r}(e_{h}) \cap \mathcal{T}_{r}(e_{t}) = \mathcal{T}_{r}(e)$ = \{$\mathcal{I}_{s}^{1},\mathcal{I}_{s}^{2}, . . . , \mathcal{I}_{s}^{m}$\}, where \emph{m} is the number of types corresponding to the head and tail entities of the triples containing the relation \emph{r}. $\mathcal{I}_{s}^{i}$ is the \emph{i-th} type of the set $\mathcal{T}_{r}(e)$. %The relation may have multiple types, but not all types need to be paid attention to; only the types related to the queries should be focused on. For example, for the relation \emph{has\_part}, it contains the type \{\emph{vehicle}, \emph{animal}, \emph{universe}, ...\}. For query \emph{``What organs are parts of cat?"}, we should give type \emph{animal} more attention. But for the query \emph{``What components are parts of car?"}, we should concentrate on the type \emph{vehicle}.
\vic{So, instead of simply concatenating (or averaging) all the type information associated to a relation, we model the relation type aggregation as an attention neural network, %. The mathematical formulas of the aggregation operation are 
defined as: \\}
\vspace{-0.2cm}
\begin{equation}
\mathcal{H}^{s} = \sum_{i} a_{i} \odot \mathcal{H}_{s}^{i}
\label{type_aggr}
\end{equation}
\vspace{-0.1cm}
\begin{equation}
a_{i} = \frac{exp(\textbf{MLP}(\mathcal{H}_{r}^{i}))}{\sum_{j}exp(\textbf{MLP}(\mathcal{H}_{r}^{j}))}
\end{equation}
%\vspace{-0.1cm}
\vic{ $\mathcal{H}^{s}$ is the vector representation of the aggregated type information $\mathsf{tp}_r(\mathcal G_\mathsf{tp})$; $\mathcal{H}_{s}^{i}\in{\mathbb{R}^{d\times1}}$ is the  vector representation of the \emph{i-th} type $\mathsf{tp}_r^i(\mathcal G_\mathsf{tp})$, which is initialized to a uniform distribution with dimension \emph{d}, $1 \leq i \leq |\mathsf{tp}_r^i(\mathcal G_\mathsf{tp})|$; $a_{i}\in{\mathbb{R}^{d\times1}}$ is a positive weight vector that satisfies $\sum_{i=1}^{n}[a_i]_{j}=1$ for all $ 1 \leq j \leq d$; %$\odot$ is the dimension-wise product;
and \textbf{MLP}($\cdot$) : $\mathbb{R}^{d} \rightarrow \mathbb{R}^{d}$ is a multi-layer perceptron network.}
%Where $\mathcal{H}^{s}$ is the vector representation of the aggregated type information $\mathcal{T}_{r}(e)$; $\mathcal{H}_{s}^{i}\in{\mathbb{R}^{d\times1}}$ is the \emph{i-th} vector representation of types $\mathcal{I}_{r}^{i}$, which is initialized to a uniform distribution with dimension \emph{d}, $1 \leq i \leq m$; $a_{i}\in{\mathbb{R}^{d\times1}}$ is a positive weight vector that satisfies $\sum_{i=1}^{n}[a_i]_{j}=1$ for all j = 1, . . . , \emph{d}; $\odot$ is the dimension-wise product; \textbf{MLP}($\cdot$) : $\mathbb{R}^{d} \rightarrow \mathbb{R}^{d}$ is a multi-layer perceptron network.
\subsubsection{Step 3: Pairwise Representation Integration}
\vic{When embedding  queries, integrating the information of entities, relations, and types can help to smooth decision boundaries, but  %to get correct answers, 
this needs to be done in a way that the intended match of the query into the KG is captured. For example, for  the query  \emph{``Which countries have held the Summer Olympics}?", %, represented by the query chain  \emph{Summer Olympics} $\rightarrow$ \emph{Held} $\rightarrow$ \emph{?}.} 
%$V?.\textit{Held(Summer Olympics},V_?)$ }.  
we need to concentrate on \emph{Held} connections from  \emph{Summer Olympics}, rather than e.g.,\ \emph{Watch} connections. Analogously, we should only consider \emph{Held} connections starting at  \emph{Summer Olympics}, rather than e.g.,\ at \emph{World Cup}.}
%For the intersection of entity and relation, the relation \emph{is Held} can restrict the relations related to entity \emph{the Summer Olympics}, although \emph{the Summer Olympics} may also relate to \emph{Watch} or \emph{Win} relations. \vic{Similarly, \emph{Summer Olympics} can narrow the scope of \emph{Held} to exclude entities such as \emph{World Cup} or \emph{Concerts}.}\nb{V: How is this example related wit types? They are not mentioned at all.} 
%For the intersection of entity and relation, the relation \emph{is Held} can restrict the relations related to entity \emph{the Summer Olympics}, although \emph{the Summer Olympics} may also relate to \emph{Watch} or \emph{Win} relations. \vic{Similarly, \emph{Summer Olympics} can narrow the scope of \emph{Held} to exclude entities such as \emph{World Cup} or \emph{Concerts}.}\nb{V: How is this example related wit types? They are not mentioned at all.}
\vic{To  properly capture this restriction %\nb{Zhiwei: Can we change to another word? because it might confuse the logic intersection.}, 
in the triple \{$\mathcal{H}^{e}$, $\mathcal{H}^{r}$, $\mathcal{H}^{s}$\}  ($\mathcal{H}^{e}$ and $\mathcal{H}^{s}$ defined as in Equations~\eqref{resnet_entity} and \eqref{type_aggr}, and $\mathcal{H}^{r}$ is the initialization relation vector), % Inspired by some previous work ~\cite{wang2018co} and ~\cite{zhang2020dcmn}, 
we introduce a \textit{bidirectional attention mechanism}~\cite{zhang2020dcmn} to integrate each state of pairwise representation pairs: \emph{entity-relation, entity-type,} and \emph{relation-type}.  %We then use a gated strategy to aggregate the representation from other information\nb{V: I dont understand this. It is too vague. Which informatioN?}. 
Here, we show how to do this for entity-relation \zhiwei{pair}.
%model the relationship between entity and relation information pair as an example. %Without loss of generality, it can be easily migrated to the other two pairs.
 Bidirectional integration representation between %the entity 
 $\mathcal{H}^{e}$ and %the relation 
 $\mathcal{H}^{r}$ can be calculated as follows:}
\begin{equation}
\mathcal{G}^{er} = Relu(W_{1}\begin{bmatrix}
\mathcal{H}^{e} \ominus \mathcal{H}^{r}\\
\mathcal{H}^{e} \otimes \mathcal{H}^{r}
\end{bmatrix} + b_{1})
\end{equation}
\vspace{-0.1cm}
\begin{equation}
\mathcal{G}^{re} = Relu(W_{2}\begin{bmatrix}
\mathcal{H}^{r} \ominus \mathcal{H}^{e}\\
\mathcal{H}^{r} \otimes \mathcal{H}^{e}
\end{bmatrix} + b_{2})
\end{equation}
\vic{ $\{W_{1}, W_{2}\} \in \mathbb{R}^{2d\times 2d}$ and \{$b_{1}, b_{2}\} \in \mathbb{R}^{2d\times 1}$ are learnable parameters. %$\begin{bmatrix} . \\ . \end{bmatrix}$ is the column-wise concatenation of two matrices. %Inspired by ~\cite{tai2015improved} , ~\cite{wang2016compare}, and ~\cite{wang2018co},
We use element-wise subtraction $\ominus$ and multiplication $\otimes$ to build better matching representations~\cite{tai2015improved}. $\mathcal{G}^{er} \in \mathbb{R}^{2d\times 1}$ is the result of integrating entity relation information. Through bidirectional integration of entities and relations, we simultaneously  get a relation-aware entity representation and an entity-aware relation representation, capturing the interaction between entities and relations.}
%using relation  to match entity information.  Similarly, we match the relation with the entity and represent the matching results as $\mathcal{G}^{re} \in \mathbb{R}^{2d\times 1}$. Through bidirectional matching of entities and relations, we obtain the relation-aware entity representation and entity-aware relation representation at the same time, which can effectively capture the relationship between entity and relation. Using the same procedure, we can calculate the type-aware entity $\mathcal{G}^{es}$ and type-aware relation $\mathcal{G}^{rs}$ representations.
%\par Where $\{W_{1}, W_{2}\} \in \mathbb{R}^{2d\times 2d}$ and \{$b_{1}, b_{2}\} \in \mathbb{R}^{2d\times 1}$ are learnable parameters. %$\begin{bmatrix} . \\ . \end{bmatrix}$ is the column-wise concatenation of two matrices. %Inspired by ~\cite{tai2015improved} , ~\cite{wang2016compare}, and ~\cite{wang2018co},
%We use $\ominus$ and $\otimes$ to build better matching representations~\cite{tai2015improved} \cite{wang2016compare}\cite{wang2018co}, denoting element-wise subtraction and multiplication between two matrices, respectively. $\mathcal{G}^{er} \in \mathbb{R}^{2d\times 1}$ represents the result of using relation  to match entity information.  Similarly, we match the relation with the entity and represent the matching results as $\mathcal{G}^{re} \in \mathbb{R}^{2d\times 1}$. Through bidirectional matching of entities and relations, we obtain the relation-aware entity representation and entity-aware relation representation at the same time, which can effectively capture the relationship between entity and relation. Using the same procedure, we can calculate the type-aware entity $\mathcal{G}^{es}$ and type-aware relation $\mathcal{G}^{rs}$ representations.

 \vic{We  then use a gated mechanism to combine the results produced by bidirectional fusion as it better regulates the information flow~\cite{highway:2015}. Take the entity fusion representation as an example, using the  relation-aware entity $\mathcal{G}^{er}$ and type-aware entity $\mathcal{G}^{es}$ representations as input, the final representation of entity is computed as }
%\par Because gate is more powerful to regulate the information flow ~\cite{highway:2015}, we use the gated mechanism to fuse the results expressed by bidirectional matching. Take the entity fusion representation as an example, using relation-aware entity $\mathcal{G}^{er}$ and type-aware entity $\mathcal{G}^{es}$ representations as input, the final representation of entity is computed as: %, we denote this aggregation way as gate-based aggregator.
\vspace{-0.1cm}
\begin{equation}
g = \sigma(W_{3}\mathcal{G}^{er} + W_{4}\mathcal{G}^{es} + b_{3} + b_{4})
\label{dcmn_1}
\end{equation}
\vspace{-0.3cm}
\begin{equation}
\widetilde{\mathcal{G}^{e}} = g\ast\mathcal{G}^{er} + (1 - g)\ast\mathcal{G}^{es}
\label{dcmn_2}
\end{equation}
 \vic{\{$W_{3}$, $W_{4}$\} $\in$ $\mathbb{R}^{2d\times2d}$ and \{$b_{3}$, $b_{4}$\} $\in$ $\mathbb{R}^{2d\times1}$ are the parameters to learn. \emph{g} is the reset gate, and  $\widetilde{\mathcal{G}^{e}}\in \mathbb{R}^{2d\times1}$ is the final entity representation.} %We refer to this  aggregation way as \emph{gate-based aggregator}.\nb{V: Why do you need to introduce this term. Where are you using it again?} }

%\{$W_{3}$, $W_{4}$\} $\in$ $\mathbb{R}^{2d\times2d}$ and \{$b_{3}$, $b_{4}$\} $\in$ $\mathbb{R}^{2d\times1}$ are the parameters to learn. \emph{g} is the reset gate. $\widetilde{\mathcal{G}^{e}}\in \mathbb{R}^{2d\times1}$ is the final entity representation. We call this aggregation way as gate-based aggregator. 
\vic{To transform the fused feature to the original vector size, we use one linear layer: $\overline{\mathcal{H}^{e}} = W_{5}\widetilde{\mathcal{G}^{e}} + b_{5}$,
%In order to transform the fused feature to the original vector size, we use one linear layer as follows.
% \vspace{-0.2cm}
% \begin{equation}
% \overline{\mathcal{H}^{e}} = W_{5}\widetilde{\mathcal{G}^{e}} + b_{5}
% \end{equation}
 where $W_{5}$ $\in$ $\mathbb{R}^{d\times2d}$ and $b_{5}$ $\in$ $\mathbb{R}^{d\times1}$ are learnable parameters. $\overline{\mathcal{H}^{e}}$ is the final  entity representation enhanced by relation and type.}

% \subsection{Design Alternatives}
% \label{section_design_alternatives}
% In our ablation experiments, we will compare TEMP with the following alternative models. In particular, when aggregating the neighbor type  of a certain entity, we propose two alternatives for aggregator, instead of the highway  aggregator in Eqs. (\ref{highway_agg_1}), (\ref{highway_agg_2}), and (\ref{highway_agg_3}). \\
% \textbf{Mean aggregator:}
% \begin{equation}
% \widetilde{\mathcal{H}_{s}^{K}} = \frac{1}{n}\sum_{i} \mathcal{H}_{s_{i}}^{K}
% \end{equation}
% \par Here, the mean operation does not require a loop operation, so the value of \emph{K} is 0. $\mathcal{H}_{s_{i}}^{K}\in\mathbb{R}^{d\times 1}$ represents the \emph{i-th} component of vector $\mathcal{H}_{s}^{K}\in\mathbb{R}^{d\times n}$. \\
% \textbf{Max  aggregator:}
% \begin{equation}
% \widetilde{\mathcal{H}_{s}^{K}} = Max(\mathcal{H}_{s_{i}}^{K})
% \end{equation}
% \par Where $Max(\cdot)$ represents the element-wise maximum operation among the entity's type information. \emph{K} also takes 0.\\
% \textbf{Concatenation-based intersection.} Inspired by ~\cite{liu2016learning} and ~\cite{reimers2019sentence}, we apply an interactive concatenation to pair of the representations $\mathcal{G}^{er}$ and $\mathcal{G}^{es}$ in Eqs.(\ref{dcmn_1}) and (\ref{dcmn_2}), and then perform a linear layer operation. The interactive concatenation can be written as follows.
% \begin{equation}
% \widetilde{\mathcal{G}^{e}} = W[\mathcal{G}^{er}, \mathcal{G}^{es}, \mathcal{G}^{er}+\mathcal{G}^{es}, \mathcal{G}^{er}\times\mathcal{G}^{es}] + b
% \end{equation}
% \par Where $+$ and $\times$ represent the element-wise addition and multiplication between two matrices $\mathcal{G}^{er}$ and $\mathcal{G}^{es}$, respectively. $[\cdot, \cdot, \cdot, \cdot]$ is used to concatenate the vector in row level.