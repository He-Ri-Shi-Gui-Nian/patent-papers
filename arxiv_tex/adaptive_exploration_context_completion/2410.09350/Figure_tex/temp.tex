\begin{figure*}[t]
% \small
\centering
\includegraphics[width=1.0\textwidth]{Figures/main_figure.pdf}
% \vspace{-5pt}
\caption{
The overall inference process of DialogGSR.
DialogGSR consists of a generative subgraph retriever and response generator.
(a) Generative subgraph retrieval autoregressively retrieves subgraphs via generative subgraph retriever with graph-constrained decoding based on entity informativeness score.
% The generative subgraph retriever decodes the next token according to the graph constraint.
% In step 0, given the dialog, GSR generates an appropriate knowledge triplet $\langle$ Messi, Nationality, Argentina $\rangle$ since \texttt{Messi} is a mentioned entity in the dialog.
% In step 0, given the dialog, GSR generates an appropriate knowledge triplet $\langle$ \texttt{Messi}, \texttt{position(s)}, \texttt{Forward} $\rangle$ since \texttt{Messi} is a mentioned entity in the dialog.
% Then, the retrieved triplet is represented as ``\texttt{[Head]} \texttt{Messi} \texttt{[Int1]} \texttt{position(s)} \texttt{[Int2]} \texttt{Forward} \texttt{[Tail]}". % through knowledge graph prompting.
In step 0, given the dialog, GSR retrieves the most relevant triplets by referring to the graph constraint.
% GSR generates an appropriate knowledge triplet ``\texttt{[Head]} \texttt{Messi} \texttt{[Int1]} \texttt{position(s)} \texttt{[Int2]} \texttt{Forward} \texttt{[Tail]}" represented with special tokens, since \texttt{Messi} is a mentioned entity in the dialog.
% Then, the retrieved triplet is represented as ``\texttt{[Head]} \texttt{Messi} \texttt{[Int1]} \texttt{position(s)} \texttt{[Int2]} \texttt{Forward} \texttt{[Tail]}".
In step 1, given the dialog and the prompt-augmented triplet, we generatively retrieve the next triplets.
(b) Resposne generator generates the responses with the dialog and the prompt-augmented generated subgraph.
}
\label{fig:main}
% \vspace{-13pt}
\end{figure*}

% The Knowledge path Prompting-based Dialogue Generation~(KPDG) model generates responses with a Knowledge Graph~(KG).
% Given a dialog history, multi{\textendash}hop knowledge paths are retrieved from the KG.
% Then, soft prompts are added to knowledge paths to improve path embeddings~\ref{sec:4.1}.
% We retrieve context{\textendash}relevant knowledge paths by calculating the relevance between the path embeddings and a context embedding~\ref{sec:4.2}.
% Finally, we generate responses with dialogue history and the retrieved knowledge paths~\ref{sec:4.3}.
