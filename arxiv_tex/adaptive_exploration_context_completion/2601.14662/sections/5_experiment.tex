\section{Experiment}

% \subsection{Experiment Setup}

% We evaluate AGEA on three document corpora from different domains (Medical, Agriculture, and Novel) and on five knowledge-graph instances built from these corpora, including Medical, Agriculture, two single-novel graphs, and the full Novel graph. 
% This setup lets us assess attack effectiveness across graphs of varying domain, size, and structure.

% \noindent\textbf{Datasets} Our main benchmarks are two large domain-specific knowledge graphs: the Medical dataset~\cite{xiao2025graphrag}, which contains clinical guidelines and medical knowledge (diseases, treatments, procedures), and the Agriculture dataset~\cite{zhou2025depth}, which focuses on agricultural practices, crops, and related technologies.
% We also use the Novel dataset~\cite{xiao2025graphrag}, a corpus of 20
% pre-20th-century novels. From this corpus, we construct three graph instances: two smaller literary subgraphs---\emph{Dragon Blood} (novel 9) and \emph{Short Story Collection} (novel 13)---and the full Novel (20 books) graph. The literary datasets are mainly used to evaluate the scalability of AGEA due to their varying sizes and diverse scope. Dataset statistics are given in Table~\ref{tab:graph_stats} and more corpus details are provided in Appendix~\ref{dataset_appendix}.

% \noindent\textbf{GraphRAG systems and threat model} We evaluate our framework on two representative GraphRAGs: LightRAG~\cite{guo2024lightrag} and Microsoft GraphRAG\cite{edge2024local} (M-GraphRAG). 
% Both index the same underlying corpora but use different graph construction strategies, resulting in distinct internal textual knowledge graphs composed of entities, relations, and associated descriptions. In all experiments, we treat each system’s constructed knowledge graph as the protected asset, and assume the attacker has only black-box query access to the GraphRAG system.


% \noindent\textbf{Infrastructure and Model Configuration} Our framework consists of multiple LLM components serving different roles. We use GPT-4o-mini as the query generator and graph extractor, DeepSeek V3.1 as the primary query model for the victim GraphRAG system, and text-embedding-3-large as the embedding model for GraphRAG retrieval. Unless otherwise specified, we adopt the local search in M-GraphRAG and the hybrid search in LightRAG.
% For the parameters in adaptive query mode selection, we use $\epsilon_{\text{init}} = 0.3$, $\epsilon_{\min} = 0.05$, decay factor $0.98$ per turn, and $\tau_{\text{init}} = 0.15$ for the main experiments. 
% More details of hyperparameters are given in Appendix~\ref{dataset_appendix}.
\subsection{Experiment Setup}
\begin{table*}[!t]
\centering
\caption{Main experimental results comparing AGEA against baseline attack methods on two GraphRAG systems with 1000 query budgets. Leak(N)/Leak(E) denote node/edge leakage rates, and Prec(N)/Prec(E) denote precision. Best results are shown in \textbf{bold} and second best are {underlined} in each system.}
\label{tab:main_two_systems}
\vskip -1em
\small
\begin{minipage}{0.49\textwidth}
\centering
\setlength{\tabcolsep}{3pt}
\renewcommand{\arraystretch}{1.05}
\resizebox{\linewidth}{!}{%
\begin{tabular}{lcccc}
\toprule
\multicolumn{5}{c}{\textbf{M-GraphRAG}} \\
\midrule
\textbf{Method*} & \textbf{Leak(N)} & \textbf{Leak(E)} & \textbf{Prec(N)} & \textbf{Prec(E)} \\
\midrule
\multicolumn{5}{c}{\textit{Medical}} \\
\cmidrule(lr){1-5}
IKEA \cite{wang2025silent}&14.27&2.31&6.76&0.78\\
CopyBreakRAG \cite{jiang2025feedback} &73.65&31.88&32.19&8.96\\
TGTB \cite{zeng2024good}&\underline{76.99}&\underline{56.04}&61.68&24.53\\
PIDE \cite{qifollow} & 58.84&33.55&\underline{72.96}&\underline{29.03}\\
\textbf{AGEA} (Proposed) & \textbf{87.09} & \textbf{80.16} & \textbf{87.09} & \textbf{61.18} \\

\midrule
\multicolumn{5}{c}{\textit{Agriculture}} \\
\cmidrule(lr){1-5}
IKEA \cite{wang2025silent}&18.41&5.75&11.04&1.60\\
CopyBreakRAG \cite{jiang2025feedback}&63.38&43.93&34.77&13.10\\
TGTB \cite{zeng2024good}&\underline{75.63}&\underline{63.79}&53.07&20.91\\
PIDE \cite{qifollow}&70.23&54.63&\underline{59.03}&\underline{24.30}\\
\textbf{AGEA} (Proposed) & \textbf{84.67} & \textbf{84.13} & \textbf{93.08} & \textbf{76.81} \\
\bottomrule
\end{tabular}%
}
\end{minipage}
\hfill
\begin{minipage}{0.49\textwidth}
\centering
\setlength{\tabcolsep}{3pt}
\renewcommand{\arraystretch}{1.05}
\resizebox{\linewidth}{!}{%
\begin{tabular}{lcccc}
\toprule
\multicolumn{5}{c}{\textbf{LightRAG}} \\
\midrule
\textbf{Method*} & \textbf{Leak(N)} & \textbf{Leak(E)} & \textbf{Prec(N)} & \textbf{Prec(E)} \\
\midrule
\multicolumn{5}{c}{\textit{Medical}} \\
\cmidrule(lr){1-5}
IKEA \cite{wang2025silent}&15.95&5.88&20.33&5.63\\
CopyBreakRAG \cite{jiang2025feedback}&63.93&26.03&23.97&8.32\\
TGTB \cite{zeng2024good}&67.84&46.08&26.05&17.78\\
PIDE \cite{qifollow}&\underline{83.92}&\underline{73.93}&\underline{76.09}&\underline{74.93}\\
\textbf{AGEA} (Proposed) & \textbf{96.42} & \textbf{95.90} & \textbf{98.34} & \textbf{97.97} \\
\midrule
\multicolumn{5}{c}{\textit{Agriculture}} \\
\cmidrule(lr){1-5}
IKEA \cite{wang2025silent}&19.39&7.71&16.08&4.10\\
CopyBreakRAG \cite{jiang2025feedback}& 45.64&18.01&19.90&5.11\\
TGTB \cite{zeng2024good}&37.49&21.10&42.10&18.80\\
PIDE \cite{qifollow}&\underline{73.17}&\underline{52.95}&\underline{56.06}&\underline{46.53}\\
\textbf{AGEA} (Proposed) & \textbf{88.05} & \textbf{87.11} & \textbf{98.11} & \textbf{96.65} \\
\bottomrule
\end{tabular}%
}
\end{minipage}
\vskip -0.1in
\end{table*}


\noindent\textbf{Datasets.}
Our main benchmarks are two domain-specific graphs: Medical~\cite{xiao2025graphrag} (clinical guidelines spanning diseases, treatments, and procedures) and Agriculture~\cite{zhou2025depth} (agricultural practices, crops, and technologies). We also use the Novel corpus~\cite{xiao2025graphrag} (20 pre-20th-century novels) to construct three literary graph instances: two smaller subgraphs (\emph{Dragon Blood}, novel 9; \emph{Short Story Collection}, novel 13) and the full Novel (20 books) graph. These literary graphs primarily probe scalability under increasing graph size and broader entity/relationship diversity. Dataset statistics are in Table~\ref{tab:graph_stats}; additional corpus details are in Appendix~\ref{dataset_appendix}.%

\noindent\textbf{GraphRAG systems and threat model.}
We evaluate two representative GraphRAG systems: Microsoft GraphRAG (M-GraphRAG)~\cite{edge2024local} and LightRAG~\cite{guo2024lightrag}. 
Though they index the same corpora, they use different graph construction strategies, producing distinct internal textual KGs (entities, relations, and descriptions). We treat each constructed KG as a protected asset to be stolen by the attacker. We use the official implementations of each GraphRAG, and set retrieval parameters to top\_k=10 for both entity and relation retrieval.
% \footnote{
% We use the official implementations: \url{https://github.com/microsoft/graphrag} and \url{https://github.com/HKUDS/LightRAG}.
% }.

\noindent\textbf{Infrastructure and model configuration.}
AGEA uses GPT-4o-mini for query generation and extraction parsing, DeepSeek-V3.1 as the victim query model, and text-embedding-3-large for retrieval embeddings. All these models are accessed via Azure OpenAI APIs. Unless noted otherwise, we use local search in M-GraphRAG and hybrid search in LightRAG. For adaptive mode selection, we set $\epsilon_{\text{init}}{=}0.3$, $\epsilon_{\min}{=}0.05$, decay $0.98$ per turn, and $\tau_{\text{init}}{=}0.15$. %
\footnote{We provide detailed description of hyperparameter in Appendix~\ref{app:hyper}.}


\noindent\textbf{Baselines.}
We compare against four black-box RAG extraction attacks under identical query budgets: (i) two fixed-query prompt-injection methods, TGTB~\cite{zeng2024good} and PIDE~\cite{qifollow}; and (ii) two adaptive methods, CopyBreakRAG~\cite{jiang2025feedback} and IKEA~\cite{wang2025silent}, which iteratively generate queries across turns based on prior responses. 
For TGTB, we follow its composite prompt form (information + command) and evaluate its targeted variant by instantiating the information component with fixed domain-relevant templates/keywords, while keeping the original command;
% For TGTB, we keep the original composite "information+command" structure and instantiate the information component with domain-relevant templates; 
for PIDE, we keep its prompt injection template and use anchor queries from WikiQA.
% \footnote{We access and sample the anchor queries from \url{https://huggingface.co/datasets/microsoft/wiki_qa}.}
For CopyBreakRAG and IKEA, whose codes are unavailable, we reproduce the core mechanisms described in the papers.
To ensure a uniform evaluation interface, we apply the same post-processing step to all methods: a fixed GPT-4o-mini extractor with a universal extraction prompt (Appendix~\ref{universal_extraction_prompt}) converts raw outputs into structured entity--relation lists for metric computation; this step does not alter the victim interaction or provide additional access. %
\footnote{Detailed discussion for baselines in Appendix~\ref{app:baselines}.}
%\suhang{do we have bullets (short paragraph) for each method in the appendix? If yes, we should mention it here}


% \noindent\textbf{Baselines} We compare multiple RAG data extraction methods under identical black-box access and query budgets: two fixed-query prompt-injection attacks, TGTB~\cite{zeng2024good} and PIDE~\cite{qifollow}, which use pre-defined adversarial prompts; and two adaptive attacks, CopyBreakRAG~\cite{jiang2025feedback} and IKEA~\cite{wang2025silent}, which iteratively generate queries based on prior responses. For TGTB and PIDE, we follow their original strategies using domain-relevant templates and WikiQA anchor queries, respectively. Since code for the adaptive methods is unavailable, we faithfully reproduce their core mechanisms from the papers. All methods share a unified evaluation pipeline: their outputs are post-processed by a fixed GPT-4o-mini LLM using the universal extraction prompt (Appendix \ref{universal_extraction_prompt}) to measure structured knowledge leakage.


\noindent\textbf{Evaluation Metrics.}
Let $\mathcal{G}^*=(\mathcal{V}^*,\mathcal{E}^*)$ be the ground-truth graph (evaluation only) and
$\mathcal{G}^{(t)}=(\mathcal{V}^{(t)},\mathcal{E}^{(t)})$ the extracted graph at turn $t$.
We report \emph{leakage} (coverage) and \emph{precision} (correctness) for nodes/edges:
$L_X^{(t)}=\frac{|X^{(t)}\cap X^*|}{|X^*|}$ and
$P_X^{(t)}=\frac{|X^{(t)}\cap X^*|}{|X^{(t)}|}$ for $X\in\{\mathcal{V},\mathcal{E}\}$.
We also evaluate degree- and PageRank-weighted node leakage to quantify how much \emph{structural importance} in $\mathcal{G}^*$ is captured ({See details in} Appendix~\ref{app:metrics}).



% \noindent\textbf{Baselines} We compare multiple RAG data extraction methods under identical system access and query budgets, covering structured prompting, prompt-injection copying, and embedding-space exploration: (i) % We include both fixed-query prompt-injection baselines and adaptive query-generation baselines. 
% TGTB~\cite{zeng2024good} and (ii) Prompt-Injection Data Extraction (PIDE)~\cite{qifollow} represent fixed-query attacks that rely on pre-defined adversarial prompts to elicit context disclosure, without adapting to previous responses. Following their original settings, we fix domain-relevant query templates for targeted attacks in TGTB, and sample non-adaptive anchor queries from WikiQA\cite{yang2015wikiqa} for PIDE. (iii) CopyBreakRAG \cite{jiang2025feedback} and (iv) IKEA \cite{wang2025silent} represent adaptive extraction attacks that iteratively generate new queries based on feedback from previous query–response pairs. Unlike fixed-query methods, they progressively explore the retrieval space across turns to increase coverage of the underlying knowledge base. Since the codes for CopyBreakRAG and IKEA are not available, we reproduce the core design and attack mechanisms strictly following the descriptions in the paper.

% % For a fair comparison, we preserve the attack mechanisms of all baselines and follow their original query selection strategies, conservatively adapting them to our datasets. 
% To evaluate structured knowledge leakage, we apply a unified graph extraction step to the outputs of all methods using a fixed GPT-4o-mini LLM extractor, which is used solely for evaluation and is not exposed to any attack method during query generation. All methods operate in a black-box attack setting and interact with the same retrieval and generation interfaces. The graph extraction prompt is using the universal extraction command (refer to Appendix \ref{universal_extraction_prompt}).

% We compare multiple RAG data extraction methods under identical system access and query budgets, covering structured prompting, prompt-injection copying, and embedding-space exploration. Methods differ in how queries are generated and how responses are processed from the GraphRAG system, but all interact with the same retrieval and generation interfaces. Some methods rely on fixed or randomly sampled queries, while others generate queries adaptively across turns based on previously extracted information. All methods operate in a black-box attack setting.
% For all baselines, we preserve their core attack mechanisms, and use a graph-extractor LLM gpt-4o-mini to extract the entity and relation triples for graph leakage comparison.
% To ensure fair comparison, we follow the original query selection strategies proposed in prior RAG datastore extraction work and adapt them conservatively to our datasets. \\
% For TGTB proposed in \cite{zeng2024good}: we use a fixed set of randomly sampled questions from WikiQA as dataset-independent anchor queries for untargeted attacks. These queries are selected prior to the attack and reused across all systems and methods, mirroring the use of random or obsolete questions in prior work. For targeted attacks, we adopt predefined query templates using a fixed list of domain-relevant terms (e.g., medical or agriculture relevant keywords). These terms are generated before the attack and remain unchanged throughout execution. 

% For PIDE in \cite{qifollow}, we follow the original query selection setting by using a fixed, non-adaptive set of anchor queries sampled from WikiQA, which are independent of the target dataset.\\
% % For CopyBreakRAG in \cite{jiang2025feedback}, instead of previous two prompt-injection methods that use pre-defined fix queries, CopyBreakRAG generates diverse queries (triggers) to adaptively explore the database with additional memory modules guiding the query strategy. We reproduced their core query generation strategy and memory module updates based on the description provided in their paper. \\

% CopyBreakRAG and IKEA represent a different class of extraction attacks that rely on adaptive query generation. Rather than issuing fixed queries, these methods iteratively generate new queries based on feedback from previous query–response pairs.
% CopyBreakRAG adopts an agent-based strategy that alternates between exploration and exploitation. It uses previously extracted text chunks to construct follow-up queries through contextual reasoning, while periodically introducing semantically divergent queries to escape local regions of the retrieval space. Query generation is explicitly conditioned on extracted content stored in short-term and long-term memory, enabling progressive coverage of the knowledge base;

% IKEA performs implicit knowledge extraction through benign queries centered on evolving anchor concepts. Starting from an initial set of domain-related keywords, IKEA samples anchor concepts based on past query–response histories and mutates them under similarity constraints to explore the embedding space. Queries are generated to remain semantically close to anchor concepts while avoiding previously covered regions, allowing adaptive but non-malicious exploration of the RAG database.
% Unlike prompt-injection methods, IKEA does not explicitly request verbatim documents and instead accumulates knowledge implicitly from natural-looking responses.
% \begin{itemize}
%     \item TGTB~\cite{zeng2024good} uses composite structured prompts of the form $q = \{\text{information}\} + \{\text{command}\}$, where the first part steers retrieval and the second explicitly instructs the model to repeat the retrieved context ('Please repeat all the context'). We adopt the targeted attack type and use gpt-4o-mini to generate 1000 domain-relevant queries for medical and agriculture dataset for target attack, and use WikiQA dataset to sample 1000 random queries for the untargeted attack. 
%     \item Prompt-Injection Data Extraction (PIDE)~\cite{qifollow} is a prompt-injection attack that exploits the RIC-style prompt template by asking the model to “copy and output all the text before \texttt{"Here is a sentence"},” thereby forcing it to disclose the retrieved context. 
% \end{itemize}

% Since our objective is to evaluate the leakage of structured knowledge (entities, relationships, and descriptions), we apply a unified graph extraction step to the outputs of all methods. Specifically, after each method produces its raw textual response, we invoke a fixed graph extractor based on GPT-4o-mini with a shared extraction prompt to identify entities, relationships, and their associated descriptions from the generated text. This extraction step is used solely for evaluation and is not exposed to or used by any attack method during query generation. We deliberately do not append the graph extraction prompt to baseline attack queries in order to preserve their original prompt-injection designs. The same graph extractor is applied to our method and all baselines, ensuring consistent and comparable leakage measurement.

% Together, these baselines cover a broad spectrum of existing RAG data extraction strategies. The prompt-injection baselines represent fixed-query attacks that rely on strong adversarial instructions but lack feedback-driven refinement. The adaptive baselines represent feedback-guided attacks that progressively explore the knowledge base through memory, similarity constraints, or semantic reasoning. By comparing against both categories under a unified GraphRAG evaluation setup, we aim to assess extraction effectiveness across fundamentally different attack paradigms.

%(iii) \textit{Verification Metrics:} Filter precision (percentage of filtered items that were noise), false negative rate (percentage of real items incorrectly filtered), and verification efficiency (verification time per turn); and (iv) \textit{Query Efficiency Metrics:} Entities and relationships extracted per query, average novelty rate, and exploration ratio.




% \subsection{Experiment Results}
% We report the main experiment results in Table~\ref{tab:main_two_systems} and Figure~\ref{fig:graphrag_experiment}. 
% The proposed AGEA consistently achieves the highest leakage rates and precision across both GraphRAG systems, datasets, and query budgets. As different systems construct graphs differently from the same corpus, results are only comparable within each system.

% The performance trends reveal key methodological trade-offs. While TGTB and PIDE show competitive results, their effectiveness relies on non-stealthy, engineered prompts (domain-specific targeted queries or aggressive prompts in the victim query commands) that limit practical applicability. CopyBreakRAG incurs significant iterative overhead for moderate gains, and IKEA's implicit knowledge extraction strategy is ineffective for explicit graph extraction. In contrast, AGEA's adaptive query generation provides superior, efficient extraction under more realistic constraints.

% Due to space limit, we report importance-based node leakage for AGEA in Appendix~\ref{app:importance_results} to evaluate the degree- and PageRank-based importance entity leakage. 

\begin{figure*}[t!]
    \centering
    \includegraphics[width=\linewidth]{experiment_figures/graphrag_v2.png}
    \vskip -1em
    \caption{M-GraphRAG Cumulative KG Leakage over query turns. }
    \vskip -0.05in
    \label{fig:graphrag_experiment}
\end{figure*}

\subsection{Experiment Results}
We report the main results in Table~\ref{tab:main_two_systems} ($T{=}1000$ queries) and Figure~\ref{fig:graphrag_experiment} (leakage over turns). Across both datasets and GraphRAG systems, AGEA achieves the best final leakage and precision at $T{=}1000$ and maintains higher node/edge leakage than all baselines for nearly the entire trajectory, demonstrating strong \emph{query efficiency}.
Since LightRAG and M-GraphRAG construct different internal graphs from the same corpus, results are comparable only within each system.

The trajectories reveal key methodological trade-offs: prompt-injection baselines (TGTB, PIDE) can be competitive but rely on conspicuous, hand-engineered prompts that reduce stealth and portability; 
%(e.g., domain templates or aggressive extraction wording) 
CopyBreakRAG adds iterative overhead for modest gains, and IKEA, designed for implicit knowledge acquisition, transfers poorly to explicit graph extraction. 
In contrast, AGEA’s adaptive query generation plus verification/filtering yield more coherent subgraph leakage, improving precision and narrowing the node-edge extraction gap.

AGEA’s gains are largest on the Agriculture dataset, where baselines’ precision drops sharply. More broadly, edge recovery is the main bottleneck for prior attacks—many extract nodes but miss relations—while AGEA substantially reduces this gap. The strongest baseline varies by system (PIDE in LightRAG; TGTB in M-GraphRAG), indicating that different GraphRAG constructions expose different attack surfaces.
We report importance-weighted node leakage for AGEA in Appendix~\ref{app:importance_results}.



\subsection{Ablation Studies}
\label{sec:ablation}
We perform three ablation studies to validate AGEA’s design choices: (i) core components (query strategy, filtering, and LLM backbone), (ii) the universal extraction command, and (iii) scalability across graph sizes. Additionally, ablation experiments on hyperparameters can be found in Table ~\ref{tab:ablation_hyper_medical} (Appendix~\ref{app: hyper_ablation}).

For compact comparison, we summarize node/edge performance by $\bar{L}^{(t)}=\frac{L_N^{(t)}+L_E^{(t)}}{2}$ and $\bar{P}^{(t)}=\frac{P_N^{(t)}+P_E^{(t)}}{2}$. For each ablation variant (“var”) and the default AGEA setting (“def”), we compute $\bar{L}$ and $\bar{P}$ at the same evaluation turn/budget and report $\Delta\bar{L}=\bar{L}_{\text{var}}-\bar{L}_{\text{def}}$ and $\Delta\bar{P}=\bar{P}_{\text{var}}-\bar{P}_{\text{def}}$.
Negative $\Delta$ indicates worse performance than default AGEA.
% \suhang{explain $\bar{P}_{\text{var}}$ and $\bar{P}_{\text{def}}$.(added)}
% We perform three ablation studies to validate AGEA’s design choices: (i) core components (query strategy, filtering, and LLM backbone), (ii) the universal extraction command, and (iii) scalability across graph sizes.
% For compact comparison, we summarize node/edge performance by $\bar{L}^{(t)}=\frac{L_N^{(t)}+L_E^{(t)}}{2}$ and $\bar{P}^{(t)}=\frac{P_N^{(t)}+P_E^{(t)}}{2}$, and report $\Delta \bar{L}=\bar{L}_{\text{var}}-\bar{L}_{\text{def}}$ and $\Delta \bar{P}=\bar{P}_{\text{var}}-\bar{P}_{\text{def}}$. 
% For each ablation variant (“var”) and the default AGEA configuration (“def”), we compute $\bar{L}$ and $\bar{P}$ at the same evaluation turn/budget (i.e., using the final values under the assigned query budget), and report $\Delta \bar{L}=\bar{L}{\text{var}}-\bar{L}{\text{def}}$ and $\Delta \bar{P}=\bar{P}{\text{var}}-\bar{P}{\text{def}}$.
% Negative $\Delta$ indicates worse performance than the default AGEA setting.

% For compact comparison, we report $\Delta$ relative to the default AGEA configuration:
% $\Delta \bar{L} = \bar{L}_{\text{var}}-\bar{L}_{\text{def}}$ and $\Delta \bar{P} = \bar{P}_{\text{var}}-\bar{P}_{\text{def}}$,


\begin{table}[t]
\centering
\caption{Ablation Study on Medical Dataset (M-GraphRAG). Default AGEA uses an adaptive query strategy with graph Filter and DeepSeek backbone. }
\label{tab:master_ablation_medical}
\vskip -0.6em
\scriptsize
\setlength{\tabcolsep}{2.2pt}
\renewcommand{\arraystretch}{0.95}

\resizebox{\columnwidth}{!}{%
\begin{tabular}{@{}lcccccc@{}}
\toprule
\textbf{Variant} &
\textbf{L(N)} & \textbf{L(E)} & \textbf{P(N)} & \textbf{P(E)} &
$\boldsymbol{\Delta \bar{L}}$ & $\boldsymbol{\Delta \bar{P}}$ \\
\midrule
\textbf{AGEA (Proposed)} & \textbf{87.09} & \textbf{80.16} & \textbf{87.09} & \textbf{61.18} & -- & -- \\
\midrule
\multicolumn{7}{@{}l@{}}{\textbf{A: Query Strategy}} \\
Explore-only & 23.77 & 10.67 & 67.60 & 32.05 & -66.41 & -24.31\\
Exploit-only & 83.98 & 75.40 & 50.78 & 59.68 &  -3.94 & -18.91 \\
\multicolumn{7}{@{}l@{}}{\scriptsize\itshape Takeaway: Adaptive yields the best leakage--precision tradeoff.} \\
\midrule
\multicolumn{7}{@{}l@{}}{\textbf{B: Filtering Module}} \\
w/o Filtering & 88.53 & 80.80 & 70.58 & 59.83 & +1.04 & -8.93 \\
\multicolumn{7}{@{}l@{}}{\scriptsize\itshape Takeaway: Filtering boosts precision at a minor leakage cost.} \\
\midrule
\multicolumn{7}{@{}l@{}}{\textbf{C: LLM Backbone}} \\
Qwen         & 83.14 & 75.45 & 73.44 & 23.19 &  -4.33 & -25.82 \\
GPT-4o-mini  & 82.61 & 75.41 & 76.19 & 51.54 &  -4.62 & -10.27 \\
\multicolumn{7}{@{}l@{}}{\scriptsize\itshape Takeaway: DeepSeek backbone achieves the strongest overall results.} \\
\bottomrule
\end{tabular}%
}
\vskip -0.8em
\end{table}

\begin{table}[!ht]
\centering
\caption{Extraction command ablation on Medical (250 queries). $\Delta$ is relative to the proposed command.}
\label{tab:command_ablation}
\vskip -0.6em
\scriptsize
\setlength{\tabcolsep}{3.2pt}
\renewcommand{\arraystretch}{0.95}
\resizebox{\columnwidth}{!}{%
\begin{tabular}{@{}lcccccc@{}}
\toprule
\textbf{Cmd} & \textbf{L(N)} & \textbf{L(E)} & \textbf{P(N)} & \textbf{P(E)} & \textbf{$\Delta\bar{L}$} & \textbf{$\Delta\bar{P}$} \\
\midrule
\textbf{Proposed} & \textbf{62.34} & \textbf{46.62} & \textbf{92.87} & \textbf{69.21} & -- & -- \\
v1 & 44.27 & 26.82 & 77.63 & 54.25 & -18.94 & -15.10 \\
v2 & 48.29 & 34.32 & 93.12 & 81.24 & -13.17 & +6.14 \\
\bottomrule
\end{tabular}}
\vskip -0.8em
\end{table}

\begin{table*}[!ht]
\centering
\caption{Scalability of AGEA across graphs of increasing size under dataset-specific query budgets $T$.
}
\vskip -0.8em
\label{tab:scalability}
\footnotesize
\renewcommand{\arraystretch}{1.1}
\setlength{\tabcolsep}{4pt}
\resizebox{\textwidth}{!}{
\begin{tabular}{lrrrcccccccc}
\toprule
\multirow{2}{*}{\textbf{Dataset}}
& \multicolumn{1}{c}{\textbf{$|V|$}}
& \multicolumn{1}{c}{\textbf{$|E|$}}
& \multicolumn{1}{c}{\textbf{$T$}}
& \multicolumn{4}{c}{\textbf{M-GraphRAG}}
& \multicolumn{4}{c}{\textbf{LightRAG}} \\
\cmidrule(lr){5-8} \cmidrule(lr){9-12}
& & &
& \textbf{Leak(N)} & \textbf{Leak(E)} & \textbf{Prec(N)} & \textbf{Prec(E)}
& \textbf{Leak(N)} & \textbf{Leak(E)} & \textbf{Prec(N)} & \textbf{Prec(E)} \\
\midrule
Novel 9 (Fiction) & 466  & 603  & 400
& 80.90 & 78.11 & 80.50 & 62.30
& 94.44 & 92.64 & 98.78 & 95.17 \\

Novel 13 (Short Stories) & 697  & 895  & 500
& 75.54 & 74.97 & 88.85 & 66.97
& 96.81 & 96.70 & 99.02 & 96.32 \\

Medical & 1415 & 2334 & 1000
& {87.09} & 80.16 & 87.09 & 61.18
& {96.42} & {95.90} & 98.34 & 97.97 \\

Agriculture & 1705 & 1878 & 1000
& 84.67 & {84.13} & 93.08 & 76.81
& 88.05 & 87.11 & 98.11 & 96.65 \\

Novel (20 books) & 8259 & 9966 & 2000
& 60.71 & 52.56 & 71.40 & 57.33
& 71.36 & 68.70 & 98.23 & 97.44 \\
\bottomrule
\end{tabular}
}
%\vskip -0.2in
\end{table*}

\noindent\textbf{Core Component Ablations.}
Table~\ref{tab:master_ablation_medical} evaluates three key design choices. The results show distinct behaviors across nodes and edges, validating AGEA's architecture. (i) \textbf{Adaptive query strategy.}
    Explore-only fails to extract meaningful structure (low leakage), indicating generic queries rarely trigger entity-rich retrievals. Exploit-only achieves high leakage but suffers from severely degraded precision, as greedy local expansion amplifies errors. AGEA's adaptive selection balances both, confirming the necessity of interleaving exploration and exploitation for effective graph extraction; (ii) \textbf{Filtering module.}
    Removing the filter yields only marginal leakage gains but causes a substantial precision drop—particularly for nodes. This indicates that the unfiltered extraction includes plausible but hallucinated entities, which the filter successfully prunes while retaining most true leaks. The module thus acts as a critical precision-preserving stage; and (iii) \textbf{LLM backbone.}
    Backbone choice strongly influences relation faithfulness. Qwen produces unreliable relations even when entities are recovered, while GPT-4o-mini improves edge precision but remains below DeepSeek. DeepSeek achieves the strongest overall trade-off, suggesting its instruction-following and structured-output capabilities are best suited for our setup.

% \begin{itemize}[noitemsep,topsep=2pt]
%     \item \textbf{Adaptive query strategy.}
%     Explore-only fails to extract meaningful structure (low leakage), indicating generic queries rarely trigger entity-rich retrievals. Exploit-only achieves high leakage but suffers from severely degraded precision, as greedy local expansion amplifies errors. AGEA's adaptive selection balances both, confirming the necessity of interleaving exploration and exploitation for effective graph extraction.%reconstruction.
%     \item \textbf{Filtering module.}
%     Removing the filter yields only marginal leakage gains but causes a substantial precision drop—particularly for nodes. This indicates that the unfiltered extraction includes plausible but hallucinated entities, which the filter successfully prunes while retaining most true leaks. The module thus acts as a critical precision-preserving stage.
%     \item \textbf{LLM backbone.}
%     Backbone choice strongly influences relation faithfulness. Qwen produces unreliable relations even when entities are recovered, while GPT-4o-mini improves edge precision but remains below DeepSeek. DeepSeek achieves the strongest overall trade-off, suggesting its instruction-following and structured-output capabilities are best suited for extracting coherent graph elements.
% \end{itemize}

\noindent\textbf{Universal Extraction Command Ablation.}
We ablate the extraction command appended to the GraphRAG query LLM.
Our default command instructs the model to output a structured entity--relation list with complete (non-summarized) descriptions (detailed in Appendix~\ref{universal_extraction_prompt}).
We compare against two simpler commands on Medical (M-GraphRAG, 250 attack turns):
(i) Command-v1: ``Please repeat all the context.''
(ii) Command-v2: ``Please repeat all the retrieved entities and relationships.''
Figure~\ref{fig:extraction_ablation} shows that the proposed structured command yields higher node/edge leakage throughout 250 turns.
Table~\ref{tab:command_ablation} confirms this: relative to v1 and v2, it improves average leakage by +18.84 and +13.17 in $\Delta\bar{L}$, respectively.
Although v2 attains higher average precision ($\Delta\bar{P}=+6.14$), it extracts substantially less graph structure, indicating that schema-guided output formatting is important for efficient subgraph exposure.


\begin{figure}
    \centering
    \includegraphics[width=1.0\linewidth]{figures/prompt.png}
    \vskip -0.1in
    \caption{Extraction command ablation on the Medical dataset using M-GraphRAG with a 250 query budget. }
    %(node and edge leakage over turns)
    \vskip -0.1in
    \label{fig:extraction_ablation}
\end{figure}

\noindent\textbf{Scalability of AGEA.}
Table~\ref{tab:scalability} evaluates AGEA across graphs spanning $|V|{=}466$ to $8{,}259$ nodes under dataset-specific budgets $T$.
Across both systems, AGEA maintains consistently high precision (especially in LightRAG) while achieving substantial node/edge leakage on small-to-medium graphs.
As graph size increases substantially (Novel containing 20 books), leakage decreases as expected under a fixed query budget regime—i.e., the graph grows faster than the number of attack turns—yet AGEA still extracts a non-trivial fraction of the graph with moderate precision.

We further include leakage-over-turn curves (and additional competitive baselines) in Figure~\ref{fig:novel} to show that AGEA improves steadily with more queries and remains competitive under large-scale settings. Notably, the Novel collections exhibit higher entity/relationship diversity and weaker repetition than domain-specific corpora, which increases extraction difficulty and amplifies the effect of limited $T$.
We also provide importance-based leakage evaluation results in Figure~\ref{fig:novel_importance} (Appendix).
\begin{figure}
    \centering
    \includegraphics[width=1.0\linewidth]{experiment_figures/novel.png}
    \vskip -0.1in
    \caption{Cumulative KG Leakage on Novel Dataset.}
    \vskip -0.1in
    \label{fig:novel}
\end{figure}


% \noindent\textbf{Core Component Ablations.}
% Table~\ref{tab:master_ablation_medical} isolates three design choices in AGEA.

% \begin{itemize}[noitemsep,topsep=2pt]
%     \item \textbf{Adaptive query strategy.}
%     Explore-only fails to extract the graph ($\Delta\bar{L}=-66.41$), while exploit-only sacrifices precision ($\Delta\bar{P}=-18.91$), indicating that mode selection is essential for a strong leakage--precision trade-off.
    
%     \item \textbf{Filtering module.}
%     Removing filtering causes a large precision drop ($\Delta\bar{P}=-8.93$) with only a small leakage change ($\Delta\bar{L}=+1.04$), highlighting that the two-stage pipeline mainly suppresses noisy/hallucinated extractions rather than limiting discovery.
    
%     \item \textbf{LLM backbone.}
%     DeepSeek-V3.1 yields the best overall performance; GPT-4o-mini remains competitive but weaker ($\Delta\bar{P}=-10.27$), while Qwen-Turbo degrades substantially ($\Delta\bar{P}=-25.82$), especially on edges (P(E)).
% \end{itemize}


% In ablation studies, we define \textbf{$\Delta$ computation:} $\Delta \bar{L} = \tfrac{1}{2}\bigl[\text{L(N)}+\text{L(E)}\bigr]_\text{var} - \tfrac{1}{2}\bigl[\text{L(N)}+\text{L(E)}\bigr]_\text{def}$ (similarly for $\Delta \bar{P}$). Negative $\Delta$ indicates performance drop vs. default.
% \noindent\textbf{Adaptive Query Method.} Table~\ref{tab:master_ablation_medical}(A) compares AGEA’s adaptive policy against fixed strategies. Explore-only yields extremely low leakage ($\Delta\bar{L} = -66.41$), while exploit-only harms precision ($\Delta\bar{P} = -27.44$). AGEA’s adaptive selection achieves the best trade-off.

% \noindent\textbf{Filtering Impact.} Removing the graph filter (Table~\ref{tab:master_ablation_medical}B) causes a sharp precision drop ($\Delta\bar{P} = -12.02$) for only a slight leakage gain ($\Delta\bar{L} = +0.54$), validating the two-stage pipeline’s role in ensuring extraction quality.

% \noindent\textbf{LLM Backbone.} Table~\ref{tab:master_ablation_medical}(C) shows transferability across victim LLMs. DeepSeek-V3.1 yields the strongest overall trade-off. GPT-4o-mini is competitive but weaker ($\Delta\bar{P} = -10.26$). Qwen-Turbo struggles notably with edge precision ($\Delta\bar{P} = -25.82$), indicating higher noise in extractions.


% \noindent\textbf{Adaptive Query Method Ablation} In Table~\ref{tab:master_ablation_medical}(A), we evaluate the contribution of the adaptive exploration-exploitation policy by comparing AGEA (adaptive mode selection) against fixed strategies: explore-only and exploit-only modes. Results demonstrate the effectiveness of adaptive mode selection versus pure exploration or pure exploitation strategies.

% \noindent\textbf{Filtering Impact} To quantify the impact of the graph filter agent $F$ and validate our two-stage pipeline design, we compare the regex-parsed raw graph $\mathcal{G}_r^{(t)}$ against the filtered verified graph $\mathcal{G}_f^{(t)}$. This comparison reveals: (1) \textit{Precision Improvement} - the gain in extraction quality; and (2) \textit{Leakage Trade-off} - any potential cost in leakage rate if the filter incorrectly removes valid entities. As shown in Table~\ref{tab:master_ablation_medical} (B), the graph filter agent significantly improves precision while maintaining high leakage, demonstrating its importance in the two-stage extraction pipeline.

% \noindent\textbf{GraphRAG Query LLM Variants} 
% Table~\ref{tab:master_ablation_medical} (C) assesses the robustness of AGEA across different victim LLM backbones (DeepSeek V3.1, GPT-4.1-mini, and Qwen-turbo). 
% All other components (query generator, verifier, embedding model) remain fixed. This ablation study measures how extraction effectiveness varies with the victim model's capabilities and response characteristics, providing insights into attack transferability across different LLM architectures.
% The results show that DeepSeek-V3.1 achieves the most favorable leakage–precision trade-off, attaining the highest strict node/edge leakage while maintaining the strongest precision, particularly on edge extraction. GPT-4.1-mini yields a slightly weaker but still competitive trade-off. Qwen-Turbo produces substantially noisier edge extractions, resulting in markedly lower edge precision despite higher extraction volume.
% \begin{figure}
%     \centering
%     \includegraphics[width=1.0\linewidth]{/experiment_figures/llm_ablation.png}
%     \caption{LLM Backbone Ablation Study}
%     \label{fig:llm_precision}
% \end{figure}

% \begin{table}[t]
% \centering
% \caption{\textbf{Ablation Analysis on Medical dataset.} Evaluating (A) query strategy, (B) filtering module, and (C) LLM backbone. \textbf{Bold} indicates default AGEA.}
% \label{tab:master_ablation_medical}
% \vskip -1em
% \footnotesize
% \setlength{\tabcolsep}{2.5pt}
% \renewcommand{\arraystretch}{1.0}
% \resizebox{0.9\linewidth}{!}{ % Reduced width
% \begin{tabular}{@{}lcccc@{}}
% \toprule
% \textbf{Variant} & \textbf{L(N)} & \textbf{L(E)} & \textbf{P(N)} & \textbf{P(E)} \\
% \midrule
% \textbf{A: Query Strategy} \\
% Explore-only & 23.77 & 10.67 & 67.60 & 32.05 \\
% Exploit-only & 83.98 & 75.40 & 50.78 & 59.68 \\
% \textbf{Adaptive (AGEA)} & \textbf{87.09} & \textbf{80.16} & \textbf{87.09} & \textbf{61.18} \\
% \midrule
% \textbf{B: Filtering} \\
% w/o Filtering & 88.53 & 80.80 & 70.58 & 59.83 \\
% \textbf{w/ Filtering} & 87.09 & 80.16 & \textbf{87.09} & \textbf{61.18} \\
% \midrule
% \textbf{C: LLM Backbone} \\
% Qwen & 83.14 & 75.45 & 73.44 & 23.19 \\
% GPT-4o-mini & 82.61 & 75.41 & 76.19 & 51.54 \\
% \textbf{DeepSeek (AGEA)} & \textbf{87.09} & \textbf{80.16} & \textbf{87.09} & \textbf{61.18} \\
% \bottomrule
% \end{tabular}
% }
% \end{table}
% Preamble (recommended)
% \usepackage{booktabs}
% \usepackage{siunitx}
% \sisetup{detect-weight=true, detect-inline-weight=math, table-number-alignment=center}


% 

% long version: We next study how the choice of extraction command in the GraphRAG query LLM prompt affects graph leakage. In AGEA, the query LLM uses a universal extraction command that explicitly asks for all retrieved entities and relations and constrains the answer to a simple schema (entity name, description, and list of relations). This structured format enables our first-stage regex parser to reliably extract candidate nodes and edges (the full prompt is given in Appendix~\ref{universal_extraction_prompt}).

% We compare this command to two simpler alternatives on the Medical dataset with a fixed query budget of 250 attack turns on the M-GraphRAG system: (i) Command-v1: “Please repeat all the context.” and (ii) Command-v2: “Please repeat all the retrieved entities and relationships.” Figure~\ref{fig:extraction_ablation} plots node- and edge-level leakage rates over turns for all three prompts.

% As shown in the figure, the proposed universal extraction command consistently achieves higher node and edge leakage than both baselines across the entire attack. Command-v2, which at least mentions entities and relationships, eventually outperforms the generic “repeat all the context” Command-v1, but still lags behind the structured command. These results indicate that GraphRAG systems require explicit, schema-level extraction guidance to surface the retrieved subgraph effectively, and that enforcing a structured entity–relation format is crucial for maximizing leakage and enabling efficient regex-based extraction.




% \paragraph{Baseline 3: Dynamic Greedy Embedding Attack (DGEA).}
% DGEA~\cite{cohen2024unleashing} is an iterative attack combining (i) GCQ-based greedy query-suffix optimization in embedding space, (ii) dynamic target selection to explore new regions of the datastore, (iii) a jailbreak roleplay prompt to bypass safety filters, and (iv) embedding-space tracking of extracted content. For GraphRAG, we use Azure OpenAI’s \texttt{text-embedding-3-large} for GCQ optimization (with reduced iterations and candidate set size for API feasibility), send the optimized jailbreak queries to GraphRAG via its CLI, and convert the jailbroken responses into entity-centric graphs using a graph-extractor LLM; embeddings of extracted entity descriptions are then used to guide subsequent target selection.


