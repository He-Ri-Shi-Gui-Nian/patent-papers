\documentclass[11pt]{article}

% Change "review" to "final" to generate the final (sometimes called camera-ready) version.
% Change to "preprint" to generate a non-anonymous version with page numbers.
\usepackage[final]{acl}

% Standard package includes
\usepackage{times}
\usepackage{latexsym}
\usepackage{graphicx}
% For proper rendering and hyphenation of words containing Latin characters (including in bib files)
\usepackage[T1]{fontenc}
\usepackage{algorithm}
\usepackage{algorithmic}
\usepackage{booktabs}
\usepackage{multirow}
\usepackage{graphicx}
\usepackage{subcaption}

\usepackage{booktabs}
\usepackage{multirow}
\usepackage{graphicx}
\usepackage[dvipsnames]{xcolor} % Required for colors

% Custom command for showing gains (e.g., +3.2)
\newcommand{\gain}[1]{\fontsize{7}{8}\selectfont\color{ForestGreen}{(+#1)}}
\newcommand{\loss}[1]{\fontsize{7}{8}\selectfont\color{red}{(-#1)}}

% \usepackage{algorithmic}
\usepackage{pifont}% http://ctan.org/pkg/pifont
\newcommand{\cmark}{\ding{51}}%
\newcommand{\xmark}{\ding{55}}%
% For Vietnamese characters
% \usepackage[T5]{fontenc}
% See https://www.latex-project.org/help/documentation/encguide.pdf for other character sets

% This assumes your files are encoded as UTF8
\usepackage[utf8]{inputenc}

% This is not strictly necessary, and may be commented out,
% but it will improve the layout of the manuscript,
% and will typically save some space.
\usepackage{microtype}
\usepackage{amsmath}
\usepackage{wrapfig}
\usepackage{enumitem}
\setlist[itemize]{leftmargin=4mm}

% This is also not strictly necessary, and may be commented out.
% However, it will improve the aesthetics of text in
% the typewriter font.
\usepackage{inconsolata}

%Including images in your LaTeX document requires adding
%additional package(s)
\usepackage{graphicx}
% \usepackage{svg}
\usepackage{mdframed}
\usepackage{hyperref} 
\usepackage{tcolorbox}
\tcbuselibrary{breakable}
\usepackage{fvextra}

\usepackage{tcolorbox}
\tcbuselibrary{breakable}
\usepackage{fvextra}
\usepackage{makecell}

\tcbuselibrary{skins}
\tcbset{before upper=\refstepcounter{prompt}}
\newcounter{prompt}[subsection]
\renewcommand{\theprompt}{\thesubsection.\arabic{prompt}}

\usepackage[inkscapelatex=false]{svg}
% \newcommand{\suhang}[1]{{\color{blue} [SW: #1]}} % comments
% \newcommand{\Dongwon}[1]{{\color{orange} [DW: #1]}} % comments
\newcommand{\shuhua}[1]{{\color{red} [Shuhua: #1]}} 
\newcommand{\jiahao}[1]{{\color{purple} [Jiahao: #1]}} 
\newcommand{\suhang}[1]{{\color{blue} [SW: #1]}} 
\newcommand{\yilong}[1]{{\color{green} [Yilong: #1]}} 

\newcommand\scalemath[2]{\scalebox{#1}{\mbox{\ensuremath{\displaystyle #2}}}}

% 
% If the title and author information does not fit in the area allocated, uncomment the following
%
%\setlength\titlebox{<dim>}
%
% and set <dim> to something 5cm or larger.

% \title{Agentic Data Extraction Attacks on GraphRAG Systems}
\title{Query-Efficient Agentic Graph Extraction Attacks on GraphRAG Systems}


\author{
    Shuhua Yang,
    Jiahao Zhang,
    Yilong Wang,
    Dongwon Lee,
    Suhang Wang
    \\
    The Pennsylvania State University, USA\\
    \texttt{\{sky5341,jjz5668,yvw5769,dul13,szw494\}}@psu.edu
}

\begin{document}
\maketitle
\begin{abstract}
Graph-based retrieval-augmented generation (GraphRAG) systems construct knowledge graphs over document collections to support multi-hop reasoning. While prior work shows that GraphRAG responses may leak retrieved subgraphs, the feasibility of \emph{query-efficient} reconstruction of the hidden graph structure remains unexplored under realistic query budgets.
We study a budget-constrained black-box setting where an adversary adaptively queries the system to steal its latent entity–relation graph.
We propose \textbf{A}gentic \textbf{G}raph \textbf{E}xtraction \textbf{A}ttack (\textbf{AGEA}), a framework that leverages a novelty-guided exploration–exploitation strategy, external graph memory modules, and a two-stage graph extraction pipeline combining lightweight discovery with LLM-based filtering. We evaluate AGEA on medical, agriculture, and literary datasets across Microsoft-GraphRAG and LightRAG systems. Under identical query budgets, AGEA significantly outperforms prior attack baselines, recovering up to 90\% of entities and relationships while maintaining high precision. 
These results demonstrate that modern GraphRAG systems are highly vulnerable to structured, agentic extraction attacks, even under strict query limits. 
The code is available at https://github.com/shuashua0608/AGEA. 

% Graph-based retrieval-augmented generation (GraphRAG) systems construct latent knowledge graphs over document collections to support multi-hop reasoning. Prior work has shown that GraphRAG responses may leak retrieved subgraphs, but whether an adversary can efficiently reconstruct the underlying graph structure remains unclear.
% We study a budget-constrained, agentic graph extraction threat model in which the attacker seeks to recover the hidden knowledge graph $\mathcal{G} = (\mathcal{V}, \mathcal{E})$  of a GraphRAG system under a fixed number of queries. We propose AGEA, an Agentic Graph Extraction Attack framework that leverages novelty-guided exploration–exploitation, external graph memory, and a two-stage extraction pipeline combining lightweight discovery with LLM-based verification.
% We evaluate AGEA on medical, agriculture, and literary datasets across Microsoft-GraphRAG and LightRAG systems. Under identical query budgets, AGEA consistently outperforms prior attacks, achieving dramatically higher node and edge leakage, recovering up to over 90\% of entities and relationships, with strong precision. These results reveal that modern GraphRAG systems remain highly vulnerable to structured, agentic extraction attacks even under strict query limits.

% \textbf{Long-version}
% Graph-based retrieval-augmented generation (GraphRAG) systems build latent knowledge graphs over documents to support multi-hop reasoning. 
% Prior work~\cite{liu2025exposing} shows that GraphRAG can leak retrieved subgraphs in its responses. 
% In this paper, we study a stronger threat model: a query-efficient, agentic graph extraction attack that aims to recover the underlying knowledge graph $\mathcal{G} = (\mathcal{V}, \mathcal{E})$ of a GraphRAG system with as few adversarial queries as possible. To our knowledge, no prior work has investigated agentic query attacks tailored to GraphRAG systems. We propose \textbf{AGEA}, \textbf{A}gentic \textbf{G}raph \textbf{E}xtraction \textbf{A}ttack framework that coordinates three LLM agents: an adaptive query generator that balances exploration and exploitation using novelty signals, a GraphRAG query LLM acting as the victim system, and a graph extractor that filters entity–relation candidates to enhance extraction accuracy. \textbf{AGEA} combines a lightweight discovery stage with LLM-based verification and maintains external memories over the evolving graph and query history to support long-horizon planning.
% We evaluate AGEA using domain-specific datasets such including medical and agriculture, as well as three structurally diverse novels in Microsoft-GraphRAG and LightRAG system. AGEA consistently achieves higher leakage and precision with fixed budget queries than state-of-the-art baselines, revealing that modern GraphRAG pipelines are vulnerable to efficient graph-level extraction.


\end{abstract}

%\input{sections/0_proposal_by_jiahao}
% \input{sections/0_plan_weekly}
\input{sections/1_intro}
\input{sections/2_related}
\input{sections/2.5_preliminary}
\input{sections/3_method_v2}
% \input{sections/4_details}
\input{sections/5_experiment}
\input{sections/6_discussion}
% \newpage

% \input{sections/7_acl_limitation_ethics}

\bibliography{custom}
\appendix
\input{sections/appendix}

\end{document}
