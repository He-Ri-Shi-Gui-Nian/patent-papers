% \section{Related Work}
% \textbf{Architectures for Multi-Agent Memory.} 
% Early developments in Large Language Model (LLM) agents established the foundational paradigms for memory management, transitioning from simple context window management to sophisticated long-term storage \citep{park2023generative, packer2023memgpt}. In the context of Multi-Agent Systems (MAS), frameworks like MetaGPT and AutoGen utilized structured message pools to facilitate inter-agent coordination \citep{hong2023metagpt, wu2024autogen}. Recent state-of-the-art systems have introduced hierarchical and temporal dimensions to this storage; for instance, MIRIX partitions memory into functional tiers such as episodic and semantic containers \citep{wang2025mirix}, while G-Memory employs graph-based tracing to capture complex interaction trajectories \citep{zhang2025g}. Despite these structural advances, existing architectures usually function as passive repositories that focus on retrieval efficiency rather than the inherent veracity of the stored knowledge \citep{chhikara2025mem0, rasmussen2025zep}. Our work deviates from this passive paradigm by introducing an active, mutual-calibration layer that treats memory as a dynamic belief system.


% \textbf{Memory Conflicts and Security Risks.} 
% The decentralized nature of MAS memory often leads to internal knowledge conflicts, where heterogeneous agents append contradictory observations or hallucinated reasoning traces to the shared pool \citep{li2023camel, zhuge2023mindstorms}. Research has shown that LLMs struggle significantly when faced with such conflicting retrieved contexts, often leading to a failure to follow instructions or maintain logical consistency \citep{wang2023resolving, wang2025astute}. Beyond these inherent inconsistencies, the emergence of adversarial threats has further exposed the fragility of current systems. Attacks such as AgentPoison and MINJA demonstrate that malicious agents can inject "toxic" trajectories into the memory to manipulate the collective decision-making process \citep{chen2024agentpoison, dong2025memory}. While proactive defense frameworks like A-MemGuard have begun to address these vulnerabilities, they often rely on static filtering rather than dynamic semantic reconciliation \citep{wei2025memguard}. Our proposed framework addresses these gaps by explicitly modeling and resolving conflicts through a probabilistic auditing process.


% \textbf{Uncertainty-driven Calibration and Deliberation.} 
% The task of enhancing factuality through multi-agent interaction has gained traction, with studies demonstrating that iterative debating can effectively mitigate individual model biases and hallucinations \citep{du2024improving, zhuge2023mindstorms}. Currently, uncertainty quantification has emerged as a vital tool for assessing LLM reliability, enabling systems to identify low-confidence outputs through epistemic metrics \citep{xiong2023can, sumers2023cognitive}. However, most existing calibration methods are localized to single-turn responses and do not extend to the global, persistent state of a shared memory system. By integrating Bayesian belief revision into a multi-agent deliberation cycle, our work bridges the gap between transient uncertainty estimation and long-term memory integrity. Unlike prior RAG-based conflict resolution techniques \citep{wang2025astute}, our mutual-calibration approach leverages the collective intelligence of the agent society to assign and update fidelity scores, ensuring a self-correcting cognitive substrate for the MAS.


\section{Related Work}
\label{sec:related}

\textbf{Multi-agent debate systems.}
Multi-agent debate (MAD) improves factuality and reasoning in LLM systems through iterative deliberation among multiple agents~\citep{chan2023chateval,du2024improving,liang2024encouraging,wang2024rethinking,xiong2023examining}. Structural variants include role-conditioned debate~\citep{chan2023chateval,wang2024unleashing}, dynamic participant selection~\citep{chen2023agentverse,liu2023dynamic}, and tournament-style aggregation~\citep{khan2024debating,zhou2024zodiac}. Theoretical work studies the consensus properties of debate~\citep{smit2023should,zhang2023exploring} and its vulnerability to adversarial participants~\citep{amayuelas2024multiagent,li2024survey}. To enable cross-task learning, recent systems pair debate with a shared memory that persists across rounds~\citep{hong2023metagpt,qian2024scaling,zhang2025g}.

\textbf{Memory design for LLM agents.}
Agent memory has evolved from fixed context windows to structured persistent stores~\citep{packer2023memgpt,park2023generative,zhong2024memorybank}. Common designs include embedding-based retrieval~\citep{shinn2023reflexion,wang2023voyager,zhong2024memorybank}, graph-structured memory~\citep{chhikara2025mem0,rasmussen2025zep}, and hierarchical or multi-tier storage~\citep{packer2023memgpt,wang2025mirix,xu2025mem}. These systems are typically optimised for retrieval quality, with veracity and provenance treated as secondary~\citep{sumers2023cognitive,wu2025human,zhang2025survey}. Multi-agent variants extend the same designs to a shared store accessible to several agents at once~\citep{qian2024scaling,rezazadeh2025collaborative,zhang2025g}.

\textbf{LLM hallucinations and memory safety.}
LLMs frequently produce plausible-sounding but incorrect content~\citep{banerjee2025llms,huang2025survey,ji2023survey,xu2024hallucination}. Common mitigations include retrieval augmentation~\citep{gao2023retrieval,lewis2020retrieval}, self-consistency verification~\citep{manakul2023selfcheckgpt,sriramanan2024llm}, and uncertainty calibration~\citep{kadavath2022language,saha2025you,tian2023just,xiong2023can}. Beyond intrinsic errors, conflicts between retrieved memories further complicate downstream reasoning~\citep{wang2025astute,wang2023resolving}. A growing body of work targets memory specifically: AgentPoison injects backdoors into memory banks~\citep{chen2024agentpoison}, MINJA achieves memory corruption through query-only interaction~\citep{dong2025memory,dong2025practical}, and related studies expose vulnerabilities across multi-agent communication channels~\citep{amayuelas2024multiagent,lee2024prompt,zhang2024agent}. On the defence side, perplexity-based filters~\citep{alon2023detecting} and LLM-based auditors~\citep{xiang2024guardagent} score individual entries before commit, while A-MemGuard~\citep{wei2025memguard} validates entries through consensus over reasoning paths derived from related memories.