\section{Preliminaries}
\subsection{Computerized Adaptive Testing}
In intelligent education scenarios, CAT efficiently assesses examinee proficiency within a finite test length by dynamically adjusting question selection based on response behaviors through continuous dynamic interactions. Formally, let the set of examinees be $\mathcal{S} = \{s_1, s_2, s_3, \dots, s_{|\mathcal{S}|}\}$ and the question bank be $\mathcal{Q} = \{q_1, q_2, q_3, \dots, q_{|\mathcal{Q}|}\}$. An interaction between an examinee and a question can be represented as a triplet $(s, q, y)$, where $y \in \{0, 1\}$ denotes the response outcome of examinee $s$ to question $q$ (1 indicates a correct answer, 0 otherwise). In the CAT process, it is assumed that the examinee's true ability remains invariant during the test. A standard CAT process consist off two core components: the CDM and Selection Strategy. At the stet $t$ , the selection strategy chooses the next question $q^{(t)}$ from  the candidate question bank on the current ability estimate $\hat{\theta}^{(t-1)}$ and the set of available questions. Subsequently, the ability estimate is updated based on the examinee's response feedback. This interactive processes can formally defined as follow:
\begin{align}
    q^{(t)} = \mathcal{P}(\hat{\theta}^{(t-1)}, \mathcal{Q}_s), \quad \hat{\theta}^{(t)} = \mathcal{D}(\hat{\theta}^{(t-1)},q^{(t)},y^{(t)}),
\end{align}
where $\mathcal{Q}_s \subset \mathcal{Q}$ denotes the set of questions not yet answered by examinee $s$,   $\hat{\theta}^{(t)}$ denote the ability estimates at  step $t$, $\mathcal{P}(\cdot)$ is the selection strategy, and $\mathcal{D}(\cdot)$ is the CDM. The two modules operate alternately during the testing process until the maximum number of test step $T$ is reached, at which point the CAT assessment process terminates.


% \subsection{Item Response Theory And Fisher Information}


% Item Response Theory (IRT)\cite{cai2016item} serves as the core framework of modern psychometrics, aiming to establish a probabilistic link between an examinee's latent ability and their observable response behaviors. In this study, to simultaneously capture both question difficulty and discrimination characteristics, we adopt the 2-Parameter Logistic (2PL-IRT) model as the foundational assessment model. Let the examinee's latent ability be denoted by $\theta \in \mathbb{R}$. For the $j$-th question in the question bank, let $b_j$ denotes the difficulty parameter and $a_j$ denotes the discrimination parameter. The probability $P_j(\theta)$ of a student $i$ answering the question $j$ correctly $y_{ij}=1$ is defined as:
% \begin{align}
%     P_j(\theta) = P(y_{ij}=1|\theta, a_j, b_j) = \frac{1}{1 + e^{-a_j(\theta - b_j)}}.
% \end{align}



% Fisher Information\cite{chang1996global ,frieden1998physics} measures the amount of information that an observable random variable carries about an unknown parameter. In CAT, it is widely used to quantify the contribution of precision that a specific question offers toward estimating the examinee's ability \cite{lord2012applications}. For the aforementioned 2PL model, the Fisher information $I_j(\theta)$ provided by question $j$ at ability level $\theta$ can be derived from the second derivative of the log-likelihood function:
% \begin{align}
%     I_j(\theta) = \frac{[P'_j(\theta)]^2}{P_j(\theta)(1-P_j(\theta))} = a_j^2 \cdot P_j(\theta) \cdot (1 - P_j(\theta)).
% \end{align}

% This formula reveals two key properties that serve as the theoretical rationale for our subsequent adaptive strategy design: (1) Difficulty Matching Principle: When $P_j(\theta) \approx 0.5$ (i.e., $\theta \approx b_j$), the term $P_j(\theta)(1-P_j(\theta))$ reaches its maximum. This implies that an question provides maximum information when its difficulty is perfectly matched to the student's ability\cite{harte2014maximum}. (2) Discrimination Weighting: The information is proportional to the square of the $a_j$. Questions with higher discrimination provide sharper gradient information, thereby accelerating the convergence of ability estimation.




\input{acmart-primary/samples/Figure/agentcat}



% \subsection{LLM-based Agents}
% A LLM-driven agent is an intelligent system that utilizes LLMs as its core decision-making unit, enhancing its long-term memory, planning, and action capabilities through external structured modules, thereby enabling the completion of complex tasks in dynamic environments\cite{gao2024large,park2023generative}. An LLM-Agent typically consists of four modules, which collectively form a ``Perception-Cognition-Decision-Action'' closed-loop system, allowing the Agent to continuously evolve rather than respond statically during multi-turn interactions\cite{wang2025user}. Formally, the agent $\mathcal{A}$ can be defined as a quadruple:
% \begin{align}
%     \mathcal{A} = \langle \mathcal{P}, \mathcal{M}, \mathcal{L}, \mathcal{A}c \rangle.
% \end{align}
% $\mathcal{P}$ denotes the Profiling Module. The Profiling module defines the Agent's identity and behavioral preferences within the target domain; essentially, it imposes high-level prior constraints on LLM's decision \cite{wang2025multi}. $\mathcal{M}$ denotes the Memory Module, used to store environmental information acquired during interactions, historical behaviors, and intermediate reasoning results\cite{hatalis2023memory}. $\mathcal{L}$ denotes the Planning Module, responsible for decomposing complex goals into executable sequences of subtasks, endowing the agent with long-horizon and multi-step decision-making capabilities. $\mathcal{A}c$ denotes the Action Module, responsible for translating decisions from the planning module and context information from the memory module into concrete actions that can practically alter the environmental state or facilitate information exchange, serving as the final output layer and actuator of the Agent architecture.

\input{acmart-primary/samples/Alg/algorithm1}