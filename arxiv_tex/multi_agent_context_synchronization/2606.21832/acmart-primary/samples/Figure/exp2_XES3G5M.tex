% \begin{figure}[htbp] % 使用 htbp 让排版引擎自动寻找最佳位置
%     \centering
    
%     % 第一张图：难度匹配误差
%     % 将子图容器宽度设为 \linewidth，独占一行
%     \begin{subfigure}[b]{\linewidth}
%         \centering
%         % 内部图片宽度设为 0.85\linewidth 或 0.9\linewidth，避免图片太大撑满全页
%         \includegraphics[width=0.65\linewidth]{Figure/image/exp2_difficulty_gap_XES3G5M.png}
%         \caption{Difficulty Matching Error ($|b_t - \hat{\theta}^{(t-1)}|$)}
%         \label{fig:diff_gap_XES3G5M}
%     \end{subfigure}
    
%     \vspace{0.4cm} % 关键：在这里添加一点垂直间距，防止上下两张子图的标题和图片粘连
    
%     % 第二张图：费雪信息量
%     \begin{subfigure}[b]{\linewidth}
%         \centering
%         \includegraphics[width=0.65\linewidth]{Figure/image/exp2_fisher_info_XES3G5M.png}
%         \caption{Average Fisher Information ($I(\theta)$)}
%         \label{fig:fisher_info_XES3G5M}
%     \end{subfigure}
    
%     \caption{Micro-level interaction analysis on XES3G5M.}
%     \label{fig:exp2_analysis_XES3G5M}
% \end{figure}



\begin{figure}[htbp]
    \centering
    
    % 第一张图
    \subfloat[Difficulty Matching Error ($|b_t - \hat{\theta}^{(t-1)}|$)]{
        % 使用 width=0.48\linewidth 代替 scale，确保两张图加起来不超过 1，刚好并排
        \includegraphics[width=0.48\linewidth]{acmart-primary/samples/Figure/image/exp2_difficulty_gap_XES3G5M.png}
        \label{fig:diff_gap_XES3G5M}
    }% 这里不要留空行！
    \hfill % \hfill 会在两张图中间自动填充水平空白，把它们向两边推开
    % 第二张图
    \subfloat[Average Fisher Information ($I(\theta)$)]{
        \includegraphics[width=0.48\linewidth]{acmart-primary/samples/Figure/image/exp2_fisher_info_XES3G5M.png}
        \label{fig:fisher_info_XES3G5M}
    }
    
    % 总标题
    \caption{Micro-level interaction analysis on XES3G5M.}
    \label{fig:exp2_analysis_XES3G5M}
\end{figure}