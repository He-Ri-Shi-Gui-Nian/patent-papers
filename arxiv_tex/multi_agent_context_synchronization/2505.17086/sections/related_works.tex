\section{Related Work}
\paragraph{MHQA with textual corpus.}
Retrieval-based methods dominate the landscape of textual MHQA since supporting contexts from external corpora can effectively compensate for deficiencies in the inherent knowledge of language models and mitigate hallucinations. Traditional approaches primarily utilized sentence transformers~\cite{bgem3} for dense retrieval or BM25~\cite{BM25} for keyword matching. Recently, extensive research has focused on optimizing the QA pipeline, particularly through the decomposition of complex questions~\cite{IM-RAG,ReAct}, query rewriting and refinement~\cite{RQ-RAG,RaFe}, and the iterative retrieval of relevant evidences in multiple steps~\cite{SelfRAG,ReSP,Iter-RetRAG,IRCoT}. 
% Most notably, ReAct~\cite{ReAct} integrates retrieval actions into the reasoning trajectory exploration process, enabling dynamic interaction with external environments. IM-RAG~\cite{IM-RAG} optimizes multi-round retrieval and reasoning via reinforced learning. Search-o1~\cite{Search-o1} introduces an agentic RAG framework, empowering the language model to decide when to retrieve external knowledge and to refine retrieved contexts, 
% %. It also incorporates a decoupled knowledge refinement module to filter redundant information from retrieved contexts, 
% achieving state-of-the-art performance.
%Recently, extensive research has focused on improving retrieval methods, particularly through iterative approaches that refine search queries at each step.
% 传统的方案主要利用sentence transformers进行dense-retrieval或使用BM25进行关键词匹配，并使用encoder-decoder or decoder-only架构的LLM进行process retrieved information 并预测答案。
% 近期的方案在转向使用LLM的同时，也关注于improve retrieval的accuracy. 

%Traditional solutions often rely on language models with an encoder-decoder architecture~\cite{T5, BART} or a decoder-only architecture~\cite{GPT, LLaMA} to process retrieved information and the question in an end-to-end manner. Due to the emergent strong semantic understanding capabilities, LLMs become increasing prevalent.  
% 早期的solution主要利用language models的semantic understanding 能力，结合question context中提供的supporting information解决MHQA问题。These models, such as BART, T5, and earlier versions of LlaMA and GPT, usually follows an encoder-decoder or decoder-only structure. 
% With the emergence of LLMs, retrieval-based methods dominate the landscape of textual MHQA as they can effectively compensate the limitations in the inherent knowledge of language models and mitigate unexpected hallucinations. 
\paragraph{Knowledge-based question answering.}
% 早期的方案将KBQA formulate成一个semantic parsing task, which关注于提取自然语言问题中的实体和关系，并将其转化为可在structural KG 上执行logical query的SPARQL statements. 尽管这些方法生成的答案是准确并可解释的，然而，由于KG triple和natural language sentences之间存在semantic gap，通过语言模型生成的SPARQL语句会有大量的句法错误，例如错误的entity 和relation label，非预期的grammar mistakes等，且知识图谱本身并不完整，因而常常导致查询失败，无法在得出正确答案。 
% Early approaches~\cite{lan-jiang-2020-query,SPARQL2021,KD-CoT} formulates KBQA as a semantic parsing task, focusing on extracting entities and relations from natural language questions and translating them into SPARQL statements capable of executing logical queries over KGs~\cite{TIARA,SymKGQA}. While these methods provide accurate and interpretable answers, they encountered significant limitations since SPARQL statements generated by language models often suffer from syntactic errors, including incorrect entity and relation labels as well as unexpected grammatical mistakes~\cite{DecAF,RoG,DoG}. Moreover, the incomplete nature of KGs~\cite{KGR3} can also lead to query failures, preventing these approaches from outputting correct answers. 

% In recent years, 研究人员偏向于将KBQA当作一个Information Retrieval任务来处理，结合RAG，利用LLM强大的语义理解能力通过知识图谱中三元组提供的factual knowledge来生成最终的答案。LLM-based KBQA methods 可以被大致分为两类, iterative exploration and subgraph reasoning. 前者利用类beam search 的方案retrieve与问题最相关的relation和neighboring entity，并利用LLM的语义理解能力进行pruning操作。这类方法有较强的可追溯性，allowing responsible reasoning，但具有较高的computational complexity。此外，由于训练数据往往并不包含ground truth reasoning path及中间节点，不允许我们对SFT进行监督微调，它们的性能往往还受限于LLM自身的internal reasoning capabilities. 相反的，以RoG为首的sub-graph reasoning方案一次性地从topic entity的局部邻域中提取出所有与问题相关的推理路径，并利用LLM直接生成答案。尽管这一方案允许我们通过SFT的方式直接提升LLM的in-context learning能力，但其性能往往被retrieval的效果而局限：提取出的推理路径往往只关注到complex question中的一部分，而缺乏相关的知识。此外，考虑到问题的长度和图的完整性，relevant reasoning path未必存在于subgraph中。在这种情况下，LLM可能会hallucinate而无法提供正确的答案。
Early approaches treat KBQA as a semantic parsing task, which generates and executes SPARQL queries~\cite{lan-jiang-2020-query,SPARQL2021,KD-CoT}. These methods usually suffer from syntax errors and inaccurate entity labels, resulting in limited accuracy. Recently, researchers have shifted towards  leveraging RAG and the advanced semantic understanding capabilities of LLMs to generate answers~\cite{RRA}. LLM-based KBQA approaches can be generally categorized into two types: iterative exploration and subgraph reasoning. Iterative exploration methods~\cite{ToG,ToG2,ToG-I,KG-CoT,PoG,FiDeLiS,GoG,KARPA,Paths-over-Graph,BeamRetrieval,DoG} progressively retrieve and verify KG elements through multi-step reasoning, balancing interpretability with computational overhead. These approaches rely heavily on LLMs' inherent reasoning capabilities due to the lack of supervised intermediate steps in most datasets. In contrast, Subgraph reasoning methods~\cite{RoG,GNN-RAG,SubgraphRAG,READS,GCR} extract relevant KG substructures in a single retrieval step for LLM-based answering, enabling efficient fine-tuning but suffering from the incompleteness of retrieved subgraphs and underlying knowledge bases.
% Recently, researchers have shifted towards considering KBQA as an information retrieval~(IR) task, leveraging RAG and the advanced semantic understanding capabilities of LLMs to generate answers based on factual knowledge stored in KGs~\cite{RRA}. LLM-based KBQA methods can be generally categorized into two types: iterative exploration and subgraph reasoning. Iterative exploration methods~\cite{ToG,ToG2,ToG-I,KG-CoT,PoG,FiDeLiS,GoG,KARPA,Paths-over-Graph,BeamRetrieval,DoG} adopt a beam search-like strategy to retrieve the most relevant relations and neighboring entities, followed by pruning operations guided by LLMs. These methods exhibit strong traceability, enabling responsible and interpretable reasoning, but often come with the drawback of high computational complexity~\cite{FastToG}. Moreover, the absence of ground truth reasoning paths and intermediate nodes in most KBQA datasets limits the feasibility of applying SFT~\cite{RoG,GNN-RAG}. As a result, their performance is inevitably constrained by the inherent reasoning capabilities of LLMs. 
% In contrast, subgraph reasoning methods~\cite{RoG,GNN-RAG,SubgraphRAG,READS,GCR} retrieve all candidate reasoning paths related to the question from the local neighborhood of the topic entities and guide the LLM to generate answers based on these paths in a single step. This approach enable direct improvement of the LLM's in-context learning capabilities through SFT. Nevertheless, its effectiveness is often constrained by the quality of retrieved context, which typically cover only a subset of the knowledge required to address complex questions, omitting other critical information.~\cite{PoG} Furthermore, due to the incompleteness of KGs, the existence of relevant reasoning paths within the subgraph is not guaranteed. In such scenarios, LLM may hallucinate and produce incorrect answers. 


% \subsection{LLM post-training methods for Multi-hop Question Answering}
% 这个名字暂且我这么摆着，因为我们整个没提RAG
