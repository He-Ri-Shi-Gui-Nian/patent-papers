


% \subsection{Tool Use Benchmarks and Limitations}
% \label{sec:tools-benchmark}

% Although tool use is a critical capability of modern LLM-based agents, we intentionally exclude tool-use experiments from our quantitative evaluation. This decision is methodological rather than practical. Across all examined frameworks, tool invocation is not implemented as a framework-level orchestration mechanism, but is instead delegated almost entirely to the underlying LLM through native function-calling interfaces. Consequently, tool-use performance is dominated by the choice of language model rather than by framework design: changing the LLM (e.g., model family, version, or prompting behavior) would substantially alter results even under an identical framework configuration. Executing tool-use benchmarks under these conditions would therefore conflate model capability with framework behavior and would not yield a meaningful or stable comparison of framework-level architectures.

% Current tool-enabled frameworks expose tools to agents by attaching them directly to the LLM prompt, after which tool discovery, selection, and invocation are handled implicitly by the model based on tool names, descriptions, and argument schemas. This design places tool use almost entirely under model control rather than framework orchestration, introducing fundamental limitations for controlled framework-level evaluation. First, LLM providers impose hard limits on the number of tools that can be exposed per request, such as the 128-tool constraint in OpenAI models, which makes tool-heavy tasks impractical without an explicit pre-filtering or selection stage. None of the frameworks examined provide native mechanisms for large-scale tool discovery, budget-aware filtering, or hierarchical tool routing, instead relying on the LLM to select among all exposed tools. Second, tool attachment is entirely developer-driven: frameworks do not offer shared or reusable tool repositories, standardized registries, or declarative selection mechanisms, tightly coupling tool availability and organization to application-specific code and limiting reproducibility and cross-framework comparability. Third, frameworks do not support dynamic tool generation or adaptation, requiring developers to manually implement and bind tools in advance even when tasks would benefit from synthesizing task-specific interfaces. Together, these limitations imply that tool-use performance would be dominated by the choice of LLM rather than by framework architecture, and that executing tool-use benchmarks under these conditions would conflate model capability with framework behavior.

% Despite excluding tool-use experiments from our evaluation, we retain tool use as a first-class component of MASBench to support future framework-level studies. We prepare a standardized tool-use benchmark based on StableToolBench~\cite{guo2024stabletoolbench}, a deterministic reformulation of ToolBench~\cite{qin2025tool,qintoolllm}, together with a unified experimental configuration that isolates tool-interface constraints from agent orchestration logic. In particular, MASBench introduces a centralized tool-selection layer that bounds tool visibility to LLM-supported limits prior to framework binding, enabling tool-heavy tasks to be instantiated without modifying framework internals. This design ensures that, once agent frameworks expose explicit abstractions for tool routing, filtering, or execution control, tool-use benchmarks such as StableToolBench can be evaluated in a controlled and reproducible manner without conflating framework behavior with model-level interface constraints.


% \input{figures/tools/tools_analysis}

% \paragraph{Tool Environment Analysis.}
% Figure~\ref{fig:stabletoolbench-overview} summarizes the structural properties of the curated StableToolBench environment used in MASBench. The tool registry exhibits substantial heterogeneity in interface complexity, with a heavy-tailed distribution over API endpoints: the majority of tools expose a single endpoint, while a small but non-negligible fraction provide rich multi-endpoint interfaces. This imbalance implies that tool-use tasks vary significantly in difficulty, ranging from straightforward endpoint selection to multi-step reasoning over closely related APIs with overlapping semantics. In addition, solvable queries are unevenly distributed across tool categories, with domains such as utilities, movies, finance, and data processing appearing most frequently, reflecting realistic workloads that require entity resolution, constraint satisfaction, and structured parameter grounding. Together, these characteristics highlight why large-scale tool environments stress not only tool-calling accuracy but also tool discovery, selection, and budgeting mechanisms, and motivate the need for benchmark infrastructure that can decouple tool-interface constraints from framework-level orchestration when such abstractions become available.




\vspace{-2mm}
\subsection{Tool Use Benchmarks and Limitations}
\label{sec:tools-benchmark}

Tool use enables LLM-based agents to interact with external APIs and services beyond language generation. Recent benchmarks, such as ToolBench and its deterministic variant StableToolBench~\cite{guo2024stabletoolbench,qin2025tool,qintoolllm} evaluate an agent’s ability to select and invoke appropriate tools from large, heterogeneous registries. These benchmarks provide structured environments with diverse tools and realistic tasks, making them suitable for analyzing tool-use behavior.


\noindent\textbf{Benchmark Structure.}
Figure~\ref{fig:stabletoolbench-overview} illustrates how StableToolBench evaluates tool use across registries spanning multiple domains and interface complexities, including both single-endpoint tools and multi-endpoint APIs~\cite{guo2024stabletoolbench}. The benchmark stresses correct tool selection, parameter grounding, and multi-step interaction with closely related endpoints. In MAFBench, StableToolBench serves as the reference environment for characterizing tool-use requirements.

\noindent\textbf{Evaluation Setup.}
MAFBench integrates StableToolBench through a unified tool-use evaluation layer that preserves the original queries, server, and scoring logic while standardizing execution. Benchmark runs are centralized through a shared runner with consistent logging and result aggregation. To ensure fair comparison, we introduce a pre-execution tool-selection step that selects a bounded subset of relevant tools per query before framework binding, ensuring identical tool sets across runs. We also enforce explicit tool budget limits aligned with model constraints and record correctness, runtime, and token usage using a shared result schema.
%
We do not report quantitative tool-use results because current frameworks delegate tool invocation entirely to the LLM via native function-calling interfaces, without architectural control over tool selection or execution. As a result, behavior is driven primarily by model choice and provider-imposed limits on exposed tools (e.g., 128 in OpenAI APIs), rather than by framework design. Tool-use evaluation is therefore deferred until frameworks introduce explicit orchestration mechanisms.

