



% \vspace{-2mm}
% \subsection{Environment}

% In this architectural taxonomy, the \emph{environment} refers to an explicit persistent shared representation of world state that evolves in response to agent actions. Graph-based and role-based frameworks do not treat the environment as a first-class component; execution is driven by workflows, tasks, and agent-local or developer-managed state, with external interactions handled via tools whose effects are not reflected in a shared world model. In contrast, GABM frameworks maintain a centralized environment which tracks global state, resolves actions, and redistributes observations over time. This design tightly and explicitly couples agent behavior to evolving world dynamics, enabling long-horizon interaction and emergent collective behavior absent from workflow-based or task-centric frameworks.



\vspace{-1mm}
\subsubsection{Environment.}
This architectural dimension determines if system execution is mediated through an explicit shared world state or through agent-local and developer-managed state. Graph-based and role-based frameworks do not maintain a first-class environment, relying instead on workflows and task context effects that are not reflected in a persistent global state. In contrast, GABM frameworks centralize execution around an explicit environment that tracks world state and applies agent actions over time~\cite{vezhnevets2023generative}. 