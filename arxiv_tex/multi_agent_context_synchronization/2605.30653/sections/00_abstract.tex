\begin{abstract}
Multi-agent LLM systems often treat agreement as evidence: when many agents in a panel give the same answer, that answer is assumed to be more reliable. 
We show that this assumption can fail after agents communicate. 
Communication can induce correlated failures and false consensus, so the same vote share may reflect reliable agreement in one topology but over-confidence in another.
We propose \methodname{}, 
a counterfactual agent-graph calibration framework for multi-agent LLMs. 
For each query, \methodname{} compares an observed post-communication agent graph with a matched counterfactual no-communication graph, capturing both pairwise failure correlations and group-level dependencies.
Rather than simply counting how many agents agree, \methodname{} estimates the counterfactual shift between observed and no-communication dependence, and calibrates confidence accordingly.
Across five benchmarks, \methodname{} improves reliability discrimination with competitive ECE, and its calibrated confidence further improves topology selection over the best fixed-topology strategy. Our code repo is available \href{https://anonymous.4open.science/r/Counterfactual-Graph-Calibration-for-Multi-Agent-LLMs-FDB0}{here}.
\end{abstract}



% \begin{abstract}
% Multi-agent LLM systems often treat agreement as evidence: when many agents in a panel give the same answer, that answer is assumed to be more reliable. 
% We show that this assumption can fail after agents communicate. 
% Communication can induce correlated failures and false consensus, so the same vote share may reflect reliable agreement in one topology but over-confidence in another.
% We propose \methodname{}, 
% %(Counterfactual Agent-Graph
% %Calibration), 
% a counterfactual agent-graph calibration framework for multi-agent LLMs. 
% For each query, \methodname{} compares an observed post-communication agent graph with a matched counterfactual no-communication graph, capturing both pairwise failure correlations and group-level dependencies.
% % For each query, \methodname{} compares the observed agent graph after communication with a matched counterfactual no-communication graph constructed from the same agents. Through these graphs, \methodname{} models pairwise failure correlations and group-level dependencies among agents 
% % that share a model family, prompting role, answer cluster, or communication topology.
% %It models pairwise failure correlations and group-level dependencies among agents that share a model family, prompting role, answer cluster, or communication topology.  
% %It captures how communication shapes dependence, rather than simply counting how many agents agree.
% Rather than simply counting how many agents agree, \methodname{} estimates counterfactual shift between observed and no-communication dependence, and calibrates confidence accordingly.
% % This reframes multi-agent calibration as estimating communication-induced dependence, rather than reducing disagreement to a single score.
% Across five reasoning and factuality benchmarks, \methodname{} achieves competitive expected calibration error (ECE) while substantially improving reliability discrimination. 
% Its calibrated confidence also supports topology selection, improving final panel accuracy over the best fixed-topology strategy. 


% % These results show that reliable multi-agent inference requires modeling how communication shapes dependence, not only counting how many agents agree.








% % Multi-agent LLM systems often treat agreement as evidence. When many agents in a panel give the same answer, that answer is assumed to be more reliable. We show that this assumption can fail after agents communicate. Communication can induce correlated failures and false consensus, so the same vote share may reflect reliable agreement in one topology but over-confidence in another.
% % We propose \textsc{CAGE-Cal}, a counterfactual calibration framework for multi-agent LLMs. For each query, CAGE-CAL compares the observed agent graph after communication with an iid counterfactual graph built from the same matched agents. It models pairwise failure correlations and group dependencies among agents that share a model family, prompting role, answer cluster, or communication topology. This reframes multi-agent calibration as estimating communication-induced dependence, rather than reducing disagreement to a single score.
% % Across five reasoning and factuality benchmarks, CAGE-CAL matches strong calibration baselines in ECE while substantially improving reliability discrimination. Its calibrated confidence also supports topology selection: CAGE-Select improves final panel accuracy over the best fixed-topology strategy. These results show that reliable multi-agent inference requires modeling how communication shapes dependence, not only counting how many agents agree.
% \end{abstract}