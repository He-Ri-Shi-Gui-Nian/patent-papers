%%%%%%%% ICML 2024 EXAMPLE LATEX SUBMISSION FILE %%%%%%%%%%%%%%%%%

\documentclass{article}  %% remove [accepted] for submission

% Recommended, but optional, packages for figures and better typesetting:
\usepackage{microtype}
\usepackage{graphicx}
\usepackage{subcaption}
\usepackage{lscape}
\usepackage{booktabs} % for professional tables
\usepackage[table]{xcolor}


% hyperref makes hyperlinks in the resulting PDF.
% If your build breaks (sometimes temporarily if a hyperlink spans a page)
% please comment out the following usepackage line and replace
% \usepackage{icml2024} with \usepackage[nohyperref]{icml2024} above.
\usepackage{hyperref}

% Attempt to make hyperref and algorithmic work together better:
\newcommand{\theHalgorithm}{\arabic{algorithm}}

% Use the following line for the initial blind version submitted for review:
% \usepackage{icml2024}

% If accepted, instead use the following line for the camera-ready submission:
\usepackage[accepted]{icml2024}

% For theorems and such
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{mathtools}
\usepackage{amsthm}
\usepackage{adjustbox}
% \usepackage{geometry}
\usepackage{changepage} % for the adjustwidth environment

% if you use cleveref..
\usepackage[capitalize,noabbrev]{cleveref}

% Todonotes is useful during development; simply uncomment the next line
%    and comment out the line below the next line to turn off comments
%\usepackage[disable,textsize=tiny]{todonotes}
\usepackage[textsize=tiny]{todonotes}

\usepackage{enumitem} % Custom list indenting.
\definecolor{bluecite}{HTML}{0875b7}
\definecolor{Gray}{gray}{0.96}
\usepackage{longtable}
\usepackage{tabularx}
%*****************************************************
%% Editing commands: visible
% %%*****************************************************
\usepackage{todonotes}
\usepackage[normalem]{ulem}

\newcommand{\hl}[1]{{\color{blue}#1}}
\newcommand{\hlc}[2]{{\color{#1}#2}}

\newcommand{\del}[1]{\hlc{red}{#1}}
\newcommand{\ins}[1]{\hlc{blue}{#1}}
\newcommand{\rep}[2]{\del{#1}\ins{#2}}

%*****************************************************
%% Author remarks
% %%*****************************************************
\newcommand{\ngr}[1]{{\color{blue}[Nathan: #1]}}
\newcommand{\adr}[1]{{\color{green}[Dries: #1]}}
\newcommand{\pdu}[1]{{\color{red}[Paul: #1]}}

% The \icmltitle you define below is probably too long as a header.
% Therefore, a short form for the running title is supplied here:
% \icmltitlerunning{Are we going MAD? Benchmarking Multi-Agent Debate between Language Models}
\icmltitlerunning{Should we be going MAD? A Look at Multi-Agent Debate Strategies for LLMs}

\begin{document}

\twocolumn[
% \icmltitle{Are we going MAD? Benchmarking Multi-Agent Debate \\ between Language Models}
\icmltitle{Should we be going MAD? \\ A Look at Multi-Agent Debate Strategies for LLMs}

% It is OKAY to include author information, even for blind
% submissions: the style file will automatically remove it for you
% unless you've provided the [accepted] option to the icml2024
% package.

% List of affiliations: The first argument should be a (short)
% identifier you will use later to specify author affiliations
% Academic affiliations should list Department, University, City, Region, Country
% Industry affiliations should list Company, City, Region, Country

% You can specify symbols, otherwise they are numbered in order.
% Ideally, you should not use this facility. Affiliations will be numbered
% in order of appearance and this is the preferred way.
\icmlsetsymbol{equal}{*}

\begin{icmlauthorlist}
\icmlauthor{Andries Smit}{equal,yyy}
\icmlauthor{Nathan Grinsztajn}{equal,yyy} 
\icmlauthor{Paul Duckworth}{equal,yyy}
% \icmlauthor{Kale-ab Tessera}{xxx}
\icmlauthor{Thomas D.\ Barrett}{yyy}
\icmlauthor{Arnu Pretorius}{yyy}
\end{icmlauthorlist}

\icmlaffiliation{yyy}{InstaDeep, London UK}
% \icmlaffiliation{xxx}{University of Edinburgh, Edinburgh, UK}
\icmlcorrespondingauthor{Paul Duckworth}{p.duckworth@instadeep.com}

% You may provide any keywords that you
% find helpful for describing your paper; these are used to populate
% the "keywords" metadata in the PDF but will not be shown in the document
\icmlkeywords{Machine Learning, ICML}

\vskip 0.3in
]

% this must go after the closing bracket ] following \twocolumn[ ...

% This command actually creates the footnote in the first column
% listing the affiliations and the copyright notice.
% The command takes one argument, which is text to display at the start of the footnote.
% The \icmlEqualContribution command is standard text for equal contribution.
% Remove it (just {}) if you do not need this facility.

% \printAffiliationsAndNotice{}  % leave blank if no need to mention equal contribution
\printAffiliationsAndNotice{\icmlEqualContribution} % otherwise use the standard text.


\begin{abstract}

% Fluff
Recent advancements in large language models (LLMs) underscore their potential for responding to inquiries in various domains. However, ensuring that generative agents provide accurate and reliable answers remains an ongoing challenge. In this context, multi-agent debate (MAD) has emerged as a promising strategy for enhancing the truthfulness of LLMs.
%
% Part 1: empirical testing of current methods
% In this work, we provide a comprehensive benchmark of MAD strategies for Q\&A, along with open-source implementations. We put a special emphasis on medical benchmarks, as it is an area where truthfulness is particularly critical. 
We benchmark a range of debating and prompting strategies to explore the trade-offs between cost, time, and accuracy. Importantly, we find that multi-agent debating systems, in their current form, do not reliably outperform other proposed prompting strategies, such as self-consistency and ensembling using multiple reasoning paths. However, when performing hyperparameter tuning, several MAD systems, such as Multi-Persona, perform better. This suggests that MAD protocols might not be inherently worse than other approaches, but that they are more sensitive to different hyperparameter settings and difficult to optimize.
%
% Part 3: extend MAD strategy to provide an improvement
We build on these results to offer insights into improving debating strategies, such as adjusting agent agreement levels, which can significantly enhance performance and even surpass all other non-debate protocols we evaluated.  We provide an open-source repository to the community with several state-of-the-art protocols together with evaluation scripts to benchmark across popular research datasets.
\end{abstract}


\section{Introduction} \label{sec:introduction}

Truthful question-answering assistants require a plethora of skills that until recently were considered out-of-reach of generative language models. Such agents require advanced natural language reading comprehension, along with accurate recall and manipulation of expert or technical knowledge. Following the increase in performance and popularity of large language models (LLMs) \citep{brown2020language, openAI2023gpt4}, there is a growing hope that these models could assist humans in various domains like medicine \citep{lievin2022can, han2023medalpaca, nori2023medprompt, wang2023clinicalgpt, singhal2023medpalm}, education \citep{pardos2023learning, kumar2024learning}, and law \citep{lai2023law}.
% Practical medical question-answering assistants require a plethora of skills that until recently were considered out-of-reach of generative language models. Such agents require advanced natural language reading comprehension, along with accurate recall and manipulation of expert medical knowledge. 
% Motivate multiple agents vs one agent. 
% Following the increase in performance and popularity of large language models (LLMs), 
%\ins{With the rise and increasing performances of large language models (LLM) \citep{brown2020language, openAI2023gpt4}, }
As a way to improve the reasoning abilities of these systems, prompting strategies have received significant attention, starting with few-shot~\citep{brown2020language}, and chain-of-thought (CoT)~\citep{wei2022chain, kojima2022large}. 
To further improve performance, a wide variety of strategies have been proposed to use interactive reasoning between multiple LLMs, by either generating answers in parallel to maintain a form of self-consistency~\citep{wang2022self}, or promoting models to simulate debate. 
These multi-agent approaches have recently seen an uptake in applications, e.g. language generation~\citep{chan2023chateval}, 
machine translation and arithmetic reasoning~\citep{liang2023encouraging}, mathematical and strategic reasoning~\citep{du2023improving},
negotiation and bargaining~\citep{fu2023improving},
and notably, medical Q\&A~\citep{anil2023palm, singhal2023medpalm, nori2023medprompt}.
%
% Motivate the comparisons
How to best utilize multiple agents for effective interactive reasoning is a prescient research question. However, to the best of our knowledge, there is no work comparing strategies, and there is no consensus for selecting one strategy over another. 

In this paper, we benchmark various prompting strategies, which include multi-agent debate (MAD), for answering multiple-choice questions across a wide range of domains. We explore the impact on, and trade-offs between, critical factors such as factual accuracy, time and cost.  
We provide an open-source suite of single-agent and MAD implementations for the research community to build upon, with a unified API to easily build and evaluate MAD systems. Finally, we demonstrate that by utilizing specific prompting strategies, LLMs exhibit improved reasoning abilities. Concretely, we provide a novel debate prompting strategy able to modulate the level of agreement between agents during a debate and improve upon the state-of-the-art for Q\&A for a given model class. 

%% Related works:
% \paragraph{Related Works} \label{sec:related_works}
% %
% %1. Debating setup
% %2. MAD benchmarks
% %3. 
% %
% %In practice, any base agents can be used in a debate. We leave additional understanding of the diversity of debating agents for future work. 


\section{Multi-Agent Debate} \label{sec:mad-strategies}

Current state-of-the-art models for Q\&A are dominated by generative LLMs. To make them more truthful and reliable, they are often fine-tuned for specific use cases. In medicine, for example, such models include 
Med-PaLM~\citep{singhal2022large}, 
Med-PaLM2~\citep{singhal2023medpalm}, 
MedAlpaca~\citep{han2023medalpaca}, 
Galactica~\citep{taylor2022galactica},
ClinicalGPT~\citep{wang2023clinicalgpt} and Medprompt ~\citep{nori2023medprompt}. 
Furthermore, many single-agent prompting strategies have been investigated in the context of medical Q\&A. 
For example, \citet{lievin2022can} %(MedQA, MedMCQA, and PubMedQA) 
applied CoT reasoning on top of Instruct GPT-3~\citep{ouyang2022training}, and achieves noticeable performance improvements.

% Related to this work,~\citet{fu2023improving} introduce a multi-agent negotiation strategy where two LLMs negotiate a target price, whilst a third, provides feedback. 
Recently, several MAD strategies have been proposed to improve upon the enhanced reasoning capabilities of single-agent prompting methods %such as CoT and self-consistency, 
leading to improved performance on challenging natural language tasks~\citep{du2023improving, liang2023encouraging, chan2023chateval}. 
Likewise, Generative agents~\citep{park2023generative}, multi-persona~\citep{wang2023unleashing}, and CAMEL~\citep{li2023camel} study the behaviour of agents taking on different roles or personas within multi-agent interactions. 
One major reason why debate strategies can be an effective tool is the ability of LLMs to adapt to additional information given in-context~\citep{zhang2023and}. 
This facilitates multiple LLMs to participate in multi-agent and/or multi-round debates entirely using in-context prompting. That is, the agents adapt their behaviour based on information provided by other agents at inference time, with no gradient-based parameter updates being required. 

In our study, we investigate several prompting strategies for Q\&A. Whilst we note that some of these strategies were introduced specifically for the medical Q\&A domain, each provides novel perspectives on how to utilize multiple collaborative agents. 
We briefly introduce each strategy here. 

\textbf{Society of Minds (SoM)} ~\citet{du2023improving} propose a MAD approach where multiple agents each provide their answers to each other in order to effectively collaborate. Optionally, answers are summarized before being added to the history that is available to the agents in future rounds.

% Tsinghua Shenzhen}/Shanghai Jiao Tong/Tencent multi-agent debate
\textbf{Multi-Persona} ~\cite{liang2023encouraging} propose a MAD strategy to encourage divergent agent outcomes via prompting different personalities, i.e.\ an affirmative agent (angel) and a negative agent (devil) each provide an answer to a judge agent who manages the process and obtains a final solution. The judge has additional agency to end the debate early if it is satisfied with the answers provided. 

\textbf{ChatEval} ~\cite{chan2023chateval} propose three MAD modes: 
(1) \emph{one-on-one}, where each agent answers the provided question in turn, and each agent is provided with the history of all previous agents' answers; 
(2) \emph{simultaneous-talk}, where agents asynchronously generate responses in each round to nullify the effects of agent order; and
(3) \emph{simultaneous-talk-with-summarizer}, which additionally summarizes answers provided in each round and overwrites the history available to all agents in future rounds. 

\textbf{Self-consistency}~\cite{wang2022self} samples multiple reasoning paths given a fixed prompt and selects the most frequent answer. 
Whilst this is not a debate per se, as samples are rolled out independently, it relies on multiple API calls so we distinguish it from the single agent case that uses a single API call.

\textbf{Ensemble Refinement (ER)} ~\cite{singhal2023medpalm} extends self-consistency. After multiple reasoning paths are sampled, a second stage concatenates them into a list of \textit{student reasonings}, after which multiple rounds of aggregation are performed conditioned on this list. 

\textbf{Medprompt} ~\citep{nori2023medprompt} enhances AI-driven medical question answering by combining few-shot exemplar selection with a two-stage process: preprocessing correct GPT-4 training set responses as few-shot examples, and an inference step employing a k-Nearest Neighbours (kNN) lookup and ensemble refinement for answer generation. This approach ensures high accuracy and reliability by using only the most relevant and validated examples. Medprompt's effectiveness is demonstrated in its superior performance on the MedQA benchmark, showcasing its potential for medical diagnostics. In this work, we do not employ the kNN approach as it requires a training and test set split. We believe this might provide Medprompt an unfair advantage in this evaluation as other protocols do not use a training set. Therefore, we only implement subcomponents of the full Medprompt, specifically, question randomization with few-shot chain-of-thought ensembling~\cite{nori2023medprompt}.


\begin{table*}[t!]
 \centering
 \rowcolors{2}{Gray}{white}
 \begin{adjustbox}{width=\linewidth,center}
 \begin{tabular}{lcccccc}
    % \toprule % Top line
    \textbf{System} & \textbf{\begin{tabular}[c]{@{}c@{}}Flexible \\ Round Numbers\end{tabular}} & \textbf{Judge} & \textbf{Summarizer} & \textbf{\begin{tabular}[c]{@{}c@{}}Sequential \\ Interactions\end{tabular}} & \textbf{\begin{tabular}[c]{@{}c@{}}Multi API \\ Calls\end{tabular}} & \textbf{\begin{tabular}[c]{@{}c@{}}Asymmetric \\ Agents\end{tabular}} \\
    \midrule % Middle line separating header from content
     Medprompt                &              &            &            & \checkmark & \checkmark &            \\
    Society of Minds         & \checkmark   &            &            & \checkmark & \checkmark &            \\
    Ensemble Refinement      &              & \checkmark & \checkmark & \checkmark & \checkmark &            \\
    ChatEval                 &  \checkmark  & \checkmark & \checkmark & \checkmark & \checkmark &            \\
    Self-consistency         &              &            &            &            & \checkmark &            \\
    Single-agent             &              &            &            &            &            &            \\
    Solo Performance Prompting                      & \checkmark   &            &            & \checkmark &            & \checkmark \\
    Multi-Persona            & \checkmark   & \checkmark &            & \checkmark & \checkmark & \checkmark \\
   
    \bottomrule % Bottom line
 \end{tabular}
\end{adjustbox}
 \caption{Feature comparison of various debating systems. The presence of a feature is indicated by a checkmark.} \label{tab:feature_comparison}
\end{table*}


We summarize the features of these various systems in Table~\ref{tab:feature_comparison}.
Each strategy determines the high-level debate prompts and how the agents share answers and histories to collaborate. However, in each case, there are multiple possible agent-level prompts available, including: 
(1)~zero-shot Q\&A prompt,
(2) zero-shot chain-of-thought (CoT)~\citep{kojima2022large}, 
(3) few-shot examples~\citep{brown2020language} which provides five Q\&A examples but no reasoning,
(4) Solo Performance Prompting (SPP) (or Multi-persona self-collaboration)~\citep{wang2023unleashing} which utilizes a single agent that mimics an internal debate, and
(5) few-shot chain-of-thought (FS-CoT)~\citep{wei2022chain} 
 which combines step-by-step reasoning steps, along with five Q\&A examples and explanations\footnote{The few-shot Q\&A examples with explanations are provided for each medical dataset in~\citet{singhal2023medpalm}. The step-by-step explanations were generated by a panel of qualified clinicians who identified the best examples and crafted the few-shot prompts as part of the Med-PaLM project.}.
We provide the complete lists of all debate and agent-level prompts in Appendices \ref{app:debate_prompts} and~\ref{app:agent_prompts}. 


\begin{figure*}[t!]
  \centering
    %\includegraphics[width=0.49\textwidth]{imgs/total_acc_vs_avg_sec_per_question_medqa.pdf}
    {\includegraphics[width=0.54\textwidth]{imgs/MedQA_Total_cost_with_x_scatter_plots.pdf}}
    {\includegraphics[width=0.41\textwidth]{imgs/medqa_total_acc_box_with_x.pdf}}
    \caption{\textit{Benchmark of experiment configurations on MedQA dataset.} \textbf{Left}: Accuracy vs average cost (\$) per question. The size of the dots reflects the average number of API calls required per question. 
    \textbf{Right}: Summarizes accuracy grouped by strategy, sorted by average performance (black dot).
    The {\color{red}X} represents improved performance using our proposed \emph{agreement modulation}, described in Section \ref{sec:agreement_modulation}.} 
    \label{fig:medqa_scatter}
\end{figure*}

\section{Experiments} \label{experiments}
% Our first contribution is a comprehensive suite of MAD experiments, minimally modifying prompts when necessary, i.e.\ where the original prompts are task-dependent. 
% We provide a unified API and open-source implementations of each strategy. 
%We run over 200 different configurations and provide results and insights below. 

As base agents for the MAD implementations, we utilize GPT-3~\citep{brown2020language} with the 3.5-turbo engine. GPT-3 is a large-scale transformer-based generative LLM~\citep{vaswani2017attention, kaplan2020scaling} available via API calls. Although not the current most powerful model available via API, we selected GPT-3.5 for its optimal balance between performance and cost efficiency, crucial for conducting a broad range of experiments, whereas more powerful models would've been prohibitively expensive. However, we informally note that some of our preliminary experiments with more advanced models like GPT-4 suggested similar findings, although these tests were limited to a smaller subset of data. %, 
%GPT4~\citep{openAI2023gpt4} 
% and PaLM2~\citep{anil2023palm} (a successor to PaLM~\cite{chowdhery2022palm}). 

% \paragraph{Q\&A Datasets}
We evaluate each system using seven datasets: three medical datasets, and three more general datasets requiring reasoning. 
For the medical tasks, we follow the evaluation protocol in Med-PaLM2~\citep{singhal2023medpalm} and evaluate the above strategies on the following multiple-choice question-answering datasets: 
\begin{itemize}[leftmargin=18pt]
    \item \textbf{MedQA}~\citep{jin2021disease} comprising of 1273 general medical knowledge questions from the US medical licensing exam (USMLE). Each question has 4-5 answer choices. In this work, we focus on the 4-answer version of MedQA as used to evaluate Med-PaLM2~\cite{liang2023encouraging} and Medprompt~\cite{nori2023medprompt}.
 \item \textbf{PubMedQA}~\citep{jin2019pubmedqa} containing 500 open domain questions, context and answers. 
 \item \textbf{MMLU} (clinical topics only)~\citep{hendrycks2020measuring} consisting of 123 medical questions covering anatomy, clinical knowledge, college medicine, medical genetics, professional medicine, and college biology.
\end{itemize}

The other datasets are difficult reasoning tasks, based on the assumptions that MAD strategies might particularly shine on datasets requiring complex and commonsense reasoning and be better able to escape reasoning traps.

\begin{itemize}
\item \textbf{CosmosQA}~\citep{2019CosmosQA}: A dataset of 7,000 general knowledge questions focusing on commonsense-based reading comprehension. It requires understanding narratives and interpreting causes and effects not explicitly mentioned. We subsampled 500 questions for economic and time feasibility.
\item \textbf{CIAR} ~\citep{liang2023encouraging}: The Counter-Intuitive Arithmetic Reasoning (CIAR) dataset consists of 50 multiple-choice questions. It is designed to evaluate reasoning abilities of LLMs, challenging them to use slow, logical thinking over intuitive responses. We added at the end of the question the two suggested answers to turn it into a multiple-choice question-answering task. 
\item \textbf{GPQA}~\citep{rein2023gpqa}: This dataset contains 448 multiple-choice questions written by domain experts in biology, physics, and chemistry. It tests the limits of both human experts and AI systems, with questions that are `Google-proof' and demand high-level understanding and reasoning.
\item \textbf{Chess}~\citep{chessstatetracking}: This benchmark task focuses on the ability of AI systems to understand and track the state of a chess game based on natural language descriptions. The task is divided into subtasks created from real games played on Lichess and synthetic games generated by random search over the chess game tree. To account for the effect of context length, the subtasks are further divided into Short, Medium, and Long. Each subtask consists of 1,000 games. We use the Short setting in our study.
\end{itemize}


We measure additional agent-level and debate-level metrics (comprehensive lists of all additional metrics are provided in Appendices~\ref{app:all_debate_metrics} and \ref{app:all_agent_metrics}). 
Examples of agent-level metrics include whether an individual agent answered a given question correctly or not and the debate round in which it first provided the correct answer.  
%the number of rounds it was correct for; 
%whether or not it changed its answer during the debate, and whether this was based on another agent's correct/incorrect answer; 
%the average prompt/response token length and costs (US Dollars \$), along with the average time to answer each question. 
Examples of debate-level metrics include whether \textit{any} agent involved in the debate provided a correct answer and 
whether the agents came to a consensus by the final round.
%the number of agents that changed their answers; 
%and the number of unique first answers.


\paragraph{Results}
First, we attempt to see whether various prompting protocols affect performance significantly enough to justify their API costs. In Figure~\ref{fig:medqa_scatter}, we present a scatter plot of the results of each experiment on the MedQA dataset, while equivalent analyses for the other datasets are given in Appendix~\ref{app:results_overview}.
In the left panel, we show the accuracy vs cost (measured in USD), where the size of each point reflects the average number of API calls required per question (we also plot accuracy vs time, and accuracy vs average prompt length in Appendix~\ref{app:results_overview}). These variations, where applicable, include changing the number of agents, rounds, reasoning and aggregation steps, altering agent prompts, implementing round summarization, adjusting sampling parameters, and modifying other system-specific hyperparameters. For a full breakdown of the experimental configurations used for each system, see Appendix~\ref{app:all_exps}. %  \ngr{To do: describe briefly what the hyperparameters are (i.e why so many dots for each system?)} 
In the right panel, we summarize accuracy over all configurations per strategy. 

We see the highest performing debating strategy from those introduced in Section~\ref{sec:mad-strategies} is SoM with multiple different configurations achieving 61\% on MedQA. However, the recently introduced Medprompt strategy~\cite{nori2023medprompt}, which does not use debate, performs the best overall. It also has lower costs associated with it.
Somewhat concerning for the domain of medical Q\&A, is that the single agent's performance, along with self-consistency, can be manipulated via prompts to achieve a high variance. 
We also note that Multi-Persona performs consistently about 7\% worse than the Medprompt strategy. We revisit this in Section \ref{sec:agreement_modulation}.

\begin{table*}[t!]
 \centering
  \begin{adjustbox}{width=\linewidth,center}
 \begin{tabular}{lccccccc}
    \textbf{System} & \textbf{MedQA} & \textbf{PubMedQA} & \textbf{MMLU} & \textbf{CosmosQA} & \textbf{CIAR} & \textbf{GPQA} & \textbf{Chess}\\
    \hline
    % \mathbf{
    Medprompt            & \textbf{0.65} (0.63) & \textbf{0.77} (0.77) & 0.74 (0.73) & \textbf{0.48} (0.47) &0.54 (0.50) & 0.27 (0.25) & 0.32 (0.30)\\
    Society of Mind      & 0.64 (0.61) & 0.74 (0.71) & 0.73 (0.70) & 0.44 (0.39) &0.\textbf{56} (0.46) & 0.27 (0.25)& 0.26 (0.25) \\
    Ensemble Refinement  & 0.64 (0.60) & 0.74 (0.72) & 0.76 (0.74) & 0.45 (0.40) &0.48 (0.46) & 0.32 (0.26) & 0.32 (0.25)\\
    ChatEval             & 0.60 (0.60) & 0.75 (0.73) & 0.71 (0.69) & 0.45 (0.43) &0.48 (0.43) & 0.26 (0.25) & 0.32 (0.23)\\
    Self-Consistency     & 0.60 (0.60) & 0.74 (0.72) & \textbf{0.78} (0.75) & 0.46 (0.46) &\textbf{0.56} (0.52) & 0.24 (0.29) & 0.27 (0.21)\\
    Single Agent         & 0.60 (0.59) & 0.75 (0.70) & 0.76 (0.72) & 0.45 (0.43) &0.50 (0.50) & \textbf{0.33} (0.28) & 0.27 (0.18)\\
    Multi-Persona        & 0.58 (0.57) & 0.70 (0.69) & 0.72 (0.69) & 0.46 (0.42) & 0.52 (0.50) & 0.29 (0.29) & \textbf{0.33} (0.29)\\
    \bottomrule % Bottom line
 \end{tabular}
\end{adjustbox}

     \caption{Best performance achieved by each system on various datasets. The highest score for each system on each dataset is listed, with the median performance over the datasets shown in parentheses.} \label{tab:best_config}
\end{table*}
\begin{figure*}[ht!]
\centering
% \begin{subfigure}[b]{0.3\linewidth}
% \includegraphics[width=\linewidth]{imgs/medqa_total_acc_box.pdf}
% \caption{MedQA Total Accuracy}
% \end{subfigure}
\begin{subfigure}[b]{0.3\linewidth}
\includegraphics[width=\linewidth]{imgs/pubmedqa_total_acc_box.pdf}
\caption{PubMedQA Total Accuracy}
\end{subfigure}
\begin{subfigure}[b]{0.3\linewidth}
\includegraphics[width=\linewidth]{imgs/mmlu_total_acc_box.pdf}
\caption{MMLU Total Accuracy}
\end{subfigure}
\begin{subfigure}[b]{0.3\linewidth}
\includegraphics[width=\linewidth]{imgs/cosmosqa_total_acc_box.pdf}
\caption{CosmosQA Total Accuracy}
\end{subfigure}
\begin{subfigure}[b]{0.3\linewidth}
\includegraphics[width=\linewidth]{imgs/ciar_total_acc_box.pdf}
\caption{CIAR Total Accuracy}
\end{subfigure}
\begin{subfigure}[b]{0.3\linewidth}
\includegraphics[width=\linewidth]{imgs/gpqa_total_acc_box.pdf}
\caption{GPQA Total Accuracy}
\end{subfigure}
\begin{subfigure}[b]{0.3\linewidth}
\includegraphics[width=\linewidth]{imgs/chess_total_acc_box.pdf}
\caption{Chess Total Accuracy}
\end{subfigure}
\caption{Boxplots illustrating the distribution of total accuracy across different datasets for each QA system. These plots demonstrate the variability and robustness of each system's performance.}
\label{fig:boxplots}
\end{figure*}

%
%ChatEval using simultaneous talk with a summarizer with 2 agents, for 3 rounds. However, SoM (XX agents YY rounds) and ER ( with few-shot CoT prompts) both performed highest on average across the three datasets, suggesting a more stable debate strategy. 
%For a detailed description of all the configurations run for each strategy, see Appendix~\ref{app:all_exps}. 

% Overall, we see a general upward trend in performance as the cost and the average number of API calls increase. This positive correlation highlights the utility of prompting strategies and demonstrates that generally speaking, the more token-compute that is used per question the better performance becomes. 
% However, we can see that certain MAD strategies do not significantly outperform the best-performing non-debating approaches (e.g. Medprompt) when using a carefully crafted prompt, which can be significantly cheaper.

While there is a noticeable trend indicating improved performance with increased cost and API usage, our findings reveal that the relationship between performance and resource investment is complex and highly dependent on specific hyperparameters and dataset/system configurations. Notably, some MAD strategies do not consistently outperform more cost-effective non-debating approaches like Medprompt, even with optimized prompting. This suggests that, beyond a certain threshold, additional computing does not guarantee better results, and performance is both nuanced and contingent on the right combination of hyperparameters and system design. This variability underscores the need for further evaluations, as undertaken in the following section, to develop a clearer picture of these trends.



% \ngr{It would be cool to give a more general conclusion. What do we conclude? Does it hold for the other datasets? What is the general conclusion at the stage for the full sweep dataset/system?
% My take: expect for very specific dataset/system combination that clearly work or does not work well, performance are both noisy and hyperparameters-dependant. Systems might perform better or worse than single-agent alternatives depending on the chosen hyperparameters, and there is hardly a correlation between cost and performance.
% }



% COST ANALYSIS:
% er_cot + FS	1:0 - single_agent, GPT	GPT3.5	MedQA Score = 0.56	Cost = $6
% er_debate_cot	er_cot + FS	3:9 - ER, GPT	GPT3.5	MedQA Score = 0.6	Cost = $83


\paragraph{The utility of debate}
Next, we consider the utility of debate in prompting strategies. Recent protocols like Ensemble Refinement~\cite{singhal2023medpalm} and Medprompt~\cite{nori2023medprompt} have deviated away from debating altogether, raising questions about its efficacy. Recall that we include three standard medical datasets (MedQA, PubMedQA and MMLU), while also including three other reasoning datasets (CosmosQA, CIAR and GPQA). These datasets are constructed in such a way as to require small amounts of logical deduction to answer successfully. This might give debating protocols an advantage as there are multiple rounds of outputs between agents, which could be used to build on logical arguments before concluding.  We now evaluate all our system setups across these seven datasets. The scores (out of 1.0) for the best-performing configurations for each QA system can be found in Table \ref{tab:best_config}.

Table \ref{tab:best_config} indicates that no protocol dominates on all datasets. Medprompt seems to perform the best overall. Interestingly enough the debating protocols do not outperform the other protocols on the reasoning datasets. To further investigate this we provide box plots, in Figure \ref{fig:boxplots}, which show the distribution of performances for each QA system across the other datasets (MedQA's results can be found in Figure \ref{fig:medqa_scatter}). These boxplots help to visualize the variability and robustness of each system under different conditions.

The protocols with the most variance in performance seem to be Ensemble Refinement, Self-Consistency and single-agent implementations. Medprompt seems to perform consistently well, except on the GPQA dataset, with low variance. It seems that MAD protocols are more sensitive to hyperparameters. We investigate this further in the next section.

\paragraph{Is MAD simply sensitive to hyperparameters?}

To benchmark MAD's sensitivity to hyperparameter choices, we adopt a K-fold approach. We divide the medical and non-medical datasets into two groups. For each method and each dataset, we gather the hyperparameter combination which has the highest average accuracy on the two held-out datasets of the same category, and use this combination to compute the final accuracy.

The results are presented in Figure~\ref{fig:k_fold}. Notably, within medical datasets, every evaluated system outperforms single-agent methods, with the exception of Multi-Persona. Self-Consistency and Medprompt emerge as the top performers. Conversely, in the context of non-medical datasets, single-agent methods surpass all evaluated systems except for Multi-Persona and Self-Consistency.

\begin{figure*}[t!]
  \centering
    \includegraphics[width=0.45\textwidth]{imgs/aggr_system_accuracy_med_default_aggreement.pdf}
    \includegraphics[width=0.45\textwidth]{imgs/aggr_system_accuracy_gen_default_agreement.pdf}
    \caption{\emph{Comparing medical vs non-medical datasets}. The red line indicates the average performance of single-agent systems. \textbf{Left}: Accuracy averaged over the three medical datasets.
    \textbf{Right}: Accuracy averaged over the three non-medical datasets.}
    \label{fig:k_fold}
\end{figure*}


These findings indicate that hyperparameter choices significantly influence system performance, with optimal settings tending to be dataset-specific. Moreover, they underscore that superior performance of various protocols over a standard single-agent approach is not assured for new datasets. It appears that dataset-specific fine-tuning is crucial for maximizing performance.


\paragraph{Measuring the performance boost from debating}\label{sec:boost_debating}


In Figure \ref{fig:relative_improvements}, we observe the relative improvement in accuracy achieved by the debate process compared to the initial answer provided by the first agent. Notably, the performance varies across the three systems: ChatEval exhibits a modest enhancement, Society of Mind shows a substantial increase, and Multi-Persona, intriguingly, leads to a decrease in performance. This divergence in outcomes, coupled with the findings in Table \ref{tab:best_config} which reveal only minor differences in final accuracy among these systems, suggests that the initial quality of responses in the first round may vary significantly.

\begin{figure}[htb!]
  \centering
    \includegraphics[width=0.45\textwidth]{imgs/relative_accuracy_improvement.pdf}
    \caption{Relative accuracy improvements, averaged on all datasets, between the answer of the first agent given during the first and last round, as well as with the final answer.}
    \label{fig:relative_improvements}
\end{figure}

\begin{figure*}[t!]
  \centering
    \includegraphics[width=0.45\textwidth]{imgs/agreement_vs_accuracy_datasets_averaged_non0.pdf}
    \includegraphics[width=0.45\textwidth]{imgs/agreement_real_first_vs_accuracy_datasets_averaged.pdf}
    \caption{\textbf{Left}: Multi-Persona's accuracy according to the agreement fixed by the prompt.
    \textbf{Right}: Multi-Persona's accuracy according to the averaged agreement during the first debate round.}
    \label{fig:agreement_dataset}
\end{figure*}

\begin{figure*}[t!]
  \centering
    \includegraphics[width=0.45\textwidth]{imgs/prompt_agreement_vs_accuracy.pdf}
    \includegraphics[width=0.45\textwidth]{imgs/agreement_vs_accuracy.pdf}
    \caption{\textbf{Left}: Accuracy on USMLE as we increase agreement intensity in our prompt.
    \textbf{Right}: Accuracy vs actual induced debate agreement.}
    \label{fig:improvements}
\end{figure*}

An unexpected aspect of this analysis is that the Multi-Persona system reduces the overall performance compared to relying solely on the initial response of the first agent. This can be attributed to the role of the second agent (the ``devil''), which is deliberately designed to contradict or disagree, even if the initial response was correct. We show in the next section that this prior willingness for different agents to agree with each other at the outset of a debate, is indeed a very important ``hyperparameter". 

\paragraph{Improving MAD via agreement modulation}\label{sec:agreement_modulation}
Our previous experiments led us to hypothesise that the degree to which agents agree with one another during a debate may significantly affect debating performance. 
Here we test this hypothesis by developing a new MAD prompting strategy that modulates (via prompts) the degree to which agents within a debate agree with each other at the outset. Our prompt takes the following simple form: ``\textit{you should agree with the other agents X\% of the time}''. We call X in this prompt the agent's \textit{agreement intensity}. 

Multi-Persona is an ideal testbed for this method, as it uses only two agents with different system prompts. Specifically, we provide the ``angel'' agent with the question, and we modulate the disagreement using the ``devil'''s system prompt. Figure~\ref{fig:agreement_dataset} shows the effect of modulating the agent's agreement on the 6 datasets. We can see that this parameter has a significant effect on the performance and that modulating the agreement in the prompt has the effect of modifying the agreement on the first debate round, which in turn affects the outcome and the final accuracy. Strikingly, the tendency is not always the same for each dataset: while MedQA and PubMedQA directly benefit from a high agreement, CIAR follows a reverse pattern. As CIAR was created to be counter-intuitive, it is therefore probable that prompting the second agent to strongly disagree raises the chance of arriving at the correct answer during the debate. 

To investigate further, we select the highest performing configurations of debating approaches from Figure~\ref{fig:medqa_scatter}, namely SoM, ChatEval and Multi-Persona, and a subset of MedQA dataset (376 multi-choice USMLE Q\&A~\citep{han2023medalpaca}). We opted for this subset due to its high-quality questions.
% , in contrast to the complete MedQA dataset, which includes unanswerable questions that reference images.
% 
In Figure~\ref{fig:improvements} (left), we plot the performance of each strategy as we increase the prompted agreement intensity from zero to 100\%. 
Figure~\ref{fig:improvements} (right) we plot the accuracy vs the actual observed debate consensus, i.e.\ how frequently all the agents agree upon a final answer at the end of the debate. 

We can see that modulating the agreement intensity in this way provides a substantial ($\approx$15\%) improvement in performance for Multi-Persona, and ($\approx$5\%) for SoM on the USMLE dataset. ChatEval, on the contrary, is hardly affected by this prompting mechanism, suggesting that the agent's agreement is not as easily modulable for every system.
Building on this finding, we apply the 90\% agreement intensity agent prompts to Multi-Persona on the full MedQA dataset and demonstrate (as far as we are aware) a new state-of-the-art result (for GPT-3), highlighted in Figure~\ref{fig:medqa_scatter} by the red cross symbol. 

% \paragraph{Debate Insights}
% We have demonstrated that multi-agent debate is a useful tool to improve performance in the medical Q\&A domain. However, one might wonder whether the additional performance is worth the (in some cases substantial) additional cost and time to generate answers. 
% %Ideally one wants the increase in accuracy to be worth the additional cost and time it takes to generate answers. To start, 
% To help answer this question, we plot the performance of each configuration compared to accuracy, US dollar cost (of API calls), number of tokens used and average time to complete each question in Figure~\ref{fig:results}


% As can be seen, there is a general upward trend in performance as the cost, time and token lengths increase. This typically means that debate setups with more agents, rounds and prompting styles that require step-by-step thinking, perform better. However, this is not always the case.

\paragraph{Analysis of debating behaviour} \label{app:additional-metrics}
% Because we work with debating strategies, we can measure specific debate-related metrics. Specifically, we track the following metrics:
% \begin{itemize}
%     \item Whether agents have reached a consensus at the end of a debate (out of a maximum of 1.0 if all agents agree all the time).
%     \item Whether any agent got the correct answer at least once in any of the rounds.
%     \item The number of agents that changed their answers at least once throughout the debate.
%     \item The number of unique answers presented in the first round.
% \end{itemize}
Our experiments section concludes with an exploration of the dynamics within MAD systems, specifically through their performance on the MedQA test set. We assess Multi-Persona, ChatEval, and Society of Mind, focusing on key aspects such as consensus accuracy, independent correct answer identification, the adaptability reflected in answer changes, and the breadth of initial perspectives. These facets are depicted in Figure \ref{fig:debate_metrics}, offering insights into each system's strategic approach to debate and problem-solving.

% \begin{figure*}[t!]
%   \centering
%     \includegraphics[width=0.49\textwidth]{imgs/prompt_agreement_vs_accuracy.pdf}
%     \includegraphics[width=0.49\textwidth]{imgs/agreement_vs_accuracy.pdf}
%     \caption{\textit{The impact of agent agreement on MAD performance.} \textbf{Left}: Accuracy on USMLE as we increase agreement intensity in our prompt.
%     \textbf{Right}: Accuracy vs actual induced debate agreement.}
%     \label{fig:improvements}
% \end{figure*}

\begin{figure*}[hbt!]
  \centering
   \begin{subfigure}[b]{0.55\textwidth}
    \includegraphics[width=\textwidth]{imgs/best_debate_metrics.pdf}
    \caption{Best system runs}
  \end{subfigure}
   \begin{subfigure}[b]{0.42\textwidth}
    \includegraphics[width=\textwidth]{imgs/mp_debate_metrics.pdf}
    \caption{Multi-Persona}
  \end{subfigure}
  \begin{subfigure}[b]{0.54\textwidth}
    \includegraphics[width=\textwidth]{imgs/ce_debate_metrics.pdf}
    \caption{ChatEval}
  \end{subfigure}
  \begin{subfigure}[b]{0.44\textwidth}
    \includegraphics[width=\textwidth]{imgs/som_debate_metrics.pdf}
    \caption{Society of Mind}
  \end{subfigure}
  
  \hfill
  
  
  \hfill
  \caption{\emph{Debating Behavior of MAD Strategies.} The first plot compares the top runs for each of the systems, while the other three plots compare the top three runs for each system. Each plot quantifies the performance on MedQA by measuring several factors: the accuracy of the final consensus among agents (scaled to 1.0), the instances of any agent identifying the correct answer independently (also scaled to 1.0), the frequency of agents changing their answers during the debate, and the diversity of initial answers provided. The descriptions for each axis include the range of scores observed, from minimum to maximum.}
  \label{fig:debate_metrics}
\end{figure*}

The four spider plots offer a more detailed analysis of the debating behavior of MAD strategies, providing insights into the effectiveness of each system through various metrics. The initial plot compares the three systems, with Society of Mind (SoM) notably exhibiting the largest surface area, suggesting it outperforms others across all debating metrics. This is further supported in the subsequent plots, which analyze the top three runs for each system and show that larger areas are indicative of better performance. These visualizations emphasize the importance of a balanced system, indicating that excellence in one aspect is not enough; a system must perform well across multiple dimensions of the debate process. A full description of each of the metrics is provided in Appendix \ref{app:all_debate_metrics}.

For example, when agents change their answers during a debate, this reevaluation must lead to more accurate conclusions. Arbitrary changes without a strategic basis could prevent consensus. This underlines the need for a balanced approach where agents not only reconsider their positions but also enhance their decision-making accuracy, contributing to the system's overall effectiveness.

\paragraph{Evaluating using other APIs}
We assess the capability of GPT-4 \citep{openai2024gpt4} and Mixtral 8x7B \citep{jiang2024mixtral} on the MedQA dataset, applying the optimal agreement modulation value identified for Multi-Persona with GPT-3.5 on USMLE. Results shown in Figures~\ref{fig:gpt4_medqa_scatter} and~\ref{fig:mixtral_medqa_scatter}.

\begin{figure*}[t!]
  \centering
    %\includegraphics[width=0.49\textwidth]{imgs/total_acc_vs_avg_sec_per_question_medqa.pdf}
    {\includegraphics[width=0.54\textwidth]{imgs/GPT4_MedQA_Total_cost_scatter_plots.pdf}}
    {\includegraphics[width=0.41\textwidth]{imgs/GPT4_medqa_total_acc_box.pdf}}
    \caption{\textit{Benchmark of experiment configurations of GPT-4 on MedQA dataset.} \textbf{Left}: Accuracy vs average cost (\$) per question. The size of the dots reflects the average number of API calls required per question. 
    \textbf{Right}: Summarizes accuracy grouped by strategy, sorted by average performance (black dot).} 
    \label{fig:gpt4_medqa_scatter}
\end{figure*}

\begin{figure*}[t!]
  \centering
    {\includegraphics[width=0.54\textwidth]{imgs/Mixtral_MedQA_Total_cost_scatter_plots.pdf}}
    {\includegraphics[width=0.41\textwidth]{imgs/Mixtral_medqa_total_acc_box.pdf}}
    \caption{\textit{Benchmark of experiment configurations of Mixtral 8x7B on MedQA dataset.} \textbf{Left}: Accuracy vs average cost (\$) per question. The size of the dots reflects the average number of API calls required per question. 
    \textbf{Right}: Summarizes accuracy grouped by strategy, sorted by average performance (black dot).} 
    \label{fig:mixtral_medqa_scatter}
\end{figure*}

The results from GPT-3.5 suggest that hyperparameter settings can be effectively transferred to GPT-4. However, this transferability does not extend well to Mixtral 8x7B. This discrepancy might be attributed to architectural differences, which could affect how hyperparameters influence model performance. We leave this for future research.

\paragraph{Code availability} Source code for this work, including all MAD implementations and configurations, is publicly available at \url{https://github.com/instadeepai/DebateLLM}.

\section{Conclusions} \label{sec:conclusion}
The investigations detailed in this work have demonstrated that MAD approaches currently do not outperform other ensembling methods such as Medprompt and self-consistency using their original implementations. 
MAD typically requires a higher number of API calls, increasing the number of tokens to produce and process and ultimately, the total running cost of the system. Interestingly, we found that this might primarily be due to MAD protocols being more sensitive to hyperparameters and not necessarily that debate inherently performs worse than single-agent methods.
To strengthen this claim, we demonstrated that a simple prompt-based manipulation of the degree to which agents agree with each other, referred to as the agreement intensity, could provide significant performance gains. By tuning this agreement intensity for Multi-Persona, it went from being the worst-performing protocol to the best-performing, beating out methods such as Medprompt and Self-Consistency.  
Therefore, while MAD systems currently trail behind other prompting strategies, we believe they hold significant potential to transform the prompting landscape. To foster further exploration, we have made our code repository open-source, for others to easily reproduce all our experiments and further pursue interesting related research directions.
% \vspace{-1.9mm}
\paragraph{Limitations} We utilize API calls to a publicly available LLM~\citep{brown2020language} which, whilst sufficient in the context of our investigation, exposes us to variable inference time calls and unforeseen model updates. Moreover, large-scale API-based benchmarking incurs substantial financial and time costs, which both limit the experiment scales and provide a barrier of entry to replication and extension efforts.  For these reasons, future works could extend this line of work using open-source models and in-house infrastructure.

%We have provided a comprehensive experiment suite, along with open-source implementations, of single and multi-agent debate strategies for medical Q\&A using generative LLMs. We highlight key insights and ``zoomed in'' on agent consensus within debates, proposing a novel MAD prompting strategy based on \ins{\textit{agreement intensity}} that outperforms previously published strategies on MedQA. 

%\paragraph{Limitations} Our work thus far relies on API calls to publicly available LLMs~\citep{brown2020language,anil2023palm}. Whilst sufficient for our preliminary investigations, we are at the mercy of external factors such as the speed of inference time calls, plus any model parameter updates that are made ``behind the scenes''. 
%Furthermore, given the substantial financial and time costs of running large scale experiments relying on such API calls, we are only able to run each experiment once using one seed and therefore cannot provide error bounds in our plots. 
%Finally, we have also observed that instruction fine-tuned agents~\citep{ouyang2022training,chung2022scaling} more reliably provide a single capital letter, and therefore do not provide incorrectly parsed answers to the questions as often. 
%Finally, whilst we record the number of incorrectly parsed answers (where an agent does not provide a single capital letter answer to the multiple choice question), we have observed that instruction fine-tuned agents~\citep{ouyang2022training,chung2022scaling} can more reliably be prompted for this. 

%We do not know the exact training data used for some of the base models. For example, if we had used MedAlpaca~\citep{han2023medalpaca}, it has been fine-tuned using USMLE question and answers, potentially contaminating our train/test splits. 

% \paragraph{Future work} 
% We intend to alleviate some of the issues with external API calls by extending our work to use in-house LLMs. This would give us the ability to leverage novel methods for fine-tuning LLMs via debate feedback, potentially from a judge or summarizer agent. 
% Other potential directions include investigating how the diversity of agents can affect the quality of the debate, and how specialist LLMs can be utilized to provide expert answers to generalist agents such as GPT4~\citep{openAI2023gpt4}. 

% Do not include this in the blind review version of the paper.
% \section{Acknowledgements} \label{sec:acknowledgements}
% TODO: Mention Kale-ab Tessera here.


\section*{Impact Statement} \label{sec:impact_statement}
% This paper presents work whose goal is to advance the field of Machine Learning. There are many potential societal consequences of our work, none which we feel must be specifically highlighted here.
This study furthers the field of machine learning by evaluating multi-agent debate (MAD) strategies and other prompting methods within question-answering datasets. Our results show that MAD approaches could be as effective as, or even superior to, other strategies like Medprompt and Self-Consistency. An open-source toolkit is provided to promote additional research in this domain. While this research aims to improve the precision of large language models (LLMs) in answering questions, with a significant focus on healthcare applications, it is crucial to consider the potential risks associated with these advancements. Specifically, medical question-answering systems can sometimes yield incorrect predictions in unforeseen ways, and there is a risk that these systems may exhibit undue confidence in their erroneous responses. Such inaccuracies, especially in the medical domain, could lead to misinformation or misdiagnosis, potentially impacting patient care and outcomes. Therefore, while this work contributes valuable insights and tools for enhancing LLM capabilities, it is imperative to proceed with caution, incorporating robust validation, transparency, and ethical considerations to mitigate risks and ensure these technologies are deployed in a manner that safeguards public health and trust.

%\newpage
\small

% \bibliographystyle{unsrtnat}
\bibliography{icml2024}
\bibliographystyle{icml2024}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% APPENDIX
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\newpage
\appendix
\onecolumn
\input{appendix}

\end{document}