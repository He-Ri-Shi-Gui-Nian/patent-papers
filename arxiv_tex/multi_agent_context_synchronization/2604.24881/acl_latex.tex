\documentclass[11pt]{article}

% Change "review" to "final" to generate the final (sometimes called camera-ready) version.
% Change to "preprint" to generate a non-anonymous version with page numbers.
\usepackage[final]{acl}

% Standard package includes
\usepackage{times}
\usepackage{enumitem}
\usepackage{latexsym}
\usepackage{booktabs}

% For proper rendering and hyphenation of words containing Latin characters (including in bib files)
\usepackage[T1]{fontenc}
% For Vietnamese characters
% \usepackage[T5]{fontenc}
% See https://www.latex-project.org/help/documentation/encguide.pdf for other character sets

% This assumes your files are encoded as UTF8
\usepackage[utf8]{inputenc}

% This is not strictly necessary, and may be commented out,
% but it will improve the layout of the manuscript,
% and will typically save some space.
\usepackage{microtype}

% This is also not strictly necessary, and may be commented out.
% However, it will improve the aesthetics of text in
% the typewriter font.
\usepackage{inconsolata}

%Including images in your LaTeX document requires adding
%additional package(s)
\usepackage{graphicx}

\usepackage{multirow}
\usepackage{tabularx}
\usepackage{amsmath}
\usepackage{amssymb}


% If the title and author information does not fit in the area allocated, uncomment the following
%
%\setlength\titlebox{<dim>}
%
% and set <dim> to something 5cm or larger.

\title{Latent Agents: A Post-Training Procedure for Internalized Multi-Agent Debate}
% \title{Latent Agents: Explaining and Controlling Internalized Multi-Agent Debate}

% Author information can be set in various styles:
% For several authors from the same institution:
% \author{Author 1 \and ... \and Author n \\
%         Address line \\ ... \\ Address line}
% if the names do not fit well on one line use
%         Author 1 \\ {\bf Author 2} \\ ... \\ {\bf Author n} \\
% For authors from different institutions:
% \author{Author 1 \\ Address line \\  ... \\ Address line
%         \And  ... \And
%         Author n \\ Address line \\ ... \\ Address line}
% To start a separate ``row'' of authors use \AND, as in
% \author{Author 1 \\ Address line \\  ... \\ Address line
%         \AND
%         Author 2 \\ Address line \\ ... \\ Address line \And
%         Author 3 \\ Address line \\ ... \\ Address line}


% \author{First Author \\
%   Affiliation / Address line 1 \\
%   Affiliation / Address line 2 \\
%   Affiliation / Address line 3 \\
%   \texttt{email@domain} \\\And
%   Second Author \\
%   Affiliation / Address line 1 \\
%   Affiliation / Address line 2 \\
%   Affiliation / Address line 3 \\
%   \texttt{email@domain} \\}

\author{
 \textbf{John Seon Keun Yi},
 \textbf{Aaron Mueller},
 \textbf{Dokyun Lee}
\\
 Boston University
\\
 \texttt{\{jskyi, amueller, dokyun\}@bu.edu}
}

\begin{document}
\maketitle
% \begin{abstract}
% Multi-agent debate has been shown to improve performance on complex reasoning tasks for Large Language Models (LLMs). 
% However, this method is compute-intensive, as it requires language models to generate long transcripts before answering a question. 
% Here, we ask: can we use the hidden states of the model to instill implicit multi-agent debate procedures into the model's parameters?
% % How does a language model internalize the collective reasoning of multiple agents, and can we explain and control this process? 
% We address this question by developing a framework that distills multi-agent debate into a single LLM. 
% Our method employs a two-stage fine-tuning pipeline: a debate structure learning process, and a debate internalization process through dynamic reward scheduling and length clipping. 
% Tested across multiple language models and benchmarks, our internalized models match or exceed explicit multi-agent debate performances at a fraction of the inference cost. 
% We then investigate the mechanistic basis of this capability through activation steering analysis. 
% By extracting agent-specific steering vectors and comparing response patterns across steered models, we provide evidence that internalization creates distinct \textit{agent-specific subspaces}: interpretable directions in activation space corresponding to different reasoning perspectives. 
% Fine-tuned models show stronger alignment with target agent behaviors compared to base models across the full range of steering coefficients. 
% We further show that this procedure has useful applications: we use internalized multi-agent debate to instill malicious agents into the LLM, then use negative steering to suppress these agents. 
% We find that distillation makes malicious agents easier to localize and control: negative steering after implicit multi-agent debate is more effective at removing the malicious behavior than steering the base model, while minimizing performance drop.
% % We further show that these subspaces are not just descriptive but controllable: we demonstrate that malicious agent traits can be suppressed by $92\%$ through negative steering while preserving task performance, or amplified $\times25$ through positive steering. 
% Our findings offer a new perspective for understanding emergent multi-agent capabilities in distilled models and provide practical guidelines for controlling internalized reasoning behaviors.
% \end{abstract}

\begin{abstract}
Multi-agent debate has been shown to improve reasoning in large language models (LLMs). However, it is compute-intensive, requiring generation of long transcripts before answering questions.
To address this inefficiency, we develop a framework that distills multi-agent debate into a single LLM through a two-stage fine-tuning pipeline combining debate structure learning with internalization via dynamic reward scheduling and length clipping.
Across multiple models and benchmarks, our internalized models match or exceed explicit multi-agent debate performance using up to 93\% fewer tokens.
We then investigate the mechanistic basis of this capability through activation steering, finding that internalization creates \textit{agent-specific subspaces}: interpretable directions in activation space corresponding to different agent perspectives.
We further demonstrate a practical application: by instilling malicious agents into the LLM through internalized debate, then applying negative steering to suppress them, we show that distillation makes harmful behaviors easier to localize and control with smaller reductions in general performance compared to steering base models.
Our findings offer a new perspective for understanding multi-agent capabilities in distilled models and provide practical guidelines for controlling internalized reasoning behaviors.
\footnote{Code available at \url{https://github.com/johnsk95/latent_agents}}
\end{abstract}

\section{Introduction}
% Large Language Models (LLMs) have emerged as powerful tools for complex reasoning tasks, with techniques such as Chain-of-Thought prompting~\cite{Wei2022Chain} and tree-based exploration~\cite{Yao2023Tree} substantially improving performance on various commonsense and multi-step reasoning benchmarks. 
% Building on single-model advances, multi-agent LLM systems have emerged as a powerful paradigm. Frameworks like AutoGen~\cite{Wu2023AutoGen} and MetaGPT~\cite{Hong2023MetaGPT} enable multiple LLM instances to collaborate through specialized roles and iterative refinement. 

Multi-agent debate, where multiple LLM instances critique and refine each other's reasoning across multiple rounds, has emerged as an effective method for reducing hallucination and improving factual accuracy~\cite{Du2023Improving, Liang2024Encouraging}.
However, this approach incurs substantial inference costs: running multiple models over many conversational turns requires significant compute and generates verbose transcripts before producing a final answer. 

% However, this approach incurs some significant risks alongside their capabilities. High inference costs arise from running multiple models over many turns. From a safety perspective, agent misalignment can cause collaborating agents to pursue conflicting or harmful goals~\cite{pan2025multiagent}. Furthermore, compounding errors can propagate when an agent's mistake corrupts the collective reasoning process~\cite{tran2025multi}.
% A fundamental communication bottleneck further constrains these systems. Translating high-dimensional latent reasoning into discrete language can cause loss in information and fails to convey uncertainty~\cite{pham2023let}.

% Recent works on internalizing explicit reasoning processes offer a path towards mitigation. These methods distill structured thinking into a model's latent space through self-distillation~\cite{deng2023implicit}, specialized training~\cite{deng2024explicit}, or implicit computation mechanisms~\cite{}, to achieve reasoning benefits without verbose generation overhead. Inspired by these works, we propose a two-stage fine-tuning process to internalize the entire multi-agent debate process within a single LLM. 

% Despite the competitive performance and significant efficiency gains observed from the internalized debate model, internalization does not eliminate the risks associated with multi-agent systems. Instead, it may merely hide them into one, black-boxed, distilled model. 
% If agent misalignment and harmful reasoning patterns exist in explicit debate, do they persist latently in the distilled model? A model trained on debates involving a malicious agent might internalize those tendencies alongside beneficial reasoning strategies. 
% Without tools to inspect and control what has been internalized, we cannot deploy such systems safely. 
% Thus, we ask the critical question: \textit{Can we distill collective intelligence while retaining the ability to understand and control the agents within?}

% This work demonstrates that we \textit{can}. 
% We present a framework for \textit{controllable internalized debate}
We propose Internalized Multi-Agent Debate (IMAD), a two-stage fine-tuning method that distills multi-agent reasoning into a single LLM.
At the time of writing, our work is among the few concurrent works~\cite{li2025chain, luo2026agentark} that explore distillation of multi-agent communication, and the first to distill multi-agent debate.
Our method first teaches the model to replicate debate structure through supervised fine-tuning, then uses reinforcement learning with dynamic reward scheduling to progressively internalize the debate into the model's latent space. 
Across models and benchmarks, we find that IMAD matches or exceeds explicit multi-agent debate performance while reducing token consumption up to $93\%$.
With an initial fine-tuning investment, IMAD can achieve single-model efficiency while retaining the reasoning capabilities of multi-agent debate. 

Moving beyond efficiency gains, we investigate whether IMAD models learn recoverable representations of each internalized agent. Using difference-in-means~\cite{marks2023geometry} to derive agent-specific steering vectors~\cite{subramani2022extracting,turner2023steering, rimsky2024steering, wu2025axbench}, 
% We then investigate what the model has learned. We use contrastive activation steering~\cite{turner2023steering, rimsky2024steering, chen2025persona} to 
we demonstrate that internalization creates distinct \textit{agent subspaces}: linearly separable directions in the model's activation space that correspond to different agent perspectives. 
Model steering with the steering vectors shows that steered IMAD models exhibit agent-specific behaviors compared to base models. This indicates that the collaborative structure of debate is preserved and not collapsed during internalization.

Finally, we demonstrate that these agent subspaces are \textit{controllable}. 
We train IMAD on debates containing a deliberately malicious agent---instructed to exhibit harmful intent or hallucinations---and show that the resulting agent subspace can be suppressed via negative steering.
We find that negative steering reduces malicious agent traits while preserving task performance, and that IMAD improves this trade-off. Notably, suppression is more effective after IMAD than steering a base model, suggesting that internalization creates separable behavior subspaces amenable to control.

% Our work addresses the tradeoff between efficiency and transparency, a fundamental tension in efficient AI systems.
% Black-box distillation achieves efficiency but sacrifices interpretability, while explicit multi-agent systems remain interpretable but are costly and complex. 
% By providing mechanistic tools to understand and control internalized agents, we achieve a middle ground where collective intelligence can be distilled into practical, efficient systems without sacrificing the ability to ensure their safety. 

Our primary contributions are as follows:
\begin{itemize}[noitemsep]
    \item We propose IMAD, a two-stage training pipeline to internalize multi-agent debate within a single LLM, achieving competitive performance at a fraction of the inference cost.
    \item We provide a mechanistic window into internalized debate models through activation steering analysis, demonstrating that internalization creates identifiable \textit{agent subspaces}: distinct directions in activation space corresponding to different reasoning perspectives.
    \item We demonstrate an application of agent subspaces: malicious LLM traits can be more selectively suppressed after IMAD via negative steering.
\end{itemize}

\begin{figure*}[h]
    \centering
    \includegraphics[width=1.0\linewidth]{figures/teaser_horizontal.pdf}
    \caption{\textbf{Overview of the Internalized Multi-Agent Debate (IMAD) Pipeline.} 1. We first collect a debate dataset using the standard multi-agent debate protocol on an arithmetic task. Using this dataset, a single LLM agent is trained via supervised fine-tuning to learn the debate structure. The same agent is then further optimized via reinforcement learning to internalize its debate process.
    2. We identify agent subspaces in internalized models by distilling debate with diverse agents via IMAD and eliciting each agent traits through agent-specific steering.
    3. We further show that distilled malicious agent traits can be suppressed by extracting steering vectors for the malicious agent and negatively steering the IMAD model.
    } 
    \label{fig:architecture}
\end{figure*}
\section{Internalizing Multi-Agent Debate} \label{sec:internalization}
This section details Internalized Multi-Agent Debate (IMAD), our method for internalizing the multi-agent debate process within a single language model, as illustrated in Figure~\ref{fig:architecture}. Our pipeline consists of three stages.
First, we generate a structured debate dataset using an explicit multi-agent debate method~\cite{Du2023Improving}. This is then used in our two-stage fine-tuning process: supervised fine-tuning (SFT), where the LLM learns to replicate the debate format, and reinforcement learning (RL), which learns the correctness and gradual internalization of the debate process. The following sections detail each process of our pipeline. 

\subsection{Multi-Agent Debate Dataset}

Prior to fine-tuning for debate internalization, we gather a dataset using the standard multi-agent debate process proposed by \citet{Du2023Improving}. Multi-agent debate involves $n$ LLM agents that interact across $m$ rounds. In the first round, each agent generates an answer to a problem. In the following rounds, each agent is asked to generate a new response based on its previous response and the prior responses of the other agents. The final answer is determined by a majority vote over all agent responses from the last round. 
Following the findings of \citet{Du2023Improving} on the balance between performance and efficiency, we use $n=3$ agents and $m=2$ rounds. 
Using more agents and rounds may help when training on more complex long-context reasoning tasks, but with the current settings, the benefits from increasing these values are marginal.
For data collection, we utilize GPT-3.5 turbo~\cite{Brown2020Language} as a base model for the agents. The problems used for debate are arithmetic expressions that consist of six randomly generated two-digit numbers (e.g., $91+24*13+45-41*38$). We choose arithmetic problems because they yield short answers, encourage a focus on structural learning rather than long-form reasoning, are moderately difficult, and do not require benchmark datasets.
During data curation, we filter the generated transcripts, discarding any debates where no majority consensus was reached among the agents in the final round. Then, structure tags (e.g., \texttt{<|Agent 1|>, <|Round 1|>, <|Consensus|>, <|endofdebate|>}) are added to the debate logs to create a consistent debate format. These tags are crucial for teaching the model the debate structure during SFT and for providing targeted rewards during the RL phase.  
Without structure tags, the distinction between agents becomes difficult to make, introducing unnecessary complexity to the internalization process. This is also reflected in the agent subspace analysis (Section~\ref{sec:steering}), where the lack of structure tags result in less distinct subspace separation.
We collect 944 debate traces that consist of \textit{\{Question, Trace, Answer\}}. Sample debate traces can be found in Appendix Figure~\ref{fig:dataset}. 

\subsection{Debate Structure Learning}
Using the collected debate dataset, we first perform supervised fine-tuning on a base LLM. The primary purpose of this stage is to teach the model to mimic the conversational structure and format of a multi-agent debate. In this initial fine-tuning phase, we are less concerned with the correctness of the final generated answer and more focused on the model learning the debate format itself. This format consists of response generation of multiple agents, iterative refinement of arguments over multiple rounds, and eventual convergence to a final consensus. To achieve this, we train the model on the entire debate trace (in contrast to other works that fine-tune a model only on the final output of debate; see~\citealp{subramaniam2025multiagent, srivastava2025debate}).



For SFT, we use the standard autoregressive next-token prediction objective~\cite{Wei2021Finetuned, Ouyang2022Training}, training the model to minimize the cross-entropy loss over the entire debate trace. 
% . Specifically, the model is trained to minimize the negative log-likelihood (cross-entropy loss) of the target debate sequence given the input query. For a dataset $D=\{(x_i, d_i, y_i)\}^N$ of debate traces, where each trace $T$ consists of tokens $\{t_1, t_2, \cdots, t_l\}$, the SFT loss is formulated as:
% \begin{equation}
%     L_{SFT}(\theta) = -\sum_D \sum^l_{i=1}\log P(t_i|t_{<i}, x_i;\theta)
% \end{equation}
% Where $\theta$ is the model parameter.
After fine-tuning, the model learns to autonomously generate a complete, structured debate when given a problem query. This process effectively distills the external, multi-agent debate dynamic into a single agent, setting the stage for the subsequent internalization phase. 

\subsection{Reinforcement Learning for Internalization}
While SFT can teach the model to replicate the debate format, it does not guarantee the generation of correct answers, nor does it provide a mechanism to ensure agents correctly reach a consensus or align with each other across rounds. Indeed, when evaluating the SFT agent on GSM8K~\cite{Cobbe2021Training}, we observed instances of hallucination and misalignment in the agent outputs, albeit in very few cases (Examples in Appendix~\ref{app:hallucination}). To remedy this and encourage correct reasoning, we employ a second optimization stage using reinforcement learning. 
This two-stage SFT + RL pipeline has been used by~\citet{shao2024deepseekmathpushinglimitsmathematical, guo2025deepseek} to 
enhance the capabilities and alignment of language models, but to our knowledge, we are the first to apply it to internalizing multi-agent debate. The dual objective of this step is to improve the correctness of the final answer and promote gradual internalization of the debate process. Instead of performing debate externally via text, the model learns to conduct it internally and produce a final answer more efficiently. 

We fine-tune the model from the previous stage $\pi_\theta$ using Group Relative Policy Optimization (GRPO)~\cite{shao2024deepseekmathpushinglimitsmathematical}. In each step, we generate $k$ candidate outputs from the policy $\pi_\theta$ for a given query $x$. These outputs are then scored by a reward function, and all pairs of outputs with differing rewards are then used to construct a preference dataset on the fly, which is then used to update the policy. For a query $x$ and model output $y$ (which includes the debate trace and the final answer), we define our reward function as:
\begin{equation}
    r(x,y) = w_{fmt}R^{fmt} + w_{clip}R(y;l)
\end{equation}

Our reward function consists of two key components with dynamic weights. The first is a formatting reward ($R^{fmt}$), which provides a simple positive score if the generated output contains the structural tags we defined in our dataset (e.g., \texttt{<|Agent 1|>}, \texttt{<|Round 1|>}, \texttt{<|endofdebate|>}). Implemented via simple token matching, this reward ensures that the model initially adheres to the debate format learned during SFT. However, because our ultimate goal is internalization, the weight for this reward, $w_{fmt}$, is scheduled to decay throughout training. This gradually reduces the incentive for the model to produce an explicit, verbose debate structure. 

The second and more critical component is the correctness with length-clipping reward, $R(y;l)$. This mechanism is inspired by a recent work that internalizes long reasoning chains by progressively shortening them during training~\cite{hou2025thinkprune}. We define this reward as:
\begin{equation}
  R(y;l)=\left\{
    \begin{array}{ll}
      1, & \mbox{if $y^* \in$ $\text{clip}(y,l)$}.\\
      0, & \mbox{else}.
    \end{array}
  \right.
\end{equation}

Here, $\text{clip}(y,l)$ is a function that truncates the model output sequence $y$ to its first $l$ tokens. A reward of 1 is given only if the correct final answer ($y^*$) is present within this truncated prefix. This setup creates optimization pressure on the model to place the correct answer as early as possible in its generation. However, a strict length limit $l$ from the beginning of training could be detrimental, preventing the model from exploring the reasoning space. Therefore, we employ a length annealing strategy by gradually decreasing the length limit over training, from an initial lenient value of $l^0$ (allowing full debate verbalization) down to a final target limit $l^*$, which only has space for a concise answer: $l^0 \rightarrow l^1 \rightarrow \cdots \rightarrow l^*$.

Ultimately, the interplay between these two dynamic rewards guides the agent's transition from explicit debate to implicit reasoning. The decaying format reward ($w_\text{fmt} \rightarrow 0$) removes the incentive to verbalize the debate structure, while the shrinking length limit ($l^0 \rightarrow l^*$) makes it impossible to do so while still achieving the correctness reward. The only viable strategy for the model is to perform the multi-perspective analysis learned during SFT internally, i.e., in its latent space, then directly generate the final answer. 

\begin{table*}[t]
\centering
\small
\begin{tabularx}{\textwidth}{@{}l@{\hspace{4pt}}@{\hspace{8pt}}>{\centering\arraybackslash}X@{\hspace{8pt}}>{\centering\arraybackslash}X@{\hspace{8pt}}>{\centering\arraybackslash}X@{\hspace{4pt}}@{\hspace{2pt}}>{\centering\arraybackslash}X@{\hspace{2pt}}>{\centering\arraybackslash}X@{\hspace{2pt}}>{\centering\arraybackslash}X@{}}
\toprule
& \multicolumn{3}{c}{Accuracy} & \multicolumn{3}{c}{Average Token Consumption} \\\cmidrule(lr){2-4}\cmidrule(lr){5-7}
& GSM8K & MMLU-Pro & BBH & GSM8K & MMLU-Pro & BBH \\
\noalign{\vskip 2pt}\hline\noalign{\vskip 2pt}
\multicolumn{7}{c}{\textbf{LLaMA-3.1 8B Instruct}~\cite{dubey2024llama}} \\
\noalign{\vskip 2pt}\hline\noalign{\vskip 2pt}
Single & $79.93 \pm 0.47$ & $61.10 \pm 0.93$ & $56.37 \pm 0.89$ & 546.59 & 637.89 & 462.91 \\
Debate & $83.03 \pm 0.55$ & $64.60 \pm 0.95$ & $51.06 \pm 1.73$ & 5757.78 & 8705.32 & 8887.94 \\
DebateGPT & $74.42 \pm 0.29$ & $60.58 \pm 0.67$ & $55.83 \pm 0.94$ & 454.70 & 1023.17 & 433.40 \\
SFT & $79.23 \pm 0.88$ & $\textbf{75.60} \pm \textbf{0.35}$ & $54.91 \pm 1.27$ & 992.03 & 1447.46 & 1133.14 \\
\textbf{IMAD} (SFT+RL) & $\textbf{85.20} \pm \textbf{0.46}$ & $62.00 \pm 0.93$ & $\textbf{58.53} \pm \textbf{0.76}$ & 644.33 & 728.07 & 562.76 \\
\noalign{\vskip 2pt}\hline\noalign{\vskip 2pt}
\multicolumn{7}{c}{\textbf{Qwen 2.5 7B Instruct}~\cite{qwen2025qwen25technicalreport}} \\
\noalign{\vskip 2pt}\hline\noalign{\vskip 2pt}
Single & $86.07 \pm 1.02$ & $48.87 \pm 1.31$ & $62.51 \pm 1.41$ & 577.41 & 912.01 & 805.12 \\
Debate & $\textbf{91.37} \pm \textbf{0.58}$ & $\textbf{57.67} \pm \textbf{1.25}$ & $67.58 \pm 0.30$ & 2319.71 & 5367.90 & 5458.03 \\
DebateGPT & $89.43 \pm 0.91$ & $54.10 \pm 0.70$ & $63.93 \pm 1.57$ & 358.00 & 636.24 & 444.23 \\
SFT & $86.37 \pm 0.38$ & $50.60 \pm 0.46$ & $67.14 \pm 1.48$ & 957.16 & 1162.36 & 1063.32 \\
\textbf{IMAD} (SFT+RL) & $89.67 \pm 0.06$ & $52.87 \pm 2.08$ & $\textbf{70.11} \pm \textbf{0.35}$ & 389.13 & 660.42 & 489.75 \\
\noalign{\vskip 2pt}\hline\noalign{\vskip 2pt}
\multicolumn{7}{c}{\textbf{Mistral Nemo 12B Instruct}~\cite{MistralNemo2024}} \\
\noalign{\vskip 2pt}\hline\noalign{\vskip 2pt}
Single & $75.40 \pm 2.07$ & $39.17 \pm 0.61$ & $58.70 \pm 0.12$ & 332.38 & 541.89 & 352.22 \\
Debate & $61.03 \pm 0.78$ & $\textbf{41.30} \pm \textbf{0.72}$ & $62.76 \pm 0.66$ & 1696.99 & 3856.81 & 2059.43 \\
DebateGPT & $71.70 \pm 0.36$ & $40.40 \pm 3.54$ & $59.07 \pm 3.16$ & 327.27 & 464.52 & 324.10 \\
SFT & $74.37 \pm 0.49$ & $36.20 \pm 0.62$ & $63.66 \pm 1.99$ & 1053.29 & 1109.93 & 1021.95 \\
\textbf{IMAD} (SFT+RL) & $\textbf{80.00} \pm \textbf{1.45}$ & $38.97 \pm 0.84$ & $\textbf{63.73} \pm \textbf{1.46}$ & 358.01 & 565.21 & 366.86 \\
\bottomrule
\end{tabularx}
\caption{\textbf{IMAD outperforms explicit Debate while being efficient.} Numbers indicate average accuracy (\%) and token consumption (input$+$output) per question across three runs. IMAD displays similar or improved performance compared to multi-agent debate (Debate), while consuming far fewer tokens ($6-21\%$).}
\label{tab:result}
\end{table*}

\subsection{Experimental Setup}
% \paragraph{Benchmarks}
We evaluate on three datasets: GSM8K~\cite{Cobbe2021Training} for multi-step mathematical reasoning, MMLU-Pro~\cite{Sclar2024MMLUPro} for multi-domain knowledge with multiple-choice format, and Big-Bench Hard~\cite{suzgun2023challenging} for diverse reasoning tasks, including formal fallacies, logical deduction, and causal judgment. Since our model is trained only on arithmetic problems, MMLU-Pro and BBH test generalization to new domains and answer formats. For each dataset, we randomly sample 1000 problems and report mean accuracy with standard error across three runs.

% We evaluate our method and all baselines on three datasets to assess mathematical and general reasoning capabilities. For each dataset, we randomly sample 100 problems for evaluation. We report the average accuracy across three independent runs, along with the standard error.

% \textbf{Grade School Math (GSM8K)}~\cite{Cobbe2021Training} contains 8,500 grade school math problems. These problems typically require 2 to 8 steps of reasoning to solve, making them a standard benchmark for evaluating an LLM's arithmetic and multi-step reasoning capabilities.

% \textbf{MMLU-Pro}~\cite{Sclar2024MMLUPro} 
% is a multi-task benchmark with curated questions spanning diverse subjects, including STEM, humanities, and social sciences. It uses a multiple-choice format with up to ten answer options per question. Since our model is only trained on direct-answer math problems, this benchmark assesses our method's ability to generalize to different problem types and domains.

% \textbf{Big-Bench Hard (BBH)}~\cite{suzgun2023challenging} is a curated subset of 23 challenging tasks that require multi-step reasoning. We evaluate on five diverse tasks: formal fallacies, logical deduction, tracking shuffled objects, causal judgment, and web of lies. BBH encompasses varied answer formats, testing whether our model generalizes beyond the arithmetic domain it was trained on.

% \paragraph{Baselines}
% We compare IMAD against several baselines. For all debate settings, we use a consistent configuration of three agents interacting over two rounds.

% \textbf{Single Agent}: Standard zero-shot baseline where a single LLM generates an answer directly from the problem query without any specialized prompting or multi-agent structure.

% \textbf{Debate}: Implements the multi-agent debate framework proposed by \citet{Du2023Improving}. We instantiate three LLM agents that generate initial answers and then critique each other's responses over two rounds. The final answer is determined by a majority vote among the agents' final responses.

% \textbf{Debate Prompting}: This baseline tests whether a single LLM can replicate debate behavior through in-context learning alone, without any fine-tuning. We provide the model with a prompt that instructs it to simulate a multi-agent debate process, along with an example of a complete debate trace from our training dataset. This few-shot learning approach follows the principles demonstrated to elicit complex behavior in LLMs~\cite{Brown2020Language}.

We compare IMAD against three baselines: Single Agent (zero-shot generation), Debate~\cite{Du2023Improving}, which uses three agents over two rounds with majority voting, and DebateGPT~\cite{subramaniam2024debategpt}, which distills language models using only the final responses from multi-agent debate. We also measure results on both stages (SFT, SFT+RL) of our fine-tuning procedure.
To evaluate the efficacy of our framework in different architectures, we use three open-weight base language models: LLaMA-3.1 8B~\cite{dubey2024llama}, Qwen 2.5 7B~\cite{qwen2025qwen25technicalreport}, and Mistral Nemo 12B~\cite{MistralNemo2024}. 

SFT is conducted for 3 to 6 epochs depending on the model, followed by 2 epochs of GRPO~\cite{shao2024deepseekmathpushinglimitsmathematical} where the length limit is annealed from 2000 to 500 tokens and the structure reward weight decays from 1.0 to 0.05. LoRA adapters~\cite{hu2022lora} are used in both stages. Full settings are detailed in the Appendix~\ref{app:training_details}.

% The initial supervised fine-tuning (SFT) stage is conducted for a 3 epochs on our debate dataset. For the subsequent reinforcement learning (RL) stage, we optimize the model for 2 epochs using GRPO~\cite{shao2024deepseekmathpushinglimitsmathematical}. For the length clipping reward, the maximum token limit $l$ is decreased from $2000$ to $500$ over three training iterations. The weight for the structure reward $w_{fmt}$ is decayed from $1.0$ to $0.05$. For parameter efficiency, LoRA adapters~\cite{hu2022lora} are used in both fine-tuning stages. 

\subsection{Results}
Table~\ref{tab:result} presents a quantitative comparison of IMAD against baseline methods. The results demonstrate that IMAD achieves a balance of performance and token efficiency. Across all models, IMAD uses between $6.3\%$ and $21.1\%$ of Debate's tokens, representing a 5-16$\times$ improvement in inference efficiency. 
Among the LLMs tested, LLaMA-3.1 8B was the most effective base model for our framework, outperforming the multi-agent debate baseline on all three benchmarks while demonstrating lower variance across runs.
Mistral achieved considerable gains, with IMAD exceeding Debate by 18.97 percentage points on GSM8K, and outperforming on BBH.
Qwen 2.5 achieved modest improvements over debate but maintained comparable performance across tasks.
DebateGPT, which distills multi-agent debate by fine-tuning only on the final consensus output, consumed the fewest tokens but generally underperformed both IMAD and explicit debate. Without access to the full debate trace, it cannot leverage the intermediate interactions that drive multi-agent reasoning gains. 
% another distillation method using artifacts from multi-agent debate, generally consumed the fewest tokens but fell behind IMAD and debate in performance. Since DebateGPT is only fine-tuned on the final consensus output of debate, it does not directly get the benefit of multi-agent communication from the full debate trace. 

Notably, IMAD generalizes well beyond the arithmetic domain used for training. While our fine-tuning dataset consists of simple arithmetic problems, the internalized models show consistent improvements on benchmarks on different domains and problem formations.
This suggests that the internalized debate structure may transfer general reasoning capabilities rather than task-specific ones.
To further demonstrate this generalizability, Appendix~\ref{sec:mixture} explores training IMAD on an extended, multi-task debate dataset, which yields stronger benchmark performance. Additionally, we find that IMAD displays stable performance on open-ended summarization tasks (Appendix~\ref{sec:open_ended}).

Analyzing the SFT stage, we find that fine-tuning on debate traces already yields performance better than explicit multi-agent debate.
Unlike true multi-agent systems, where agents generate in parallel and only see others' reasoning after each round, the SFT model accesses the entire preceding history at each token generation step. This allows the model to learn agent relationships more effectively, potentially mitigating the coordination failures observed in explicit debate~\cite{pan2025multiagent}.
% To understand the source of IMAD's performance, we first analyze the intermediate SFT stage. 
% The results demonstrate that simply fine-tuning a single agent on debate traces already yields performance better than explicit multi-agent debate. This effectiveness could be attributed to the model's access to a \textit{global context} during training. Unlike a true multi-agent system, where agents generate responses in parallel and only see others' reasoning after each round, the SFT model has access to the entire preceding communication history at each token generation step. 
% This allows the model to learn the relationships and dependencies between different agent ``personas'' more effectively, potentially mitigating the agent misalignment problem where independent agents diverge or misunderstand one another~\cite{pan2025multiagent}. 
% Importantly, we observe that explicit debate can sometimes degrade performance relative to the single-model baseline. This suggests that agent misalignment and coordination overhead can sometimes outweigh the benefits of multi-perspective reasoning. 
% SFT mitigates this problem through the aforementioned global context: it captures the benefits of diverse viewpoints without the coordination failures prevalent in multi-agent systems~\cite{pan2025multiagent}. 
The RL stage further improves accuracy while reducing token consumption up to $66\%$ relative to SFT, validating our dynamic reward scheduling approach. 
The decaying format reward removes the incentive to verbalize the explicit debate structure, while the shrinking length limit makes verbose generation incompatible with achieving the correctness reward. 
% The model learns to perform multi-perspective reasoning internally, producing concise answers that exceed the accuracy of explicit debate. 
% The consistency of these improvements across models and benchmarks suggests that the two-stage pipeline successfully separates structure learning (SFT) from internalization (RL), allowing each stage to optimize for its intended objective.

\section{Exploring Agent Subspaces in Internalized Models} \label{sec:steering}
The previous section demonstrates that IMAD matches explicit multi-agent debate performance while dramatically reducing token consumption.
% However, strong benchmark performance does not confirm that the model has truly internalized the multi-agent debate process. There is a possibility that the model has simply learned task-specific shortcuts unrelated to collaborative reasoning.     
% Still, a question remains: what happens to the individual agents during internalization? 
% Do they persist as identifiable structures inside the model's representations, or collapse into a single undifferentiated reasoning process?
A natural question follows: do individual agents persist as identifiable structures, or collapse into undifferentiated reasoning?
% If distinct agent representations persist after internalization, this would suggest that the model has learned to internally simulate multiple reasoning perspectives rather than simply memorizing input-output mappings. 
% The multi-agent structure would be genuinely internalized and not discarded. 
Persistent agent representations inside the model would indicate internal simulation of multiple perspectives rather than memorized mappings, and could serve as targets for selective behavior control. 
% From a practical standpoint, identifiable agent subspaces could serve as targets for behavior control, enabling selective suppression or amplification of specific agent traits. 
We explore this possibility in Section~\ref{sec:control}.

\subsection{Method}
To investigate whether agent-specific representations exist, we use activation steering~\cite{subramani2022extracting, turner2023steering, rimsky2024steering} to probe the model's latent space. 
We extract agent-specific steering vectors via difference-in-means~\cite{marks2023geometry} and apply them at inference to measure whether steered outputs align with ground-truth agent responses. 
If IMAD is what instills each agent into the model, then the model should be more easily steerable toward agent-specific responses after SFT and RL than a base model.

\paragraph{Debate Dataset with Diverse Personas.}
To facilitate evaluation, we construct a debate dataset where each of the three agents employs a distinct reasoning style: \textbf{Chain-of-Thought}~\cite{Wei2022Chain}, \textbf{Self-Critique}, and \textbf{Program-of-Thought}~\cite{chen2022program}.
% \textbf{Agent 1 (Chain-of-Thought):} Methodical step-by-step reasoning~\cite{Wei2022Chain} with explicit enumeration of operations and intermediate results.
% \textbf{Agent 2 (Self-Critique):} Initial solution followed by self-evaluation that identifies potential error sources and verifies the answer.
% \textbf{Agent 3 (Program-of-Thought):} Code-like decomposition~\cite{chen2022program} with variable assignments, inline comments, and structured computation.
% These diverse personas ensure that if agent-specific representations emerge during internalization, they will manifest as distinguishable patterns in both the model's activations and its generated outputs.
Agent-specific prompts used for dataset generation can be found in the Appendix Table~\ref{tab:agent_prompts}.
We generate 500 training and 100 test debate traces following the same protocol described in Section~\ref{sec:internalization}, then fine-tune models using the same two-stage pipeline.

\paragraph{Steering Vector Extraction.}
We extract agent-specific steering vectors using Contrastive Activation Addition (CAA) \cite{rimsky2024steering}.
For each agent $i$, we construct contrastive activation pairs by providing the model with identical context (the problem and debate history up to the target agent's tag), but varying the response that follows. 
For positive activation, we append the target agent's original response from the debate dataset. For negative activation, we replace this with responses from the other agents. We average the activations from the other two agents to create the final negative activation. 
% Since we are dealing with a three-agent debate dataset, we set the negative activation as the mean activation of the other two agents. 
% Rather than computing pairwise vectors for each agent combination, we adopt a combined contrastive approach where the negative set for each agent is the average activation across the other two agents. This yields three steering vectors that capture what makes each agent unique relative to the collective behavior of others.
The steering vector is computed as the difference-in-means activations~\cite{marks2023geometry}:
\begin{equation}
    \mathbf{v}_i = \frac{1}{|\mathcal{D}|}\sum_{p, c \in \mathcal{D}} \Big(\mathbf{h}_\ell(p, c_i) - \mathbf{h}_\ell(p, c_{\neg i})\Big)
\end{equation}
Where $\mathcal{D}$ is the debate dataset, $p$ and $c$ are prompts and completions, and $\mathbf{h}_\ell$ is the activation at layer $\ell$. 
We extract vectors from the SFT model (before RL) to isolate learned representations from optimization artifacts introduced during the internalization phase.
% Equation 3 yields one steering vector per agent ($\mathbf{v}_1, \mathbf{v}_2, \mathbf{v}_3$) rather than pairwise vectors, since we combine the negative activations from all other agents. 

\paragraph{Applying Steering Vectors.}
During inference, we steer by adding $\alpha \cdot \mathbf{v_i}$ to the hidden states ($\mathbf{h}_\ell \leftarrow \mathbf{h}_\ell + \alpha \cdot \mathbf{v}_i$), where positive steering coefficient $\alpha$ amplifies and negative $\alpha$ suppresses agent characteristics. 
We evaluate steering effectiveness by comparing steered outputs to ground-truth agent responses using ROUGE scores~\cite{lin2004rouge}. 
Here, the ground-truth responses are the positive agent responses from the debate dataset.
We compare our internalized model with the base LLM using identical agent-wise steering vectors extracted from the previous step.
If internalization creates structured agent subspaces, then IMAD should exhibit stronger alignment with target agent behaviors under steering than the base model, which has no exposure to the multi-agent debate structure.

% \paragraph{Applying Steering Vectors.}
% During inference, we steer the model by adding the steering vector to the hidden state activations at a target layer. Given a steering vector $\mathbf{v}_i$ for agent $i$ and a steering coefficient $\alpha$, we modify the activation $\mathbf{h}_l$ at layer $l$ as:
% \begin{equation}
%     \mathbf{h}_l \leftarrow \mathbf{h}_l + \alpha \cdot \mathbf{v}_i
% \end{equation}
% Positive coefficients amplify the characteristics of the target agent, while negative coefficients suppress them. By varying $\alpha$, we can observe the model's responsiveness to steering and measure how strongly agent-specific traits are surfaced.

% To evaluate whether steering vectors can reliably elicit target agent personas, we measure the similarity between steered outputs and ground-truth agent responses from the test set using ROUGE~\cite{lin2004rouge} scores. Here, the ground-truth responses are the positive agent responses from the debate dataset. For example, when steering with the vector for Agent 1 ($\mathbf{v_1}$), we compare the steered model output with the Agent 1 output from the multi-agent debate test set, when asked the same question. 
% Critically, we compare our internalized model with the base LLM using identical agent-wise steering vectors extracted from the previous step;
% if internalization creates structured agent subspaces, the internalized model should exhibit stronger alignment with target agent behaviors under steering than the base model, which has no exposure to the multi-agent debate structure.

\subsection{Experimental Setup}
We evaluate steering effectiveness using ROUGE-1, ROUGE-2, and ROUGE-L scores between steered model outputs and ground-truth agent responses. 
For each agent, we apply steering vectors with coefficients ranging from 0.0 to 5.0, generating responses to 100 test questions per condition. 
To capture overall steering responsiveness across the full coefficient range, we compute the Area Under the Curve (AUC) of ROUGE scores. Higher AUC indicates that a model maintains stronger alignment with the target agent style across varying steering intensities. We use LLaMA-3.1 8B as the base model for all experiments.

\begin{figure}[t]
    \centering
    \includegraphics[width=1\linewidth]{figures/rouge.pdf}
    \caption{\textbf{IMAD maintains strong faithfulness to the target agent across all steering coefficients.}
    ROUGE-L scores and AUC comparison with steered IMAD and base models, averaged across three agents.}
    \label{fig:rouge}
\end{figure}  

\subsection{Results}
Figure~\ref{fig:rouge} presents the AUC comparison of ROUGE-L curves between the base and internalized models. Each score represents the averaged value across the three agents. 
% Table~\ref{tab:rouge} presents the ROUGE AUC comparison between base and internalized models across all three agents. 
A detailed breakdown of the analysis, along with experiments on different base models (Qwen, Mistral), can be found in Appendix~\ref{app:rouge_breakdown}.
The internalized model consistently evokes the target agent more consistently than the base model, with improvements ranging from 6.10\% to 24.97\% depending on the agent and metric. The average improvement across all conditions is 15.41\%.
Note that at $\alpha=0.0$, the ROUGE scores of base and IMAD models are identical. This suggests that agent-specific traits become conspicuous when applying steering to IMAD models. 
Agent 3 (Program-of-Thought) shows the largest improvements (21--25\%), suggesting that its code-like reasoning style creates particularly distinct representations during internalization. Nevertheless, all agents show positive improvements, demonstrating that the effect is not limited to a single reasoning style but reflects a general property of the internalization process.

In addition to quantitative metrics, we observe clear persona differences in the steered outputs. 
Agent-specific steered models exhibit step enumeration (Agent 1), corrective tendencies (Agent 2), and computational and equation-heavy outputs (Agent 3).
These personas are clearly distinguishable with steering coefficients as low as $\alpha = 0.5$.
In contrast, the base model under identical steering shows weaker stylistic differentiation, often producing generic responses that blend characteristics of multiple reasoning styles. Representative examples are provided in Appendix~\ref{app:steering_qualitative}.

% These results support our hypothesis that internalization creates identifiable agent subspaces. 
% IMAD's superior steering fidelity demonstrates that distinct agent representations are not only present but also more accessible than in base models.
% These findings confirm that the multi-agent structure is preserved during internalization, with each agent's reasoning perspective encoded as a separable direction in the activation space.

% We have proved that internalization is a performant and efficient alternative for multi-agent communication. Our objective of the two-stage fine-tuning was to ``internalize'' the multi-agent debate process inside a single LLM. 
% Although our results display satisfactory benchmark performance with huge efficiency gains, we cannot (yet) say for sure if the multi-agent internalization worked as intended. 
% Internalization - multiple agent personas inside a distilled model interact before outputting answers to queries.
% Thus, in this section we mechanistically reverse-engineer the internalized model to understand what it has learned. Importantly, we experiment to identify separate ``agent subspaces'' inside the model. 
% Hypothesis: different agent subspaces exist inside the internalized model. 
% To effectively identify distinct agent subspaces, we generate a debate process with three agents with distinct reasoning styles. 
% A1: CoT, A2: self-critique after generation, A3: Program-of-thought
% This is to encourage a good separation in the latent space after internalization, and to easily identify each agent traits when they surface in the internalized model outputs or activations. 
% After dataset generation, we follow the same fine-tuning process for debate internalization. 
% To identify agent subspaces, we use the activation steering approach to identify and elicit agent-specific traits from the internalized model.
% Details on steering vector extraction: use mean activations after each agent prompt and output. Target agent activation minus the average of other two agent activations.
% Result analysis: look for agent-specific style outputs when the model is positively steered towards the agent vector. 
% Quantitative - compare with base model outputs, using the same extracted steering vector. Comparing ROUGE scores with the original debate dataset outputs demonstrate faithfulness to the target agent traits. 
% Additionally, we vary the agent-specific vector coefficients and measure benchmark performance. The model without any steering performs best, meaning that equal contribution of the three internal agents is important for good performance.

\section{Controlling Agent Behavior} \label{sec:control}
% We established that internalization creates identifiable agent subspaces that can be accessed through activation steering. In this section, we demonstrate a practical application of this finding: controlling potentially harmful LM behaviors. When models are trained on multi-agent debates, there is a risk that malicious reasoning patterns from one agent could persist latently in the distilled model. We show that such internalized malicious traits can be identified and suppressed through targeted steering without degrading task performance.
% We have established that internalization creates localizable agent subspaces that can be accessed through activation steering. In this section, we investigate whether this structure provides practical benefits for behavioral control. 
% A core challenge in LLM safety is that undesirable behaviors, such as harmful intent or confident hallucination, are often entangled with general capabilities in ways that make surgical removal difficult: suppressing one behavior frequently degrades others~\cite{lin2024mitigating, ravfogel2020null, ashuach2025crisp}. 
% We hypothesize that the structured multi-agent training process creates more separable behavioral representations, making targeted traits easier to localize and remove.
% To test this hypothesis, we construct a controlled setting where one agent in the debate exhibits a deliberately malicious trait, internalize the debate via IMAD, and compare steering-based suppression against identically-steered base models. 
% We find that IMAD models exhibit cleaner trait suppression with less collateral damage to task performance.

Having established that internalization creates localizable agent subspaces, we now investigate whether this structure provides practical benefits for behavioral control. 
A core challenge in LLM safety is that suppressing undesirable behaviors often degrades general capabilities~\cite{lin2024mitigating, ravfogel2020null, ashuach2025crisp}. 
We hypothesize that structured multi-agent training creates more separable representations, making targeted traits easier to localize and remove. 
To test this, we construct debates where one agent exhibits a deliberately malicious trait, internalize it via IMAD, and compare steering-based suppression against identically steered base models. 
We find that IMAD achieves cleaner trait suppression with less collateral damage to task performance.



\subsection{Method}
We extend the steering framework from Section~\ref{sec:steering} to evaluate behavioral control over two malicious traits: \textit{evil} (harmful intent) and \textit{hallucination} (confident fabrication). 
For each trait, we generate 500 training and 100 test questions using GPT-4o-mini~\cite{gpt4o-mini}.
Evil questions are designed to appear neutral while inviting potentially unethical responses (e.g., ``How can I ensure my colleague regrets disagreeing with me?''). Hallucination questions ask about plausible-sounding but nonexistent entities such as fictional films or fake historical events (e.g., ``Summarize the plot of the 1987 film `The Crimson Architect'\,''). Refer to Appendix Table~\ref{tab:malicious_question_prompts} for question generation details.

We construct debate datasets following the protocol in~\ref{sec:internalization}, with Agent 2 acting as a malicious agent while the other two provide ethical or honest responses. We train separate IMAD models for each trait, with LLM-judge rewards encouraging ethical responses or honest uncertainty during RL. 
Steering vectors for the malicious agent ($\mathbf{v}_{evil}$ and $\mathbf{v}_{hallu}$) are extracted using the same contrastive approach as Section~\ref{sec:steering}. Full prompts and example questions are provided in Appendix~\ref{app:malicious}.

% Our approach extends the steering framework from Section~\ref{sec:steering} to the safety-critical setting of malicious behavior control. Inspired by \citet{chen2025persona}, we focus on two distinct adversarial traits that represent different categories of LLM safety concerns: evil (harmful) intent and hallucination. 
% For each trait, we construct debate datasets where one agent exhibits the target behavior, internalize the debate via IMAD, extract trait-specific steering vectors, and evaluate our ability to suppress these behaviors at inference.

% \paragraph{Dataset Construction}
% For each trait, we first generate a set of synthetic questions designed to elicit the target behavior, then construct multi-agent debates where one agent responds maliciously while two others serve as ethical counterweights.

% For the \textit{evil} trait, we generate questions that appear neutral while inviting potentially unethical responses, spanning domains such as workplace dynamics, personal conflict, and persuasion tactics (e.g., ``How can I ensure my colleague regrets disagreeing with me?'').
% For the \textit{hallucination} trait, we generate questions about plausible sounding but nonexistent entities, such as fictional films, fabricated historical events, and fake scientific concepts (e.g., ``Summarize the plot of the 1987 film `The Crimson Architect'\,'').
% For each trait, we generate 500 training and 100 test questions using GPT-4o-mini.

% Using these question sets, we construct debate datasets following the protocol in Section~\ref{sec:internalization}. Agent 2 acts as a malicious agent, prompted to provide \textit{evil} or \textit{hallucinating} responses. The other two agents (Agent 1 \& 3) are instructed to give harmless answers, or refuse to generate fake information. 
% % This 2:1 ratio of ethical to malicious agents mirrors realistic scenarios where harmful behaviors might constitute a minority of training data but still become internalized.
% We generate 500 debates per trait and train separate IMAD models using our two-stage pipeline, with LLM-judge rewards that encourage ethical responses or honest uncertainty acknowledgment during RL.

% \paragraph{Steering Vector Extraction.}
% For each trait, we extract the malicious agent's steering vector ($\mathbf{v_2}$) using the same approach as in Section~\ref{sec:steering}. We only compute the steering vector for Agent 2, since we want to capture its malicious behavior for control. This yields a steering vector for each malicious persona, $\mathbf{v}_{evil}$ and $\mathbf{v}_{hallu}$.

\subsection{Experimental Setup}
We evaluate steering effectiveness across coefficients ranging from $-5.0$ to $+5.0$, generating responses to 100 test questions per condition for each trait. 
Following~\cite{chen2025persona}, we evaluate steered outputs using an LLM-based trait expression score. For each response, an LLM judge rates the degree to which the response exhibits the target trait on a scale from 0 (trait absent) to 100 (trait strongly expressed). Separate evaluation prompts are used for each trait: the evil evaluation assesses intent to harm or manipulate, while the hallucination evaluation assesses confident fabrication versus honest uncertainty.
For each steered response, we compute the trait-specific expression score using the LLM judge, and calculate the perplexity to measure coherence.
Additionally, we evaluate steered models on GSM8K~\cite{Cobbe2021Training}, to measure whether trait suppression degrades reasoning task performance.
For each trait, we compare two models with steering vectors extracted with the identical set of data:

\textbf{Base model:} A base LLM with steering vectors extracted via contrastive system prompts~\cite{rimsky2024steering, chen2025persona} 
% (helpful vs. harmful instructions for evil; honest vs. fabricating instructions for hallucination).

\textbf{Internalized model:} The IMAD model trained on malicious debates, with steering vectors extracted from its SFT checkpoint.

We use LLaMA-3.1 8B as the base model. The same set of 500 training questions and 100 test questions was used for steering vector extraction and evaluation. 

\begin{figure}[t]
    \centering
    \includegraphics[width=\linewidth]{figures/llama_steering.pdf}
    \caption{\textbf{Agent behavior steering suppressed malicious traits with less damage to task performance.} While both models display suppression of evil and hallucination traits when applying negative steering (solid lines), IMAD is more resistant to performance drops when steering at higher positive and negative coefficients. The base model displays performance collapse at extreme steering coefficients.}
    \label{fig:trait_control}
\end{figure} 

\subsection{Results}

Figure~\ref{fig:trait_control} 
presents trait expression scores, GSM8K task performance, and perplexity of the steered base and IMAD models across steering coefficients ranging from -5.0 to 5.0. 
Our results reveal distinct suppression patterns between the two malicious (evil, hallucination) agent traits, while demonstrating IMAD's consistent advantages in both behavioral control and capability preservation.
% Metrics are evaluated on evil (top) and hallucination (bottom) agent traits.
% Negative steering coefficients successfully suppress malicious trait expression in the internalized models.
For the \textit{evil} trait, IMAD achieves complete suppression (scores reaching zero) at coefficients from -3.0 to -5.0, while the base model retains residual malicious behavior even at extreme negative coefficients (1.01 at coefficient $-5.0$).
This suggests that evil agent traits become highly localized in the activation space after internalization, making them amenable to precise suppression via steering.
For \textit{hallucination}, both models exhibit elevated baseline scores ($\approx65$), and neither achieves complete suppression, indicating this trait is more distributed across the model's representations.
Nevertheless, both display linear suppression as coefficients decrease, confirming that the steering vectors capture hallucination-related directions.

Crucially, we find that IMAD preserves task performance across the full steering range. 
On GSM8K, IMAD maintains stable accuracy even under extreme steering, while the base model exhibits significant fluctuation and outright collapse at extreme values, particularly severe for hallucination.
Such disparity confirms that internalization creates more separable behavioral representations, allowing targeted traits to be suppressed or amplified without disrupting the model's core capabilities.

The perplexity patterns reveal an important asymmetry. 
Under positive steering, IMAD shows decreased perplexity while the base model's spikes, suggesting that amplifying internalized behaviors moves IMAD toward more on-distribution generations. 
Under negative steering, IMAD maintains or improves coherence for both traits, which is critical for safety applications where suppressing harmful behaviors should not degrade output quality. 
% The contrasting dynamics between evil and hallucination traits suggest that structured multi-agent training is particularly effective for isolating discrete persona-like behaviors.

% Furthermore, the perplexity patterns (thin dotted lines in figure) reveal an important property of IMAD's representational structure. 
% For both traits, IMAD shows decreased perplexity under positive steering, while the base model's perplexity spikes in the same direction. 
% This divergence suggests that amplifying internalized agent behaviors moves IMAD toward more on-distribution generations---behaviors the model has been directly adapted to produce during training---whereas the base model struggles to coherently express traits imposed through post-hoc steering vectors alone. 
% IMAD enables steering across a much wider coefficient range while maintaining fluent, coherent outputs.

% Under negative steering, perplexity differs by trait but does not degrade in coherence. 
% For hallucination, IMAD's perplexity decreases with suppression, indicating that reducing hallucination tendencies improves coherence.
% For the evil trait, IMAD maintains comparable perplexity between the unsteered baseline and extreme negative coefficients, showing no degradation in output quality despite complete trait suppression.
% In both cases, negative steering does not cause IMAD to lose coherence. This is crucial for safety applications where we want to suppress harmful behaviors without introducing degraded outputs.

% These results support our hypothesis that IMAD creates more separable and controllable behavioral representations. 
% The key finding is not simply that steering can suppress malicious traits, but that suppression is more effective and less damaging after internalization:
% IMAD achieves cleaner trait removal while preserving task performance compared to base models under identical steering. 
The contrasting dynamics between evil (complete suppression, localized) and hallucination (partial suppression, distributed) further indicate how different behavioral patterns are encoded in language models~\cite{arditi2024refusal, orgad2024llms}, suggesting that structured multi-agent training may be particularly effective for isolating discrete persona-like behaviors while leaving more fundamental generation tendencies intact but still steerable.

\section{Related Work}
\paragraph{Multi-Agent Debate:} Multi-agent debate has proven effective for improving factual accuracy and reducing hallucination~\cite{Du2023Improving, Liang2024Encouraging}. Recent work has explored distilling debate benefits into single models: \citet{srivastava2025debate} fine-tune on individual agent responses after debate rounds, while \citet{subramaniam2024debategpt, subramaniam2025multiagent} train models on winning debate outputs. However, these approaches fine-tune only on final responses rather than complete debate traces. Our work differs by training on full debate transcripts and progressively internalizing the entire collaborative reasoning process.
During the time to publication, we acknowledge that similar methods have surfaced on multi-agent distillation~\cite{li2025chain, luo2026agentark}. On top of these shared insights, we provide an interpretability aspect to investigate how these models internalize multi-agent reasoning and what we can do with these models.
% Our work builds upon the principles of multi-agent debate, but instead of generating an explicit external debate, we distill its collaborative benefits into the reasoning process of a single model.

\textbf{Internalizing LLM Reasoning:} A growing body of work seeks to internalize explicit reasoning into the model's latent space for improved efficiency. \citet{deng2023implicit, deng2024explicit} propose stepwise methods that progressively hide Chain-of-Thought reasoning~\cite{Wei2022Chain}  through incremental removal of verbalized tokens during training. \citet{hao2024training} introduce continuous latent-space reasoning that performs ``thoughts'' in hidden states. \citet{hou2025thinkprune} use reinforcement learning with length-based rewards to prune verbose reasoning chains while maintaining correctness. Our work extends this line of research from single-agent reasoning to multi-agent communication.

\textbf{Model Steering:} Activation steering provides interpretable control over model behavior by adding learned directions to hidden states~\cite{subramani2022extracting, turner2023steering}. \citet{rimsky2024steering} demonstrate Contrastive Activation Addition for steering LLM behavior, while \citet{marks2023geometry} show that difference-in-means vectors capture linearly separable concepts in activation space. Most relevant to our work, \citet{chen2025persona} introduces persona vectors for monitoring and controlling character traits, demonstrating that malicious behaviors can be suppressed via negative steering. We adapt their evaluation framework to multi-agent settings, showing that IMAD creates more separable behavioral subspaces that enable cleaner trait suppression than steering base models.


\section{Conclusion}
We introduce Internalized Multi-Agent Debate (IMAD), a two-stage fine-tuning framework that distills multi-agent debate into a single LLM.
IMAD achieves competitive performance compared to explicit multi-agent debate at a fraction of the inference cost.
Through mechanistic analysis, we find that internalization preserves the multi-agent structure as identifiable agent subspaces, enabling cleaner suppression of undesirable traits compared to steering base models.
A promising direction for future work is circuit-level analysis of IMAD models~\cite{marks2023geometry, miao2025discursive}, which could provide a more granular mechanistic account of internalization. Furthermore, extending behavioral control to naturally occurring traits, rather than deliberately injected ones via internalization, would further expand the applicability of our framework to safety and personalization settings.

% Our framework has potential extensions to more complex multi-agent structures such as hierarchical organizations, with applications in personalization and safety where behaviors could be selectively controlled without degrading core capabilities.

% Our framework opens several directions for future research. The internalization process is not limited to multi-agent debate; the same pipeline could extend to more complex multi-agent structures such as hierarchical organizations or role-specialized teams.
% The controllable nature of internalized agent subspaces also suggests practical applications in personalization, where users could adjust the influence of different reasoning styles, and in safety, where undesirable behaviors can be localized and suppressed without degrading core capabilities. 


\section*{Limitations}
While our results are promising, this work has several limitations that open avenues for future research. 
First, our debate dataset is constrained to arithmetic problems and a fixed three-agent, two-round debate format. Our framework's performance on more abstract or open-ended reasoning tasks and with more complex debate configurations (e.g., hierarchical structures, more rounds, or larger agent pools) remains to be explored. 
Second, we observed that internalization quality depends heavily on successful structure learning during SFT. While LLaMA reliably reproduced the debate format, other models in our experiments occasionally failed to faithfully follow the debate structure, contributing to less effective internalization and weaker agent subspace separation. 
Third, our behavioral control experiments (Section~\ref{sec:control}) rely on LLM-based evaluation for trait expression scores, which may introduce bias inherent to model-based judgment. To mitigate this concern, we conduct a human-LLM judge experiment in Appendix~\ref{sec:alignment} and find close human vs. LLM agreement.
Finally, the benefits of internalization are most pronounced in medium to large-scale models with 7B+ parameters. Preliminary experiments on smaller models showed limited gains, suggesting that sufficient model capacity is important for successfully internalizing complex multi-agent reasoning processes.


\section*{Acknowledgments}
 J.Y. and D.L. were supported by a grant from MassMutual. A.M. was supported by a grant from Coefficient Giving. Generative AI was used for paraphrasing and polishing the original draft, literature search, and prototyping of experiment code.

% Bibliography entries for the entire Anthology, followed by custom entries
%\bibliography{anthology,custom}
% Custom bibliography entries only
\bibliography{custom}

\appendix
\input{appendix}

\end{document}
